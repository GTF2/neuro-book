# 整章尺度 smoke 首次真实运行记录（2026-09-18/19）

开发者于 2026-09-18 授权选择方向并执行；本轮在真实 State Root 与真实 Provider 上首次运行 `smoke:keyframe-scale`。实验未跑完：暴露两个代码缺陷（已修复）与一个实验级发现（待复核）。

## 运行环境

- 命令：`NEURO_BOOK_STATE_ROOT='C:/Users/Administrator/AppData/Local/NeuroBook/data' AGENT_HTTP_BASE_URL='http://127.0.0.1:3000' bun run --cwd packages/neuro-book smoke:keyframe-scale -- --project xin-xiao-shuo`
- dev server：`bun run dev`（Source Dev，指向同一 State Root）
- 模型链路：dev server 已配置的 Provider（config.json 读取，密钥未出进程边界）；实际打到的模型为 `deepseek/deepseek-flash`（session trace 确认）
- 目标项目：`xin-xiao-shuo`（作者真实项目；实验素材全部为 `k-scale-*` / `d-scale-tween-overturn` 临时名）
- 清理选择：默认（finally 自动清理）

## 第 4 次运行：校准后（2026-09-19，开发者拍板「调整校验提示词逐条比对声明与截面」后）

- 校准提交：`8d149080 fix(workflow): 回撞校验员提示词强制逐条比对声明与截面`——校验员系统提示词改为五步逐条作业程序（逐条列项 → 兑现证据 → 对照截面 → 截面为事实基准 → 只报有证据项），校验消息要求先逐条枚举再汇总；回归 3/3 通过。
- 素材：决策 id=7、帧 id=25-28。
- 结果（operations log + job JSON）：
  - 区间1 K1→K2 → **violated**(826.9s)：「芥末此刻在堤下十米处,郭莹完全不知道」被判声明未兑现（正文无芥末在场证据）；帧仍被无条件 settle 为 confirmed（smoke 对区间1无 verdict 断言）。
  - 区间2 K2→K3(埋矛盾) → **violated**(1192.1s)：抓到「这条缝从此不能再用了」未兑现（正文里郭莹留信封维持缝可用）→ 裁决路径 B 完整执行：k-scale-3 推翻留痕(decisionRefId=7)。
  - 区间2重跑 K2→K3-rev(修正帧) → **violated**(1173.6s)：抓住「名单原件」与正文「三张抄件」直接相抵；k-scale-3-rev 仍被无条件 settle 为 confirmed。
  - 区间3 K3-rev→K4(埋未兑现) → **violated**(1199.4s)：埋的「芥末送蛤蜊隔门传话」与「假名单进入官方渠道」两条均因无兑现证据被抓——路径 A 触发成功。
  - 区间3修订重跑：两次均死于 Provider `402 Insufficient Balance`（job `job_bec7a35f`、`job_8583520d`）——DeepSeek 账户余额不足，实验在此中断。
- 清理：finally 删除 5 帧 / 1 决策；`.contrast/keyframe-scale/` 已删除；报告未生成。

### 第 4 次运行的发现

1. **校准生效**：逐条比对让「声明未兑现」类冲突全部落网（区间2 的缝可用性、区间2重跑的原件vs抄件、区间3 的两条）——这些在未校准时都被合并总评放行。
2. **新语义问题（待复核）**：「声明未兑现即上报」规则会误伤段外状态条目——区间1 的「芥末此刻在堤下十米处,郭莹完全不知道」本是刻意不在正文显影的段外事实；帧声明的语义（状态锚点）与校验规则（逐条兑现）需要设计层裁决：给段外条目显式标注豁免，或放宽「未兑现」判定。
3. **埋的计数级矛盾条仍未被抓**：区间2 overall 称「4条中3条兑现且与截面一致」——矛盾条（郭莹此前伸手放信封 vs 截面「两次都没有伸手」）这轮依然被调和放行；区间2 因其它条目 violated 才使路径 B 闭环。计数/次数级冲突识别是 deepseek-flash 校验员的持续弱点。
4. **外部阻塞**：DeepSeek `402 Insufficient Balance`——区间3 修订重跑与最终报告无法完成，需充值或换 Provider/模型后重跑。
5. smoke 小缺陷（未修）：区间1 与区间2重跑无 verdict 断言，violated 也无条件 settle confirmed——smoke 收口时是否补断言由开发者决定。

## 运行记录（按时间序）

1. **第 1 次**：脚本先清理了上一轮会话遗留的旧素材（4 帧 / 1 决策），随后创建决策失败——服务端报 `Decision name 已存在：d-scale-tween-overturn`，而 GET 列表已无同名决策。创建事务回滚，未留残留；推断为历史软删/归档数据仍参与唯一性校验，未进一步追查。
2. **第 2 次**：素材创建成功（决策 id=5、帧 id=17-20）；区间 1 的 writer workflow 两次均在 `report_result` 未返回 `data.summary` 处被 workflow 严格校验拒绝（job `job_cb7843eb`、`job_c1674d1a`），脚本按设计重试后失败退出，finally 清理 4 帧 / 1 决策。
   - 根因（session 47/48 JSONL + writer profile 合同比对）：writer profile 的交付协议是「`report_result.data` 默认不填，除非调用方明确需要结构化结果」，而 `keyframe-tween-review` 在 `chapterPath` 模式的消息里从未索要 `data.summary`，校验却强制要求——合同矛盾，模型服从 profile 是正确行为。
   - 修复：`c73c5f45 fix(workflow): 补间交付合同显式索要 data.summary`——workflow 消息在两种模式下都显式索要结构化字段；校验不放宽；新增回归断言（3/3 通过）。
3. **第 3 次（修复后）**：素材创建成功（决策 id=6、帧 id=21-24）。
   - 区间 1（K1→K2）：workflow 完整跑通——writer 写出 1806 字正文并返回结构化摘要，回撞 verdict=**clean**，双帧 PATCH 为 confirmed。
   - 区间 2（K2→K3 埋矛盾）：workflow 完整跑通，回撞 verdict=**clean**；但 smoke 断言失败「预期 violated 实得 unknown」后退出，finally 清理 4 帧 / 1 决策。
   - 「unknown」的根因（job `job_989ada9e`、`job_20fc2dd4` 经 HTTP 复核）：jobs API 的 `result` 是 `{runId, workflowKey, status, result: <workflow 返回值>, ...}` 包装层，smoke 读的是 `job.result.verdict`（缺一层）——两个 job 的真实 verdict 分别为 clean / clean。
   - 修复：smoke `runTween` 按 `result.result` 解包（本提交），类型检查通过。

## 实验级发现（未修复，待复核）

- **回撞校验员漏报硬矛盾**：区间 2 的 writer 如实把埋的矛盾条写进正文（「她先伸过一次手。几天前……把一只信封塞进了缝里」，session 51 JSONL 可复核），与世界截面「这条缝此前被用过两回,郭莹两次都从头看到尾,没有伸手」直接冲突；但回撞员 overall 明确声称「完整兑现四项不可逆变化，且与世界状态截面一致」。矛盾条由 writer 主动实现为「第三次使用」的叙事，冲突是计数级的，deepseek-flash 校验员未识别。
- smoke 自身设计把这种情况定义为 `埋冲突失效,实验设计需要复核`——补间与回撞链路本身已按合同工作，继续跑完整实验前需要先裁决：收紧矛盾条措辞（改为非计数级硬冲突）、或校准回撞校验员提示词/模型。

## 残留与未运行项

- 项目数据：三次运行的帧/决策均已在 finally 删除，查询确认无 `k-scale-*` / `d-scale-tween-overturn` 残留；`.contrast/keyframe-scale/` 补间正文目录已随 finally 删除。
- State Root 中的应用数据（job JSON、session JSONL、trace、`.contrast/keyframe-scale-operations.log`）按产品数据语义保留，未清理；本档案引用的 job/session 编号以其为准。
- 实验报告 `keyframe-scale-report.md` 未生成（区间 2 断言失败，未走到报告步骤）；整章连续性与两条裁决路径均未获得完整实验结论。
- dev server 在第 3 次运行结束后自行以 exit 1 退出，stdout 无错误栈（`[nitro] built` 与 workspace-history 维护日志之后直接 `error: script "dev:runtime" exited with code 1`）；原因未验证，未影响实验数据的完整性。
- 未运行：区间 2 修正帧重跑、区间 3 两条裁决路径、`--keep` / `--cleanup-only` 路径。
