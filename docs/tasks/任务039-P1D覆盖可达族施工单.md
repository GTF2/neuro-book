# 任务039-P1D覆盖可达族施工单（P1-09/10/11/12·四包收尾）

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：GLM-5.3-Flash + 推理档位 high（工程队·常驻窗口；一次一包，包 D 施工期间不领其他单）。
- **模式**：目标模式 + 完全访问（**产物全在 out/ 非 git**，无分支概念，以断言脚本+指纹守边界）。
- **给用户的现成命令**（让用户直接粘贴）：
  - `/goal 任务039-P1D覆盖可达族施工单：按施工单产出 P1D 覆盖可达族样张页+断言脚本+规格节文本，逐条断言交付`
- **对口技能与命令**（照计划第五节）：
  - UI 形态参考 → `impeccable`（reference/quieter.md；detect 在仓库外跑）+ `ui-ux-pro-max`（§1 focus-appearance/键盘可达、缩放与对比度判据）
  - 页面验证 → 无头断言两式（Edge dump-dom 用 `node` 直跑；playwright-core 用 `cd D:/MyProject/neuro-book/packages/neuro-book && bun <脚本>`——差异见审合记录 §5）
  - 完工自查 → 断言输出贴明+交付报告（CLI 审查豁免沿用参谋部原型层裁定）

## 目标

产出 P1 覆盖可达族规格样张页（包 D 收尾，矩阵 R13-R18）：浮层契约、快捷键与可访问性、响应式五档取证、i18n/长文案/RTL。用户可感=**每个浮层都知道谁开的怎么关的关后回哪；纯键盘能走完全流程且快捷键有地方查；放大到 200% 不碎、切 RTL 不乱、长文案不吞关键动作**。

## 范围与产物

- **产出三件**：
  1. `out/图纸/单D-P1D-覆盖可达样张.html`（新文件；Token/形态照基板抄值，禁自创）
  2. `out/工具/_verify_p1d-coverage.mjs`（断言脚本，逐条留输出）
  3. 规格节文本（随交付报告附：Overlay/快捷键与 a11y/响应式/i18n-RTL 四节条文，供收口轮并入 v3.md）
- **明确不做**：不改冻结基板/nb-ui/仓库文件；P2 登记项禁顺手实现；axe 以**等价规则审计**实现（断言脚本内置规则清单逐条 DOM 检查，不引外部 CDN 依赖）；真机接线（033 遗留下层卡/ReviewSidebar 等）归后续轮。

## 契约基线（逐条编号，照计划第一节包 D 行展开）

1. **浮层契约**（P1-09）：每浮层声明五件套=唯一 owner（aria-expanded 触发钮）/z-index 层级位/关闭链（点外+Esc+显式关闭钮）/焦点链（开=焦点陷阱入内、关=焦点回触发钮）/aria 三件套（role+aria-modal 视型+labelledby）——样张覆盖四形态：目录抽屉（边缘进入保留任务上下文）/Tooltip（延迟显隐 hover+focus 双路）/Popover（自动翻转不溢出视口）/Workflow 式浮层；窄屏不推挤（浮层覆盖不挤压内容流）。
2. **033 遗留「下层卡 inert」示范落点**：样张内示范 helper inert 锁定范式（aria-hidden 容器内可聚焦件同刻 inert——照任务034 基板接线 helper 既有实现抄值）；**真实下层卡修复归收口轮照范式合并**（本单只示范，不动基板/产品代码——参谋部裁定落点）。
3. **快捷键与帮助层**（P1-10）：快捷键总清单+`?` 帮助层（清单即帮助层内容）；发送/目录/收起 dock/撤销重做键位演示；输入框内冲突规则（键入字符优先于全局快捷键——Combobox 双态口径延续）。
4. **a11y 底座**：skip link（跳 main）、landmark 五件（header/nav/main/aside/footer）、live region（polite/assertive 各一演示）、错误与字段 aria-describedby 关联（照包 A 口径）、listbox 键盘（照 1.3/包 A）、下层卡 inert（条目 2）。
5. **等价规则审计**（P1-10 验收 axe 等价）：断言脚本内置规则清单逐条 DOM 检查——色非唯一状态信号（状态必有文字/图标伴生）/对比度（--ink 对纸面 ≥7:1 抽查 --muted 对纸面 ≥4.5:1，实测 computed 值）/img 与图标 alt/aria 标注/标题层级不跳级/焦点可见（focus-visible 3px 照基板）。
6. **200% 缩放**：浏览器缩放 200% 断言（无横向滚动+关键动作可点——无头用 CSS zoom 模拟+真实缩放移交用户复验，披露）。
7. **响应式五档**（P1-11，档位口径照任务035 澄清③）：桌面三档 1920/1440/1366（完整模式+分栏拖）+窄屏两态 md 768（AI/稿面互斥标签+禁拖+抽屉入口）/sm 390（单栏+抽屉+稳定焦点顺序）——**五张截图各留 DOM 断言**（sm 无横向滚动断言+md 禁拖复用 P1C 窄轮先例+长标题/表格/错误态不溢出）。
8. **i18n/长文案/RTL**（P1-12）：locale 切换演示（zh/en 双语+缺键回退键名可见）；德文长词样例段（长词不断行溢出断言）；日期数字复数格式演示；**RTL 验证**（dir=rtl 翻转后布局不乱/图标不反向错义/关闭钮仍在预期侧——断言+截图）；长文案断言（关键动作不剩省略号+tooltip 兜底全文）。

## 步骤建议

1. 读根 AGENTS.md「本 Fork 工作规矩」节→读本施工单→读参考材料（基板 scaffold/1.3 listbox 键盘/helper inert/包 B 空错三要素/P1C 窄轮先例）。
2. 样张页四段骨架（Overlay/快捷键 a11y/响应式/i18n-RTL）+Token 抄值。
3. 浮层四形态+五件套契约填肉；下层卡 inert 示范段。
4. 快捷键清单帮助层+a11y 底座件填肉。
5. 响应式五档取证（playwright 三轮编排扩五轮或分脚本）。
6. i18n/RTL 段填肉（dir=rtl 切换器）。
7. 断言脚本（双轮编排照先例+等价规则审计清单）+双主题截图+规格节文本+交付报告（四件自查）。

## 验收标准（逐条留命令输出，禁报完成数量只报逐条明细）

1. 样张在盘（ls 佐证）+Token 抄值佐证（`grep -c "var(--"` 计数）。
2. 断言脚本全绿逐条输出——至少覆盖：浮层四形态五件套各一、Tooltip 延迟、Popover 翻转、焦点陷阱与回焦、下层卡 inert 示范、skip link/landmark/live region、快捷键帮助层开合、错误关联、等价审计规则清单全绿、200% 缩放、五档响应式各一、RTL 翻转、长文案不省略、缺键回退。
3. `grep -c "<select" 单D-P1D-覆盖可达样张.html` =0；`grep -n "style.display"` 零残留（helper 范式除外，豁免逐处登记）。
4. 亮暗双主题截图各一张+响应式五档截图（共 7 张：`out/审计/单D-P1D-样张-亮主题.png`/`-暗主题.png`/`-响应式-1920.png`/`-1440.png`/`-1366.png`/`-md768.png`/`-sm390.png`）。
5. 规格节文本随报告附（Overlay/快捷键 a11y/响应式/i18n-RTL 四节条文+等价审计规则清单全文）。
6. 交付报告四件自查：产出在盘 ls 佐证/断言输出贴明/交付报告写待审/零越界改动（基板/样张①②+P1A/B/C/仓库文件指纹对照）。

## fixture 与真实接线边界

- 全在 out/ 原型层；真机接线（033 下层卡/ReviewSidebar/写作链路）归后续轮——本单只示范范式。
- 200% 缩放无头模拟（CSS zoom）+真实缩放移交用户复验；读屏行为以 aria 属性断言替代真读屏（真读屏移交用户）；axe 等价审计=内置规则清单非外部依赖（披露）。

## 参考材料

- `docs/tasks/任务035-P1四包分包派单计划.md`（第一节包 D 行含 inert 落点澄清/第四节验收/第五节技能/第六节挂账）
- `out/审计/单D-基板通用件补全与Ant对账施工计划-v1.md` §P1-09/10/11/12+§4.2 本地法偏离清单
- `out/图纸/单D-设计基板.html`（scaffold/1.3 listbox/helper inert 实现/Token/focus-visible）
- `out/图纸/单D-P1C-结构交互族样张.html`（窄轮真实视口先例/roving tabindex/拖拽禁拖口径）
- `out/审计/单D-P1C-审合记录-v1.md`（三轮编排先例+复跑命令两式）
- 任务034 基板接线 helper（下层卡 inert 范式抄值源，`图纸/单D-设计基板.html` p0 IIFE）

## 疑问区（施工窗口填写）

（施工窗口填。规格起草 9 条歧义全文见 `out/审计/单D-P1D-规格节文本-v1.md` ③节+§0.5 新立数值登记表 10 行；主窗口终稿无新增冲突，择要 7 条：）
1. **z-index 归并表与 A-08 衔接**：样张立五档（popover 55＜drawer 60＜tooltip 70＜遮罩 79＜modal 80）+教具层 90——与审计矩阵 A-08「确认层(40)＜下拉层(55)倒挂」的裁定衔接：确认层 40 系行内无遮罩范式有意安排；--z-* token 化归收口轮，归并表已立为 A-08 决策输入。
2. **焦点陷阱两型分**：模态型强锁（Workflow 式浮层/帮助层）vs 非模态轻量链（抽屉/Popover/Tooltip/行内确认层）——P1B 移交的「Tab 循环锁定全量」按此切分，行内确认层归轻量链；已登记终裁，改强锁只动条文一行。
3. **Tooltip 延迟 @default=hover 300ms/focus 立即显**（不罚键盘用户）——无基板先例，登记终裁。
4. **快捷键键位表全表提案值**（Alt+T/Alt+D/Alt+W/Ctrl+Z/Ctrl+Shift+Z/?）——收口轮定稿；帮助层=清单即内容（单一真相防漂移）；撤销/重做只承诺键位+回执，禁越 P2 全局撤销栈。
5. **RTL 下左 2px 选中线语义方向**：宪法 3 物理 border-left 在 RTL 与文字起点背离——登记不施工，border-inline-start 泛化归收口轮（三处清单：下拉选中线/标签选中线/审稿当前项）。
6. **Workflow modal 三路关闭落位**：Esc+显式关闭钮+主行动钮，**点外不关** @default（模态防误触）——白名单原文未指明，登记终裁。
7. **live region 唯一性**：polite 容器恰一（包 B 反馈两 zone+独立播报位合并口径）+assertive 恰一——新立约束待认。
（另两条纯登记：1280 档被任务035 澄清③收敛出五张口径、升档补拍归收口轮；md 768 抽屉入口形态由样张实现定。）

## 交付报告（施工窗口填写）

**状态：待审**（工程队不维护 BOARD，请前线走查后登记）。会话 ID：sess_f7630ad3-9078-4e74-b751-945479569d6e。

### 做了什么（三件产物+七张截图，全在 out/）

1. **样张页** `out/图纸/单D-P1D-覆盖可达样张.html`（67,459 B）——R13 浮层契约四形态（目录抽屉非模态/Tooltip 延迟 hover+focus 双路/Popover 自动翻转/Workflow 式模态）**五件套契约总表逐格声明**（OVS 注册表单一接线：owner aria-expanded/z 位 pop55＜drawer60＜tip70＜遮罩79＜modal80/关闭链三路/焦点链开关回焦/aria 三件套视型）+同刻至多一个模态；**下层卡 inert 示范**（照基板 P0-04 lock 范式抄值，under1/under2 四钮同刻 inert+主卡可聚焦，页面注明真实修复归收口轮）；R15 快捷键帮助层（`?` 开层=清单即内容 8 行+Alt+D/Alt+W/Ctrl+Enter/Ctrl+Z/Y 真实键位回执+输入框内字符优先冲突规则）；R16 a11y 底座（skip link 首位跳 main/landmark 五件/live region polite+assertive 双通道/错误 aria-describedby/listbox 键盘）；**等价审计 EQ1-EQ7**（色非唯一信号/对比度实测亮 10.36 与 7.58 暗 14.73 与 8.24/alt/标题不跳级/焦点环/label 关联/按钮可访问名——内置清单无外部 CDN）；200% zoom 模拟（无横向滚动+关键动作 elementFromPoint 命中+**挖出并修掉 100vw zoom 溢出真缺陷改 calc**）；R17 响应式五档；R18 i18n（zh/en 键表+缺键回退键名+Intl 日期复数）+德文长词+RTL（dir 翻转布局不乱+关闭钮物理侧断言+装饰图标 aria-hidden）+长文案不省略+tooltip 兜底。
2. **断言脚本** `out/工具/_verify_p1d-coverage.mjs`（51,409 B）——**七轮编排**：主轮 1280 stub 94+RM 真实 reduce 9+宽档三档 1920/1440/1366 各 6+md 768 轮 8（互斥标签双向+指针键盘双路禁拖+抽屉入口）+sm 390 轮 8（无横向滚动/单栏纵排/焦点顺序 compareDocumentPosition 校验）+静态 4+7 截图。`node` 直跑。
3. **规格节文本** `out/审计/单D-P1D-规格节文本-v1.md`（33,562 B）——§A Overlay 29 条（z 归并表/焦点链两型表/aria 视型表）+§B 快捷键 a11y 23 条（键位表+审计六条全文）+§C 响应式 8 条+§D i18n-RTL 16 条+§E 对照表 38 行+§0.5 新立数值登记表 10 行。
4. 七张截图按验收标准命名落 `out/审计/`：单D-P1D-样张-亮/暗主题.png+响应式-1920/1440/1366/md768/sm390.png。

### 为什么

照任务035 包 D 行与任务039 契约 8 条施工；P1 四包收尾单，D 完成后进收口轮。

### 自测结果（施工员跑+主窗口独立复验，双复现）

- 断言 **139+4 静态+7 截图全绿**，主窗口复跑 `P1D-COVERAGE ALL PASSED (139 断言 + 7 截图 + 4 静态检查)` 退出码 0。关键项：浮层四形态五件套逐格核对（含 Esc 反序关栈/焦点回 owner/Popover 翻转不溢出/Tab 陷阱双向循环）；下层卡 inert 同刻锁定；等价审计 EQ1-EQ7 全绿（对比度 WCAG 公式实测全过）；200% zoom 无溢出+关键动作可点；五档各留 DOM 断言（md 禁拖双向+sm 焦点顺序稳定）；RTL 翻转+长文案不省略+缺键回退键名可见。
- grep 三连：`<select`=0、`style.display`=0、`var(--)`=95 行。
- 零越界：基板（145,927）+样张①②（13,764/21,764）+P1A/B/C（52,012/54,689/57,100）六文件指纹对照原样；仓库文件仅本任务书两节（授权范围）。
- 施工员五跑收敛（11→7→3→1→0）全程披露，两类真缺陷在样张内修掉：**100vw 在 CSS zoom 下不缩致 200% 溢出 672px**（改 calc(100%-32px)）+**浮层过期定位在缩放后坐标系溢出**（缩放时清除重算）；headless 视口坑扩展实锤（视口=窗口-32 且下限钳 510——md 轮窗口给 800 使视口恰 768，照 P1C 先例加防混档断言）。
- 截图亲核：浮层契约总表/导航锚/键位标注清晰，Token 亮皮生效。

### 遗留问题

- 疑问区 7 条待前线裁定（z 归并表衔接 A-08/焦点陷阱两型分/Tooltip 300ms/键位表提案值/RTL 选中线 border-inline-start 泛化/modal 点外不关/live region 唯一性）+§0.5 登记表 10 项终裁。
- 移交用户复验项（headless 无法驱动，页面页脚+报告披露）：真实浏览器缩放 200%、真实手机 390px 视口、真读屏（NVDA/JAWS）实测。
- 未做项（范围外）：typeahead 未入 P1D listbox（属 P1A Combobox 口径）；真机接线（033 下层卡/ReviewSidebar）归收口轮照范式合并。
- P1B 规格正文 Undo 仍写 6s（起草旧值）——本稿已按审合定稿写 5s，收口轮同步 P1B 正文时一并改。

### 完工四件自查

①三件产物+7 截图在盘（ls 佐证）✓ ②断言输出逐条贴明（施工员报告全文）✓ ③交付报告=本节，状态**待审** ✓ ④零越界改动（六文件指纹对照）✓
