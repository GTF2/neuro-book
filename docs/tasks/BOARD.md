# 任务板（BOARD）

> 唯一任务真相源。所有窗口读写此处；状态流转：`待办 → 进行中 → 待审 → 完成/打回`。
> 任务书命名：`docs/tasks/任务NNN-短名.md`。禁止代号式命名（T0/T1 禁用）。

## 当前状态

> 模型建议依据（2026-09-19 官方文档+测评）：GLM-5.3 **默认档是 max**（不设置=烧最贵，主动降档省钱）；思考 token 全额计费。GLM-5.3-Flash 价格约为旗舰 1/9，代码能力有差距（复杂任务首次通过率低），但工具使用基准领先同档，且**支持图片输入**（5.3 纯文本）。选型口诀：仓库级长链路/失败代价高 → 5.3；量大/成本敏感/要看图 → Flash。
>
> 模式建议依据（ZCode 官方文档）：**计划模式**=先探索出计划→用户审批→动手（复杂/意图不明/防乱改）；**目标模式**=`/goal <目标>` 设定后自主多轮跑到完成（明确的多轮任务，支持 replace/pause/resume）；**工作流模式**=`/workflow` 多子代理并行编排（可扇出可门控）；**完全访问**=少确认不打断（低风险+边界明确的活）。工程队在 worktree 隔离+任务书边界内默认可用完全访问减少打断。

| 编号 | 任务 | 负责窗口 | 模型建议 | 模式建议 | 状态 | 任务书 |
|---|---|---|---|---|---|---|
| 001 | ~~旧分支抢救推送~~ | — | — | — | **取消**（用户决定从头来，旧工作不合入） | — |
| 002 | 资产摘录归档 | 参谋部 | — | — | **完成** | 见 `docs/reference/`（9 份：6 UI重设计稿+handoff+NEXT-PLAN+MOVE-NOTES） |
| 003 | 三件套基建（AGENTS 定制节/任务板/交接文档） | 参谋部 | — | — | **完成** | 本文件 + `HANDOFF.md` + 根 `AGENTS.md` 定制节 |
| 004 | 环境验证（复跑 dev server + 浏览器确认） | 前线指挥部 | GLM-5.3 + high | **目标模式**（"跑通环境并截图"单一明确目标，让它在坑位清单辅助下自主排坑连跑到完成）+ 完全访问 | **完成**（2026-09-20：dev server 跑通+截图存证+测试 5文件/79 全绿，见任务书执行记录） | ✅ 已建：`任务004-环境验证.md` |
| 005 | 测试基线（vitest 全量复跑） | 工程队·常驻窗口 | Flash + low（纯执行跑测试收结果） | 目标模式 + 完全访问 | **待审**（2026-09-20：10包全绿，主包17失败/llmlint 24文件收集失败/harness全量挂起分片521全绿，见任务书交付报告） | ✅ 已建：`任务005-测试基线.md`（文档类，无 worktree） |
| 006 | 模块地图（页面/路由/组件复杂度清单） | 工程队·常驻窗口 | Flash + high（静态分析+归纳成文档） | 目标模式 + 完全访问（产出单一文档） | **待审**（2026-09-20：`docs/knowledge/模块地图.md` 产出，18路由/372组件/72个重构候选，novel-ide 占前端 73%） | ✅ 已建：`任务006-模块地图.md`（文档类，无 worktree） |
| 007 | 竞品 IA 调研（Scrivener/Obsidian/Ulysses 等，带截图） | 工程队·常驻窗口 | Flash + high（**多模态可看截图**，5.3 反而纯文本） | 目标模式 + 完全访问（单窗口串行调研） | **待审**（2026-09-20：报告+7图入库；官网实时截图因本机代理不可用改为官方素材图，口径见任务书疑问区） | ✅ 已建：`任务007-竞品IA调研.md`（文档类，无 worktree） |
| 008 | 上游新 UI 评测（三层评判，等上线） | 前线指挥部 | GLM-5.3 + max（深度综合判断；截图初审可派 Flash 子代理） | **计划模式**（先出评测方案给用户确认，再执行评判） | 待办·阻塞 | 由前线按模板创建（触发条件：上游发布新版 UI） |
| 009 | 定自己的重构方向（008 之后，用户拍板） | 用户+前线 | GLM-5.3 + max（或回参谋部讨论） | **计划模式**（方案必须用户审批后生效） | 待办·阻塞 | 讨论纪要形式，无需模板 |
| 010 | 开发者知识摘要（12 包结构/构建测试命令/核心依赖蒸馏） | 工程队·常驻窗口 | Flash + high | 目标模式 + 完全访问 | **待审**（2026-09-20：`docs/knowledge/开发者知识摘要.md` 产出，12包+命令速查+依赖表+文档地图+坑位7条） | ✅ 已建：`任务010-开发者知识摘要.md`（文档类，无 worktree） |
| 011 | TipTap 3 用法笔记（本项目用法与扩展点） | 工程队·常驻窗口 | Flash + high | 目标模式 + 完全访问 | 待办 | ✅ 已建：`任务011-tiptap笔记.md`（文档类，无 worktree） |
| 012 | llmlint 中文素材整理 | 工程队·常驻窗口 | Flash + high | 目标模式 + 完全访问 | 待办 | ✅ 已建：`任务012-llmlint中文素材.md`（文档类，无 worktree） |

## 状态规则

- 状态只能由负责窗口或前线指挥部改写；工程队改状态必须同时附交付报告。
- **节拍规则**：领任务、交付、审查三个时刻，必须重新从磁盘读取本文件与任务书当前内容，禁止依赖本会话早先读过的版本（其他窗口可能已修改）。用户传来的任务指针（"做任务NNN"/"任务NNN 待审"）即重读触发信号。
- `打回` 必须在任务书写明返工意见；`完成` 必须有确定性检查证据（命令输出/截图）。
- 新任务由前线指挥部创建并登记；工程队不得自行加任务。

## 历史决议（只读）

- 2026-09-19：fork 重建（GTF2/neuro-book），旧仓库 79 提交工作不合入（用户判断旧方向有误）；旧档案保留在 `D:\MyProject\neuro-book失败fork开发但可以学习经验\`，仅摘录文档资产。
- 2026-09-19：UI 重构推迟至上游新 UI 上线评测后（任务008/009）。
- 2026-09-19：本 fork 纯自玩定位，不联系上游作者，master 定期 rebase upstream。
- 2026-09-20：批量任务书 005-012 建成并推送（550ec838）。闲时任务"安全审计+上游动向调研"因**账号配额冷却**未能创建；当晚用户拍板**取消重建**（前线备的指令存档已删除作废，配额恢复后亦不重试），两个活改派**临时工程队窗口**今晚直接执行；前线指挥部待用户通知后验收产出并补登记任务行。
