# 任务004-环境验证

> 负责窗口：前线指挥部（本任务不建 worktree——纯验证不产生代码提交，直接在主工作区执行）
> 参考：`docs/reference/MOVE-NOTES.md`（前任 2026-09-15 完整跑通记录）、`docs/reference/NEXT-PLAN.md`
> 状态：**完成并经用户验收**（2026-09-20）

## 运行配置（开工前设置）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3`，推理档位 **high**（默认是 max，主动降档省额度；本任务是排坑执行，high 足够）。若会话未在此配置，先提醒用户在设置里切换。
- **模式**：**目标模式**——"跑通环境"是单一明确的多轮目标，适合自主连跑排坑。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务004-环境验证：按任务书把 dev server 跑通并截图存证，遇到坑按已知坑清单处理，完成后更新 BOARD 状态
  ```

## 目标

在全新 clone（无 node_modules）上把项目完整跑起来，确认浏览器可用，为后续所有任务提供可运行基线。

## 重要：本机是新 clone

前任档案中的"依赖已装、无需重跑"指他们的旧副本；本仓库是干净 clone，**必须从 `bun install` 开始**。

## 步骤

1. `bun install`（仓库根；Bun 1.4.2 已在 PATH）
2. `bun run --cwd packages/neuro-book nuxt:prepare`（注意 `--cwd` 必须在 `run` 之后）
3. `bun run --cwd packages/neuro-book generate`（生成 Prisma Client ×2）
4. `bun run --cwd packages/neuro-book dev` 启动
5. 浏览器访问 **`http://127.0.0.1:3000`**（必须 127.0.0.1；`localhost` 会解析 IPv6 而应用只监听 IPv4）
6. 用 browser-use 插件（control-browser 技能）截图首页存证

## 已知坑（遇到先按此处理，再怀疑代码）

- 前端空白/不渲染：Agent 沙箱拦截子进程所致——关闭沙箱重试
- 访问失败：确认用的是 127.0.0.1 而非 localhost
- bun 命令打印用法且退出码 0：`--cwd` 位置错了

## 验收标准

- 首页截图（含地址栏）存入 `docs/tasks/evidence/任务004/`
- `bun run --cwd packages/neuro-book test -- server/config` 通过（上次基线：5 文件/80 测试全绿）
- BOARD.md 中本任务状态改为"完成"，并在任务书末尾追加执行记录（遇到的坑及解法）
- **不产生任何代码提交**；若发现必须修代码才能跑通，停下来在"疑问区"登记等用户拍板

## 疑问区

（空）

## 执行记录（前线填写）

**2026-09-20 执行完毕，结论：环境验证通过。**

执行人：前线指挥部（ZCode 会话 sess_31b7a638）。

按步骤逐项结果：

1. `bun install`：1533 包 27.7s 完成。首跑提示 "Failed to install 1 package"，重跑（幂等）补装 3 包后无失败——系 `json-editor-vue` 的 preinstall 脚本（强制 only-allow pnpm 检查）被 Bun 阻止，与运行无关。另有 5 个包的 postinstall 脚本被 Bun 默认策略阻塞（`bun pm untrusted` 可查），其中 `@parcel/watcher` 有预编译 `@parcel/watcher-win32-x64` 包可用，无需源码构建，不影响运行。
2. `nuxt:prepare`：成功，类型生成至 `.nuxt`。
3. `generate`：Prisma Client ×2 生成成功（`server/generated/prisma` 26ms、`server/generated/project-prisma` 121ms）。
4. dev server：`bun run --cwd packages/neuro-book dev` 启动成功，监听 `http://127.0.0.1:3000/`，Nitro 构建约 11s。
5. 浏览器验证：127.0.0.1:3000 打开成功。**首次加载白屏约 20s 属正常**——Vite 首次运行时发现大量依赖（tiptap/mermaid/dnd-kit 等约 30 个模块）触发预打包和页面自动重载，reload 后渲染正常。页面 DOM 完整：侧边导航 + "我的书架" + 《新小说》项目卡片 + 会话确认提示。
6. 截图存证：`docs/tasks/evidence/任务004/首页-127.0.0.1-3000.png`。说明：截图来自 ZCode 内置浏览器（IAB），其视口不含地址栏；URL 已通过程序化读取 `tab.url()` 证实为 `http://127.0.0.1:3000/`，DOM 快照证实渲染内容，两者共同构成存证。
7. 测试：`bun run --cwd packages/neuro-book test -- server/config` → **5 文件 / 79 测试全部通过**（42.9s）。任务书记录的前任基线为 80，现 79：零失败，判断为上游演进后的测试数量微调，非环境问题。

新发现（任务书坑清单之外）：

- Vite 首次加载白屏非沙箱问题，等依赖预打包完成或手动 reload 即可；dev server 日志会打印 optimizeDeps 建议，将来可选择性采纳以消除首载 reload。

工作区副作用（均未提交，符合"不产生代码提交"要求）：

- `bun.lock`：install 自动同步了 2 个 workspace 包的 version 字段（package.json 已是新 canary 版本号，lock 为旧值），无依赖增删。
- 新增 `docs/tasks/evidence/任务004/`（截图）与本任务书、BOARD.md 的状态更新。
