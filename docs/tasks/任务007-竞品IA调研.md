# 任务007-竞品IA调研

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3-Flash`，推理档位 **high**（Flash 支持图片输入，看截图比 5.3 合适）。若当前会话未在此配置，开工第一句先提醒用户切换。
- **模式**：**目标模式 + 完全访问**——调研+截图+归纳成单一文档，串行执行即可，无需工作流扇出。
- **工作区**：`D:\MyProject\neuro-book` 主工作区（master 分支）直接执行，**无 worktree**（文档类任务豁免）。工程队不执行任何 git commit，文件写好后由前线统一提交。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务007-竞品IA调研：按任务书调研 Scrivener/Obsidian/Ulysses 等写作工具的信息架构并带官网截图，产出 docs/knowledge/竞品IA调研.md，完成后在任务书末尾写交付报告
  ```

## 目标

调研主流长篇写作工具的信息架构（IA），带官网截图存证，产出对比调研文档，为 NeuroBook 的 IA 重构方向（任务008/009）提供参照系。

## 开工前置

1. 读 `D:\MyProject\neuro-book\AGENTS.md` 的"本 Fork 工作规矩"节（尤其工程队分区：只写 `docs/knowledge/`、`docs/tasks/evidence/` 和本任务书交付报告区）
2. 读本任务书全文，确认模式/模型已就位
3. 确认在 `D:\MyProject\neuro-book` 主工作区（master 分支）：`git rev-parse --show-toplevel` 应输出该路径

## 范围与边界

- 允许改动：仅新建 `docs/knowledge/竞品IA调研.md` 与 `docs/tasks/evidence/任务007/` 下截图；本任务书疑问区与交付报告
- 禁止改动：任何源码、配置、master 分支提交、BOARD、其他任务书
- 只调研公开官网/文档/评测，不注册账号、不下载安装试用

## 执行步骤

1. 竞品清单（至少 3 个必做）：**Scrivener、Obsidian、Ulysses**；可选补充至多 2 个（如 Novelcrafter、iA Writer）
2. 每个竞品逐项调研，IA 维度固定四项：
   - 导航层级（一级导航是什么、几层深）
   - 项目/文件组织模型（binder？文件树？标签/链接？）
   - 编辑器与元数据的关系（大纲卡、角色档案怎么挂）
   - 长文写作特有功能（快照/版本、目标字数、编译导出）
3. 每竞品截官网关键页 2-3 张（首页 + 功能/文档页），存 `docs/tasks/evidence/任务007/`，视口 1920×1080，同类页面统一视口保证可比；文件名 `竞品名-页面.png`
4. 写 `docs/knowledge/竞品IA调研.md`：① 每竞品一节（四维度+截图相对路径引用）② 汇总对比表（竞品 × IA 要素）③ 对 NeuroBook 的启示 3-5 条（纯建议，不实施任何改动）

## 产出文件

- `docs/knowledge/竞品IA调研.md`（唯一报告交付物）
- `docs/tasks/evidence/任务007/*.png`（截图存证）

## 验收标准

- 3 个必做竞品全部覆盖四维度，每竞品至少 2 张 1920×1080 截图且报告内相对路径可点开
- 对比表和启示齐备；全程无源码改动

## 完成定义（AGENTS.md 硬标准，文档任务变体）

任务完成 = ① 产出文件已写入仓库工作区 ② 任务书交付报告已填（做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）③ 任务书内声明"待审"。BOARD 状态更新与 git 提交由前线在审查通过后执行。**仅在聊天中报告结果不算完成。**

## 参考材料

- `docs/reference/ui-redesign-handoff.md` 与 `docs/reference/*-redesign.html`（前任对 NeuroBook IA 问题的理解，用作对照出发点）
- BOARD.md 表头"模型建议依据"（Flash 多模态选型理由）

## 疑问区（工程队填写）

1. **截图口径偏差（重要）**：本机代理节点当前不可用（实测 Scrivener/Obsidian/Ulysses 官网经 IAB、桌面 Edge、curl 直连、curl 走 127.0.0.1:7890 代理四条路径均 ERR_CONNECTION_CLOSED/SSL 失败；百度 200 正常、GitHub 被墙同症），**无法实时打开官网按 1920×1080 视口截图**。已改用图片搜索获取官方素材图/评测实拍图（7 张，来源与原尺寸标注于报告图注）。请前线决定：接受该口径，或待代理恢复后由工程队补拍实时截图。
2. 附带发现：ZCode 内置浏览器（IAB）的截图功能在本机持续超时不可用（含空白页），属宿主环境问题，已超出本任务边界。

## 交付报告（工程队填写）

**做了什么**：完成 Scrivener/Obsidian/Ulysses 三个必做竞品 + Novelcrafter 补充的 IA 调研，产出 `docs/knowledge/竞品IA调研.md`（每竞品四维度 + 截图引用 + 竞品×IA要素对比表 + 对 NeuroBook 启示 5 条）与 `docs/tasks/evidence/任务007/` 下 7 张截图。

**为什么**：为任务 008/009 的 IA 重构提供参照系。

**自测结果**：四维度全部覆盖；截图 7 张均已下载入库并逐张目检内容相符（Scrivener 2、Obsidian 2、Ulysses 2、Novelcrafter 1，其中 Novelcrafter 为 1920×1080 官网整页）；功能事实经 WebSearch 对照官方文档/评测核实，来源列于报告文末；全程无源码改动。

**异常与绕过说明**：官网实时截图因本机网络受限改为图片搜索官方素材图（详见疑问区 1）；期间先后尝试 IAB 截图（超时）、headless Edge（网络不通）、桌面 Edge via Computer Use（网络不通）、宿主 WebFetch（走本机网络同样不通），最终经 WebSearch（服务端）+ 图片搜索（服务端）+ 宿主 https 下载完成。

**遗留问题**：实时截图补拍（待代理恢复，见疑问区）；ZCode IAB 截图功能故障待前线知悉。

**状态**：**待审**。

**本窗口会话 ID**：sess_f7630ad3-9078-4e74-b751-945479569d6e
