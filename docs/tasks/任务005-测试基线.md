# 任务005-测试基线

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3-Flash`，推理档位 **low**（纯执行跑测试收结果，机器时间为主，low 即够）。若当前会话未在此配置，开工第一句先提醒用户切换。
- **模式**：**目标模式 + 完全访问**——"复跑全部测试并产出基线报告"是单一明确的多轮目标，任务书边界内无需逐项确认。
- **工作区**：`D:\MyProject\neuro-book` 主工作区（master 分支）直接执行，**无 worktree**（2026-09-20 起文档类任务豁免 worktree；早先创建的 task-005 worktree 不再使用，由前线择机清理）。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务005-测试基线：按任务书复跑全部 12 个包的测试并产出基线报告，失败只记录不修，完成后在任务书末尾写交付报告
  ```

## 目标

在主工作区全量复跑 monorepo 全部 12 个包的测试，产出一份**测试基线报告**（每包的通过/失败/跳过数与失败摘要），作为后续所有开发任务的对照基线。

背景：前任 2026-09-15 记录的"275 条全绿"只是 neuro-book 包内 5 个选定目录的子集（`server/plot server/config server/agent/workflow plot-tools leader-assets`）；全仓从未统一跑过。本任务补齐这个空白。

## 开工前置

1. 读 `D:\MyProject\neuro-book\AGENTS.md` 的"本 Fork 工作规矩"节（尤其工程队分区：只写 `docs/tasks/evidence/` 和本任务书交付报告区）
2. 读本任务书全文（含运行配置，确认模式/模型已就位）
3. 确认在 `D:\MyProject\neuro-book` 主工作区（master 分支）内：`git rev-parse --show-toplevel` 应输出该路径
4. 主工作区环境已就绪（任务004 已装依赖、生成 Prisma Client 并验证），**无需重装**；仅当 node_modules 缺失时才按任务004 执行记录重跑环境三步
5. 跑测试前先 `git status` 记录当前状态，跑完再对照——测试不应产生任何已跟踪文件的改动（任务004 已实测 server/config 套件无污染）

## 范围与边界

- 允许改动：仅新建 `docs/tasks/evidence/任务005/` 下的基线报告与日志文件；本任务书末尾的交付报告与疑问区
- 禁止改动：任何源码、任何测试文件、任何配置文件、master 分支、`.agents/` 治理文件、其他任务书
- **测试失败不许修代码**——失败本身就是基线信息，只记录
- 不确定时：停下，在任务书"疑问区"登记，待前线答复

## 步骤建议

1. 按开工前置装好环境
2. 主包全量：`bun run --cwd packages/neuro-book test`（无过滤参数，跑全部；预期 5-15 分钟）
3. 其余 11 个包逐一：`bun run --cwd packages/<包名> test`。包清单：neuro-agent-harness、neuro-book-contracts、neuro-book-manager、neuro-book-test-support、nb-memory、nb-history、nb-workflow、nb-ui、llmlint、owned-process、file-snapshot-cache
4. **不要跑** `test:real-model`（真实 Provider 调用，无授权）；其他 `test:*` 变体也不跑，只跑各包默认 `test`
5. 汇总产出 `docs/tasks/evidence/任务005/测试基线.md`，每包一行：命令 / 测试文件数 / 测试条数 / 通过 / 失败 / 跳过 / 时长；失败项附错误摘要（错误消息前 5 行），完整的失败输出另存同目录日志文件
6. 已知锚点对照：前任基线 275 条（5 目录子集）；任务004 实测 server/config 79 条全绿。若这两处数字偏差大，在报告中标注可能原因（从输出推断，不用深查）

## 验收标准

- `docs/tasks/evidence/任务005/测试基线.md` 存在且覆盖全部 12 个包，数字与日志文件可对上
- 全程无源码/测试/配置文件改动（`git status` 干净，仅 evidence 与任务书新增）
- 交付报告含：改动文件清单、异常与绕过说明、遗留问题、本窗口会话 ID

## 完成定义（AGENTS.md 硬标准，文档任务变体）

任务完成 = ① 产出文件已写入仓库工作区 ② 任务书交付报告已填（做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）③ 任务书内声明"待审"。BOARD 状态更新与 git 提交由前线在审查通过后执行。**仅在聊天中报告结果不算完成。**

## 参考材料

- `docs/reference/NEXT-PLAN.md` 第二章（前任 275 条基线的命令与覆盖范围）
- `docs/tasks/任务004-环境验证.md` 执行记录（环境三步的实测结果与已知无害告警）
- `docs/testing/README.md`（测试、临时根和验收证据合同）

## 疑问区（工程队填写）

1. neuro-agent-harness 全量挂起（见基线报告"挂起记录"）：是否需要单独立任务深查根因？工程队未深查。
2. llmlint 的 web/ 目录缺 tsconfig 导致 24 个测试文件无法收集：修复属改配置文件，超出本任务边界，请前线定归属。

## 交付报告（工程队填写）

**做了什么**：在主工作区全量复跑 12 个包的默认 `test` 脚本（未跑 test:real-model 等），产出 `docs/tasks/evidence/任务005/测试基线.md` 及 13 份原始日志（主包 1 份 + 各包 11 份 + harness 重试 1 份）。主包跑了两次：第 1 次只保留了 tail 摘要，第 2 次完整落盘并作为基线口径。

**为什么**：补齐全仓从未统一跑过测试的空白，为后续开发提供对照基线；失败不修，如实记录。

**自测结果**：10 包全绿；主包 17 条失败（两类根因：worker ESM 模块解析失败 12 条、缺 NEURO_BOOK_REPOSITORY_ROOT 5 条）；llmlint 24 个测试文件 TSCONFIG_ERROR 收集失败；harness 全量挂起、分片合并 521 条全绿。详见基线报告。

**异常与绕过说明**：① harness 挂起后未无限等待，先跳过再分片补齐 9 个未执行文件，合计口径在报告中标明"分片合并"；② 中途一次循环脚本因 shell 工作目录残留导致 10 个包未实际执行（秒退），已用绝对路径重跑，无影响；③ 跑测试期间其他窗口修改了 BOARD.md 并新增闲时任务文件，与本任务无关，未纳入本次提交。

**遗留问题**：见疑问区 2 条。

**状态**：**待审**（工程队按开发者本次指令直接改 BOARD 并 commit；AGENTS.md 任务书原文的"BOARD 由前线更新"以开发者指令为准）。

**本窗口会话 ID**：sess_f7630ad3-9078-4e74-b751-945479569d6e
