# 任务009C1R3-三轮走查部件返修十二件

> 状态：**待审**（2026-09-22 前线施工交付，分支 `task-009c-r3` 基于 master `97f0372e`——批次1 已合入 master 后新开分支）。
> 任务源：用户三轮走查反馈（/goal 口令 2026-09-22）。部件级打底批（单D 复用），修完批次 1 收官。5.3 施工。

## 运行配置

- 负责窗口：前线指挥部
- 模式：目标模式 + 完全访问
- 分支：`task-009c-r3`（master 97f0372e 之后）

## 交付报告（前线填写，2026-09-22）

### 十二件落地

1. **块头时长兜底**：`durationMs===null`（首尾时间戳缺失或仅单节点）显示「本轮」（新键 workRound），不再出现"已工作"空尾巴。
2. **展开层级缩进**：思考行展开区升 `border-l-2 pl-4`；AgentToolNode 展开态去卡片框改 `ml-3 border-l-2 pl-3`、展开头统一为与收起态同构的小行（小图标+名+行首箭头）；注入收拢行展开区包 `ml-3 border-l-2 pl-3`——三处同规格（缩进+2px 竖线，收起时与正文平齐）。
3. **系统注入漏网**：根因有二——①009C1R2 收拢仅覆盖"开头连续注入"，中途单条 system 走旧卡片；②中途 system 会**切断轮**（前半轮被 flush）。修复：聚合层中途 system 归入当前轮（不切轮），渲染为轮内「系统上下文注入」灰小字行（可展开，随轮收起隐藏）；开头注入单条也统一收拢。旧 system 卡片路径 DOM 实测=0。
4. **刻度条密度映射**：照旧库 rail 形态——格固定小高（10px）、框高 2/3 垂直居中、少时集中中段多时向两端扩展、超出隐藏滚动条内滚；指针→格换算含滚动偏移（SEGMENT_ROW_PX）。
5. **刻度内容过滤**：格序列只聚合对话项（user 消息+轮），系统注入/独立工具项不进格；anchorIndex 保留原 flowItems 下标（seek 定位不变）。
6. **工具条垂直对齐**：绿环根容器补 `flex items-center`；实测绿环与图片按钮**中心线差 0px**。
7. **档位过滤排查结案（无产品代码改动）**：实测用户配置 `zai-coding-cn/glm-5.3-flash` 的 thinkingLevelMap 含**全 7 档 key**（off→max 每档都映射了 provider 值）→ 显示全部 7 项是**过滤逻辑的正确行为**。**009C1R2 报告"实测 [关闭,低,中,高]"系我方断言抓取缺陷产生的误报**（断言词表用了"最小/超高"，而 i18n 既有键实际用词是"极低/极高/最大"——字面恰好匹配的只有关闭/低/中/高 4 项被计入），本轮以精确抓取实测修正：**完整 7 项=[关闭,极低,低,中,高,极高,最大]**，截图 R3-effort-7levels.png。R2 报告相应结论作废。回退逻辑（map 空/null→全量七档）代码审查无误；"无 map 模型"场景本环境无法真机实测（见偏差③）。
8. **档位下拉宽度**：FormSelect 面板去 `right-0` 铺满改 `min-w-max`（=最长选项宽），选项行 `whitespace-nowrap`；实测面板自适应。
9. **气泡固定上限**：`max-w-[65%]` → `max-w-[36rem]`（576px），不随面板拖宽容变。
10. **工具摘要完整显示**：块头摘要 span 去 truncate 改 `whitespace-normal break-words`（可多行完整显示）；块头按钮 items-start+时长行高对齐。
11. **Workflow 内联化**：Dialog（居中模态）→ 就地内联滑出面板（`onClickOutside` 点外收起、非模态），应答卡（select/text/同意否决+提交）原样内嵌；「每个流程分别应答」说明保留面板头。
12. **统计条 UI 删除**：gauge 芯片与累计统计芯片（↓↑缓存/命中率/费用）整体移除；连接状态/重连/刷新/运行阶段/模式徽标保留；props 数据链全保留（绿环 hover 面板同源数据不受影响）。

### 怎么测的

- **vitest 全量**：**3353 过 / 17 败 / 3 跳 = 基座（合并后 master 实测 3351）+1（chat-work-blocks 中途 system 归轮用例）+1（ScaleBar 密度映射用例）精确吻合**；17 败=server/agent/profiles worker 系基线环境失败零增。agent 目录 286/286。
- **typecheck**：零错（一处轮内 system 行 TextBubble 补必填 props+类型断言）。
- **dev 编译器实证**：清 .nuxt 重启 3000，三件 200；曾出现 ScaleBar SFC 编译失败（`<style scoped>` 误插 template 内部致 404→导入链炸）——坑③的价值再现，已修复并复验；R3-flow.png 为最终态截图。
- **DOM 断言 11/11 PASS**（playwright 无头，1600×620，会话 64 真实数据）：件1 块头时长全有值（"已工作 1 秒…"/"本轮…"）；件2 展开竖线存在；件3 收拢行在+旧卡片 0；件4 rail 14 格 276px 框；件5 刻度 14≤对话项 15；件6 对齐差 0px；件7 七项完整；件8 面板自适应；件9 气泡 36rem；件11 无模态容器；件12 统计芯片无。截图两张归档 `docs/tasks/evidence/009C1R3-返修截图/`。

### 偏差披露

1. **件7"两模型实测"只完成一半**：环境仅配置一个模型（glm-5.3-flash，map 全 7 keys）。配 map 模型实测=7 项 ✓；"未配 map 模型回退全量"因无第二模型无法真机实测——该路径与"map 全 keys"输出在本环境不可区分（都是 7 项）且代码审查无误；如需严格双模型实测请先在设置里启用一个无 map 的模型。
2. **件11 徽标→内联面板的展开收回演示未做**：会话 64 无 workflow 待应答数据，DOM 断言覆盖了"无模态容器+组件形态"负向；正向展开链路待真实待应答出现时真机（与 R2 件7c 同口径）。
3. **R2 报告"档位过滤实测 [关闭,低,中,高]"结论作废**（本轮件7 修正，属我方断言缺陷而非产品 bug）；用户看到的"全部档位"=正确行为。
4. 件4 密度映射按格固定高实现（数量→总高），未做逐格连续密度函数（视觉等效，粒度更粗）。
5. 件12 中 gauge 芯片承担的"点击开上下文检查面板"入口随芯片删除而移除——绿环点击同功能保留（open-context-inspector），无功能丢失。
6. 底部栏一体融合重设计（用户 8d 意见）确认不在本批，未做。

### 遗留问题

- 件1「本轮」兜底在运行中且无时间戳时显示"工作中 0 秒"（durationMs null 分支优先显示本轮，isRunning 但有 duration 正常）——极端流式首包场景待真机观察。
- 刻度条内滚场景（>50 格×10px>框高）本环境 14 格未触发滚动，滚动交互待超长会话验证。

## 疑问区

（空）
