# 任务033-G2G3审稿态契约

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：GLM-5.3-Flash 执行（2026-09-25 用户终裁授权硬上先例沿用），推理档位 high。
- **模式**：目标模式 + 完全访问（**涉码**，主仓库分支 `task-033`，无 worktree）。**本单先契约后施工：本契约单经用户+参谋部复核通过前，禁止 checkout 分支动任何代码。**
- **给用户的现成命令**（复核通过后粘贴生效）：
  - `/goal 任务033-G2G3审稿态契约：按任务书契约基线与验收标准完成审稿态/块级 diff 施工，逐条断言交付`
- **对口技能与命令**：
  - 代码/diff 审查 → `ocr-review`；过度工程检查 → `ponytail-review`；前端施工规矩 → `neuro-frontend`
  - UI/UX 判据 → `ui-ux-pro-max`；dev server 验证 → 无头脚本（`127.0.0.1:3000`，禁 localhost）

## 目标

落定 P0-02「审稿态与块级 diff 契约」（施工计划 v1 §P0-02）：审稿态（reviewMode）与正文块级 diff 从零实现到契约+施工，支撑蓝图 ：97 融合稿面审稿界面（右区正文+审稿标签页切换、逐块通过/拒绝）。用户可感行为：**逐块通过的段落在正文视图立即出现；拒绝的段落变成输入框上方的引用气泡可带意见重提案；整件拒绝全组销号不留幽灵；刷新/重进后审稿状态原样；每次写入都在文件历史时间线上可溯可撤销。**

## 契约基线（前线定，执行中偏差必须披露并给理由；复核方可增删）

1. **块级 diff 数据结构（G3）**：`ReviewBlock{blockId, sourceId, old, new, status, impactIds}`——status 枚举=`pending/accepted/rejected/void`（施工计划 P0-02 明文）。blockId 全局唯一且不复用（旧组 void 后重新提案=新组新 ID，防幽灵）；**void 组的 ID 与决议记录不物理删除**（保留可溯，防历史断链——参谋部复核裁定）。
2. **状态机**：`pending→accepted`（触发正文写入）；`pending→rejected`（转引用气泡+重新提案通道）；整件拒绝=全组 `void`（终态，不可复活，与 K4 整组销号对称）。void/rejected 块不得再次迁移。
3. **块↔正文映射**：blockId 与章节文件行段的映射结构**挂 nb-history 快照体系扩展**（架构对账 G3 建议落点），**禁另起第二套历史/快照系统**。
4. **写入与撤销通道**：通过块立即写正文=`WorkspaceHistory.performWrite/registerWrite`（时间线自动可溯）；撤销=`revertAtRevision`（乐观锁，防并发覆盖）——撤销/恢复验收的底座，不自写回滚。
5. **对照视图底座**：diff 计算用 `WorkspaceHistory.textDiff(beforeHash, afterHash)`（现成，底层 diffLines），禁复制另一套 diff 实现；审稿窄栏展示的块级 old/new 从 textDiff 结果切块派生。
6. **与 G1 统一待办库对接**：`UnifiedTodoKind` 的 `content_review`（G1 占位）接实——审稿件入统一库、计数读库（A8 延续）、**拒绝块转堆叠卡待办**（断头路 A10：拒绝防丢）。
7. **拒绝闭环**（蓝图 :99）：rejected 块变引用气泡挂输入框上方，用户带引用写修改意见发送→AI 重新提案（新 ReviewBlock 组）；旧组 void 保留可溯。
8. **决议记录归档**：逐块决议（通过/拒绝/整件）归档可溯（断头路 A10③）——载体=nb-history 操作日志侧记录（写入原因/决议引用），验收锁「任一块的终态可回答谁在何时因何通过/拒绝」。
9. **审稿视图准入**：仅涉正文实质改动件进审稿视图（蓝图 :101⑤ 用户定）；轻量快批件（工具审批/设定拍板/Workflow 应答）走堆叠卡不入审稿。
10. **UI 舞台**：右区正文+审稿**标签页切换**（随时切回正文互不打断）；审稿窄栏列审稿项+影响行；quiet 规格+基板 token，禁第二套「计划卡/审稿真相」。
11. **审稿状态持久化载体**（参谋部复核必补项）：审稿组（`ReviewBlock[]` 含 status 与块映射）落盘为**章节伴随状态文件**（与章节正文同目录的 `.review.json`，精确路径与命名施工时定），且**读写全走 `WorkspaceHistory` 管道**——registerWrite 进时间线/快照，决议记录=状态文件的历史快照链（基线 8 由管道免费满足，禁额外决议日志系统）。刷新/重进恢复=读状态文件重建 store（G1 `content_review` 件随之恢复）。accepted 块的正文写入与状态迁移须同轮落盘：状态文件记录已应用正文的 revision，启动对账检测半完成态（状态 accepted 但正文 hash 不符→该块转 void 并披露，禁静默重写）。

## 范围与边界

- **允许改动**：`packages/neuro-book/app/` 审稿链路新模块/组件/测试（review-blocks 状态机、审稿视图组件）、`app/stores/` 相关 store、`packages/nb-history/` 仅限既有扩展点内的挂载，优先向后兼容扩展（可选参数/钩子，**不改签名**）；若判断必须动公开 API——**先停手，疑问区登记上报待裁**（不得边披露边做——参谋部复核裁定）、i18n 双语（只增不改不删+zh/en 对称）、相关契约测试。
- **禁止改动**：master 直接提交；`packages/nb-ui/` 全族（只用不改）；批次1 四件（ChatFlow/chat-work-blocks/TextBubble/ScaleBar）行为逻辑；G1 unified-todo 契约面（只消费不重构）；上游 rebase 冻结期合并；任何第二套历史/快照/diff/待办计数系统。
- **不确定时**：停下，在任务书「疑问区」登记，待前线答复。

## 步骤建议

1. 契约测试先行（先红）：状态机四迁移+整件销号+部分成功混合态+撤销恢复+刷新持久化。
2. 块结构+状态机纯逻辑（不碰 UI）；块映射与 nb-history 对接。
3. G1 `content_review` 接入统一库（计数同减用例）。
4. 审稿视图 UI（窄栏/标签页/影响行）——三源复用清单为凭。
5. 真机 smoke+ocr/ponytail 自查+分段交付报告。

## 三源复用清单（本批必带，验收员查「该抄未抄=违规重写」）

- **源一·上游底座**：nb-ui 组件（标签页/行卡/按钮基件）；布局位对齐 workbench 右区槽位语义。
- **源二·自家批次1**：ChatFlow 过程行聚合形态=审稿视图内 AI 交互的行为底本（换皮不重写）；AgentUserInputPrompt 阻塞卡不动（审稿项不走阻塞卡）；拒绝引用气泡复用注入行范式（injection 系）。
- **源三·prefab**：`DecisionStack`（堆叠卡）承载「拒绝块转待办」；`CockpitPanel` 待办行接审稿计数（`inspirationCount` 同模式扩展）。**如实修正**：架构对账所称「审稿台空壳」在 prefab 七件（Cockpit/DecisionStack/History/Inbox/Settings/TierDrop/WorkspaceStage）中实际不存在——审稿视图为新组件，但三源已有件禁重写。

## 验收标准（逐条留命令输出，禁报完成数量只报逐条明细）

1. 契约测试（先红后绿）：pending→accepted（正文写入触发）/pending→rejected（气泡+重提案通道）/整件拒绝全组 void 且不可复活/部分成功混合态（10 块过 7 拒 3）/revertAtRevision 撤销成功/刷新重进状态不变。
2. 块映射往返：encode→decode 恒等（blockId↔行段映射无漂移）。
3. nb-history 对接：通过块写入后 `timeline` 可查+`textDiff` 对照正确+决议记录可回答「谁何时因何」。
4. G1 对接：content_review 件入统一库，审稿计数与驾驶舱/堆叠卡同源同减（A8 延续用例）。
5. 全量 vitest 对账精确到个位：基线=开工首日 master 实测（当前 3408 过/17 败 profiles 环境族），新增用例逐条列名。
6. `bun run typecheck` 零错；i18n 只增不改不删+zh/en 对称（grep 两侧键数相等）。
7. 真机 smoke：审稿标签页开合/逐块通过后正文视图同步/刷新后状态保持，截图归档 evidence。
8. ocr-review+ponytail-review 自查，结论回写交付报告。

## fixture 与真实接线边界

- 审稿项用合成 fixture（章节正文+预切块 diff），不真实调用 Provider/Model（AI 提案侧归写作链路轮次）。
- nb-history 用 `neuro-book-test-support` 临时根 fixture（005 基线先例）；真实会话数据仅真机 smoke 只读观察。
- 本单只做审稿态契约与审稿视图——P0-03/P0-04 样张接线和上游 workbench 骨架合并（适配轮）不在本单。

## 参考材料

- `out/审计/单D-基板通用件补全与Ant对账施工计划-v1.md` §P0-02（规格源）
- `out/审计/单D-断头路排查清单.md` A10/B2（部分通过/问题项转待办施工规格）
- `docs/design/009-单D-架构UI对账报告.md` :17-19（G2/G3 现状）与 :52-55（惊喜发现）
- `docs/design/009-单D-UI总攻蓝图.md` :97/:99/:101（融合审稿/拒绝闭环/准入）
- `packages/nb-history/src/workspace-history.ts`（revertAtRevision/textDiff/timeline 现成 API）
- `docs/tasks/任务031-G1统一待办库契约.md`（content_review 占位与 unified-todo 契约面）

## 疑问区（施工窗口填写）

（空）

## 交付报告（施工窗口填写）

（空。格式：做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）
