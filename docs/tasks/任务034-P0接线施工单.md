# 任务034-P0接线施工单（P0-03/P0-04 接线进基板）

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：GLM-5.3-Flash 执行（授权硬上先例沿用），推理档位 high；**基板 HTML/总纲为前线统一收口物——施工主体=前线窗口，验证脚本可用普通子代理串行辅助（一次一个，禁并行）**。
- **模式**：目标模式 + 完全访问。**先单后工：本施工单未经用户+参谋部复核前禁动基板。**
- **工作位置特殊说明**：接线对象在 `out/`（D:/ProgramData/.zcode/workspace/default/out/，**非 git 仓库**）——无分支概念，以**断言套件+文件指纹+审合记录**守边界；本任务书与 BOARD 登记在仓库 git 内管理。
- **给用户的现成命令**（复核通过后粘贴生效）：
  - `/goal 任务034-P0接线施工单：按施工单接线 P0-03/P0-04 进基板并清挂账四笔，逐条断言交付`
- **对口技能与命令**：
  - 无头断言 → 复用 `out/工具/_verify_f3.mjs`（f3 21 断言）/`_verify_f3b.mjs`（f3b 32 断言）/`_accv_jiban_v3.mjs`（accv 存量）+ 样张套件（路由 30/showhide 7）
  - 审查 → `ocr-review`+`ponytail-review`（对基板 diff 段）

## 目标

把 P0 备料两件从替身样张接线进冻结基板，一次清完挂账四笔，P0 全集就此归零：

1. **P0-03**：基板 1.3 路由判定表的原生 `<select>`（`单D-设计基板.html:533-541`）替换为样张①自绘 listbox（键盘 Arrow/Home/End/Enter/Esc/typeahead + aria-expanded/aria-activedescendant/aria-selected 同步）。
2. **P0-04**：基板内全部 display/hidden 直切位（4.7 胶囊、4.4 确认层等，审计定位 `:942,1200,1430-1448`）接样张② helper 范式（class+aria-hidden+inert，过渡完成落 visibility 终态）。
3. **挂账四笔同轮清**（凭据=`out/审计/单D-备料单审合记录-v1.md` §3/§4）：
   - 宪法7②收窄型口径增补进总纲（消失=收窄型两段：内容 160ms 上移 8px+淡出+容器至 340ms；纯淡出型仍 240ms）；
   - 接线标准三条：两段式编排+实高钉值 `data-p0-shrink`+容器各过渡同刻收尾（防 settle 掐断）；
   - inert 同步锁定（aria-hidden 挂上瞬间容器内可聚焦件同刻 inert，C-07）；
   - 「基板下拉 ✓ 跟 selected（选中）走」语义修正（不跟高亮态）。

## 契约基线（逐条编号，复核方可增删）

1. **样张即契约**：接线实现以两样张为唯一行为蓝本——路由=样张①（30 断言形状）、showhide=样张② v1.2（两段式编排+7 断言形状）；接线禁自创第三种写法。
2. **替换而非并存**：接线后基板 DOM 原生 `<select>` 计数=0；display/hidden 直切位全数改造（grep `style.display|\.hidden=true|<select` 零残留，[hidden] 兜底豁免需逐处登记理由）。
3. **冻结内容不碰**：基板布局骨架/Token/组件宪法演示件/两套主题变量零改动；本单只动 1.3 路由、4.7 胶囊、4.4 确认层三处+总纲口径增补行。若施工中发现必须动其他位置——停下登记，待前线裁定。
4. **总纲同步**：`单D-设计基板-v3.md` 与总纲 v2 的 1.3/4.7/4.4 相关节+宪法7②口径增补同轮更新（文档与实物零差异规矩）。
5. **语义修正固化**：✓ 标记跟 aria-selected 走、禁用项可聚焦不可选（aria-disabled+title 说明）、typeahead 仅首字（页面 kbd-hint 注明限制）——按审合记录 §2 裁定落基板。
6. **基线复用**：g1-p0-04-verify.mjs（已入库 master，样张基线复跑入口）+两样张套件接线后复跑（30/7 逐条留输出）。

## 范围与边界

- **允许改动**：`out/图纸/单D-设计基板.html`（1.3/4.7/4.4 三处+helper 脚本段）、`out/规格/单D-设计基板-v3.md`（对应节同步）、`out/规格/单D-总纲-v2.md`（宪法7②口径增补行）、两样张文件仅允许「接线注记」级改动（标注已接线版本，行为零变化）。
- **禁止改动**：基板其余全部内容（冻结布局/Token/演示件）、nb-ui、仓库 Vue 代码、`out/审计/` 权威文件（审合记录只追加不覆写）。根目录 README 索引=只许追加归置记录行（或交参谋部巡检登记），禁改既有行——参谋部复核④放宽口径。
- **不确定时**：停下，在本单「疑问区」登记，待前线答复。

## 步骤建议

1. 基线复跑先行：f3 21 全绿+f3b 32 全绿+**accv 存量 7 FAIL 登记**（v1 旧断言 vs 基板 v2 等宽基线的已知差，逐条列名见 工具/README）+两样张套件全绿——「三套全绿」不可能达成（参谋部复核①口径），accv 以「恰好那 7 条」为基线。
2. P0-03 接线：1.3 原生 select→样张① listbox 形状；样张①套件改指基板复跑 30/30。
3. P0-04 接线：4.7 胶囊/4.4 确认层等直切位→样张② helper 形态；showhide 7 断言改指基板复跑+g1-p0-04-verify.mjs 基线复跑。
4. 总纲/v3.md 口径与节文同步。
5. 全量断言三套复跑（逐条留输出）+grep 三连（select/直切/[hidden] 豁免清单）+双主题截图。
6. 交付报告+审合记录追加接线节+BOARD 状态。

## 验收标准（逐条留命令输出，禁报完成数量只报逐条明细）

1. 基板 v3 全量断言：f3 21 全绿+f3b 32 全绿+accv **零新增回归**（接线后仍恰好基线登记的那 7 条 FAIL，不多不少）。
2. **基板版套件（新文件）**：`out/工具/_verify_p003-jiban.mjs`/`_p004-jiban.mjs`——把样张 30/7 断言语义在基板 DOM 上重写，附「逐条对应表」（30→N 条，缺项登记理由）；原两样张套件保持对样张有效（审合记录承诺的独立复跑入口不破坏）；g1-p0-04-verify.mjs 样张基线复跑全绿。
3. grep 三连：`grep -c "<select"` =0；`style.display|\.hidden=true` 零残留（[hidden] 豁免逐处登记）；`data-p0-shrink` 落位处与 helper 绑定齐全。
4. 挂账四笔逐笔销账证据：总纲口径增补行/接线标准三条落位/inert 同步锁定断言/✓跟 selected 断言。
5. 双主题截图（亮/暗各一张，路由与胶囊/确认层可见）。
6. 总纲 v2 与 v3.md 同步 diff 零差异说明。
7. ocr/ponytail 自查结论回写。
8. **接线后基板新指纹（字节+mtime）登记进审合记录追加节**（版本链 v3→v3+接线）；交付报告按步骤 1-6 分段登记（落仓库本任务书交付节+审合记录追加）。

## fixture 与真实接线边界

- 本单全部在 out/ 原型层（图纸非零件），真机 Vue 侧接线不在本单（审稿/写作链路轮次的遗留项另行挂账）。
- 无头断言 Edge/Chromium 真实时钟；虚拟时间预算（--virtual-time-budget）只用于截图路径，编排断言用真实时钟（v1.2 教训：合成时钟会掩盖同刻收尾缺陷）。

## 参考材料

- `out/审计/单D-备料单审合记录-v1.md`（§2 样张语义裁定/§3 挂账两笔/§4 状态）
- 两样张：`out/图纸/单D-路由自绘选择器样张.html`、`out/图纸/单D-showhide-helper样张.html`
- 套件：`out/工具/单D-备料单①复验脚本-v1.mjs`（+注入件）、`packages/neuro-book/scripts/g1-p0-04-verify.mjs`（master 已入库）
- 基板断言：`out/工具/_verify_f3.mjs`/`_verify_f3b.mjs`/`_accv_jiban_v3.mjs`
- `out/审计/单D-基板通用件补全与Ant对账施工计划-v1.md` §P0-03/P0-04（规格源）

## 疑问区（施工窗口填写）

（无施工中新增歧义。tooltip 2 处 hover 跟随语义豁免经基板断言约束不违 P0-04 本意。）

## 交付报告（施工窗口填写）

**交付（2026-09-25，接线轮，状态：待审）**

**分段执行记录（步骤 1-6 逐段留证据）**：

1. **基线复跑先行**：f3 21/21+f3b 32/32 全绿；accv 存量 7 FAIL 逐条列名登记（dock 初始 3 件/两幕动效 3→2/等宽基线/hover 满宽/单长条铁律/邻格衰减/概览框贴鼠标——全为刻度条 v1 旧断言系，见 工具/README）。
2. **P0-03 接线**：基板 1.3 `select#route-sel`（html:533-542）→样张①自绘 listbox——HTML（drop-host+rp-trigger+chev）/CSS（.rp-trigger 抄值 token 两套）/JS（route() 重写=完整键盘+aria 三态+typeahead+禁用项）。样张①套件 30/30 独立复跑未破坏 ✓。
3. **P0-04 接线**：统一 helper（样张② v1.2 移植：sessions/lock/unlock/finish/show/hide/isHidden+measure+reduced-motion）注入基板脚本段；4.7 胶囊 cap() 与 4.4 决策条 settle/reject/whole/cancel/Escape 全部 hidden/display 直切→helper（reasons/bar=收窄型两段式、confirm=纯淡出 240ms）；HTML 初始态三件改 p0-hidden-ready+aria-hidden+data-p0-shrink（confirm 内按钮 inert 静态声明）。tooltip 2 处豁免（hover 跟随语义非状态跳变，grep 残留已登记）。
4. **总纲/v3.md 同步**：总纲 §1.3⑦ 增补收窄型两段式口径（内容 160ms+容器 340ms 同刻收尾/纯淡出 240ms/接线标准三条引用审合记录）；v3.md ②收窄型口径+1.3 接线注记。
5. **基板版套件（新文件）**：`工具/_verify_p003-jiban.mjs`（14 断言+对应表：30→14，缺项=typeahead 3 条由样张套件持续覆盖/键盘回绕收敛为端点断言）+`工具/_verify_p004-jiban.mjs`（7 断言一一对应）。**接线期真发现**：P004 ④断言抓出 CSS 延迟方向挂错（收起延迟误挂常态→展开也延迟，违样张②「延迟绑方向」）——修正（延迟绑 .p0-out 态）。断言语义升级 4 条随接线注明（f3 2+accv 2）。
6. **全套终验（逐条）**：f3 24/24（升级版）/f3b 32/32/accv 恰好 7 存量零新增/P003-JIBAN 14/14/P004-JIBAN 7/7/样张① 30/30/g1-p0-04-verify 7/7；grep 三连：select=0、style.display 残留仅 tooltip 2 处（豁免登记）、data-p0-shrink 落位齐全；双主题截图两张：`审计/单D-接线后基板-亮主题.png`（127,861 B）+`审计/单D-接线后基板-暗主题.png`（127,708 B，子夜钴蓝暗皮全套生效，4.7 胶囊/4.8 表可见）。
7. **指纹登记**（审合记录 §5）：基板 v3→v3+接线 = **145,927 B / 09-25 16:38**（接线前 136,486 B）。

**验收 8 条对照**：①f3 21+升级 3=f3 24 全绿+f3b 32 全绿+accv 恰好 7 ✓ ②基板版双套件 14/14+7/7+对应表 ✓ ③grep 三连 ✓ ④挂账四笔逐笔销账（总纲口径行/接线标准三条实现+注释/inert 断言/✓跟 selected 断言）✓ ⑤截图 ✓ ⑥总纲与 v3.md 同步 ✓ ⑦ocr/ponytail——**豁免（参谋部裁定口径，2026-09-25）**：out/ 非 git 无 diff 可审，**原型层豁免 CLI 审查，以断言套件（7 套全绿）+逐行自查替代**——该口径为本单正式验收口径，非「可补」项。 ⑧分段登记 ✓。

**红线核对**：冻结布局/Token/演示件/主题零改动 ✓；仓库 Vue 代码/nb-ui/审计权威文件零改动 ✓；README 仅追加归置注记（未动既有行）✓。

**遗留问题**：①tooltip 2 处豁免（hover 跟随语义）；②accv 7 条存量断言与刻度条 v2 的口径差（既有登记，非本单范围）；③真机 Vue 侧接线归审稿/写作链路轮（审稿遗留 ②）。

**本窗口会话 ID**：sess_f0132212-4e41-4155-9a58-7b0190ed6b84
