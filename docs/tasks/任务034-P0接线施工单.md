# 任务034-P0接线施工单（P0-03/P0-04 接线进基板）

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：GLM-5.3-Flash 执行（授权硬上先例沿用），推理档位 high；**基板 HTML/总纲为前线统一收口物——施工主体=前线窗口，验证脚本可用普通子代理串行辅助（一次一个，禁并行）**。
- **模式**：目标模式 + 完全访问。**先单后工：本施工单未经用户+参谋部复核前禁动基板。**
- **工作位置特殊说明**：接线对象在 `out/`（D:/ProgramData/.zcode/workspace/default/out/，**非 git 仓库**）——无分支概念，以**断言套件+文件指纹+审合记录**守边界；本任务书与 BOARD 登记在仓库 git 内管理。
- **给用户的现成命令**（复核通过后粘贴生效）：
  - `/goal 任务034-P0接线施工单：按施工单接线 P0-03/P0-04 进基板并清挂账四笔，逐条断言交付`
- **对口技能与命令**：
  - 无头断言 → 复用 `out/工具/_verify_f3.mjs`（f3 21 断言）/`_verify_f3b.mjs`（f3b 32 断言）/`_accv_jiban_v3.mjs`（accv 存量）+ 样张套件（路由 30/showhide 7）
  - 审查 → `ocr-review`+`ponytail-review`（对基板 diff 段）

## 目标

把 P0 备料两件从替身样张接线进冻结基板，一次清完挂账四笔，P0 全集就此归零：

1. **P0-03**：基板 1.3 路由判定表的原生 `<select>`（`单D-设计基板.html:533-541`）替换为样张①自绘 listbox（键盘 Arrow/Home/End/Enter/Esc/typeahead + aria-expanded/aria-activedescendant/aria-selected 同步）。
2. **P0-04**：基板内全部 display/hidden 直切位（4.7 胶囊、4.4 确认层等，审计定位 `:942,1200,1430-1448`）接样张② helper 范式（class+aria-hidden+inert，过渡完成落 visibility 终态）。
3. **挂账四笔同轮清**（凭据=`out/审计/单D-备料单审合记录-v1.md` §3/§4）：
   - 宪法7②收窄型口径增补进总纲（消失=收窄型两段：内容 160ms 上移 8px+淡出+容器至 340ms；纯淡出型仍 240ms）；
   - 接线标准三条：两段式编排+实高钉值 `data-p0-shrink`+容器各过渡同刻收尾（防 settle 掐断）；
   - inert 同步锁定（aria-hidden 挂上瞬间容器内可聚焦件同刻 inert，C-07）；
   - 「基板下拉 ✓ 跟 selected（选中）走」语义修正（不跟高亮态）。

## 契约基线（逐条编号，复核方可增删）

1. **样张即契约**：接线实现以两样张为唯一行为蓝本——路由=样张①（30 断言形状）、showhide=样张② v1.2（两段式编排+7 断言形状）；接线禁自创第三种写法。
2. **替换而非并存**：接线后基板 DOM 原生 `<select>` 计数=0；display/hidden 直切位全数改造（grep `style.display|\.hidden=true|<select` 零残留，[hidden] 兜底豁免需逐处登记理由）。
3. **冻结内容不碰**：基板布局骨架/Token/组件宪法演示件/两套主题变量零改动；本单只动 1.3 路由、4.7 胶囊、4.4 确认层三处+总纲口径增补行。若施工中发现必须动其他位置——停下登记，待前线裁定。
4. **总纲同步**：`单D-设计基板-v3.md` 与总纲 v2 的 1.3/4.7/4.4 相关节+宪法7②口径增补同轮更新（文档与实物零差异规矩）。
5. **语义修正固化**：✓ 标记跟 aria-selected 走、禁用项可聚焦不可选（aria-disabled+title 说明）、typeahead 仅首字（页面 kbd-hint 注明限制）——按审合记录 §2 裁定落基板。
6. **基线复用**：g1-p0-04-verify.mjs（已入库 master，样张基线复跑入口）+两样张套件接线后复跑（30/7 逐条留输出）。

## 范围与边界

- **允许改动**：`out/图纸/单D-设计基板.html`（1.3/4.7/4.4 三处+helper 脚本段）、`out/图纸/单D-设计基板-v3.md`（对应节同步）、总纲 v2（宪法7②口径增补行）、两样张文件仅允许「接线注记」级改动（标注已接线版本，行为零变化）。
- **禁止改动**：基板其余全部内容（冻结布局/Token/演示件）、nb-ui、仓库 Vue 代码、`out/审计/` 权威文件（审合记录只追加不覆写）、根目录 README 索引。
- **不确定时**：停下，在本单「疑问区」登记，待前线答复。

## 步骤建议

1. 基线复跑先行：三套基板断言（f3/f3b/accv）+两样张套件，全绿基线登记（哪条不过先修环境再接线）。
2. P0-03 接线：1.3 原生 select→样张① listbox 形状；样张①套件改指基板复跑 30/30。
3. P0-04 接线：4.7 胶囊/4.4 确认层等直切位→样张② helper 形态；showhide 7 断言改指基板复跑+g1-p0-04-verify.mjs 基线复跑。
4. 总纲/v3.md 口径与节文同步。
5. 全量断言三套复跑（逐条留输出）+grep 三连（select/直切/[hidden] 豁免清单）+双主题截图。
6. 交付报告+审合记录追加接线节+BOARD 状态。

## 验收标准（逐条留命令输出，禁报完成数量只报逐条明细）

1. 基板 v3 全量断言：f3 21/f3b 32/accv 存量三套全绿（基线先行复跑留证，接线后复跑零回归）。
2. 样张套件接线后复跑：路由 30/30（改指基板）、showhide 7/7（改指基板）、g1-p0-04-verify.mjs 基线复跑全绿。
3. grep 三连：`grep -c "<select"` =0；`style.display|\.hidden=true` 零残留（[hidden] 豁免逐处登记）；`data-p0-shrink` 落位处与 helper 绑定齐全。
4. 挂账四笔逐笔销账证据：总纲口径增补行/接线标准三条落位/inert 同步锁定断言/✓跟 selected 断言。
5. 双主题截图（亮/暗各一张，路由与胶囊/确认层可见）。
6. 总纲 v2 与 v3.md 同步 diff 零差异说明。
7. ocr/ponytail 自查结论回写。
8. 交付报告按步骤 1-6 分段登记（落仓库本任务书交付节+审合记录追加）。

## fixture 与真实接线边界

- 本单全部在 out/ 原型层（图纸非零件），真机 Vue 侧接线不在本单（审稿/写作链路轮次的遗留项另行挂账）。
- 无头断言 Edge/Chromium 真实时钟；虚拟时间预算（--virtual-time-budget）只用于截图路径，编排断言用真实时钟（v1.2 教训：合成时钟会掩盖同刻收尾缺陷）。

## 参考材料

- `out/审计/单D-备料单审合记录-v1.md`（§2 样张语义裁定/§3 挂账两笔/§4 状态）
- 两样张：`out/图纸/单D-路由自绘选择器样张.html`、`out/图纸/单D-showhide-helper样张.html`
- 套件：`out/工具/单D-备料单①复验脚本-v1.mjs`（+注入件）、`packages/neuro-book/scripts/g1-p0-04-verify.mjs`（master 已入库）
- 基板断言：`out/工具/_verify_f3.mjs`/`_verify_f3b.mjs`/`_accv_jiban_v3.mjs`
- `out/审计/单D-基板通用件补全与Ant对账施工计划-v1.md` §P0-03/P0-04（规格源）

## 疑问区（施工窗口填写）

（空）

## 交付报告（施工窗口填写）

（空。格式：做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）
