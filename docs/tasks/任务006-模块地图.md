# 任务006-模块地图

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3-Flash`，推理档位 **high**。若当前会话未在此配置，开工第一句先提醒用户切换。
- **模式**：**目标模式 + 完全访问**——静态分析+归纳成单一文档，目标明确无需逐项确认。
- **工作区**：`D:\MyProject\neuro-book` 主工作区（master 分支）直接执行，**无 worktree**（文档类任务豁免）。工程队不执行任何 git commit，文件写好后由前线统一提交。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务006-模块地图：按任务书静态分析全仓页面/路由/组件复杂度，产出 docs/knowledge/模块地图.md，完成后在任务书末尾写交付报告
  ```

## 目标

静态分析全仓页面/路由/组件复杂度，产出一份模块地图文档，为后续信息架构（IA）重构（任务008/009）提供数据底座。

## 开工前置

1. 读 `D:\MyProject\neuro-book\AGENTS.md` 的"本 Fork 工作规矩"节（尤其 worktree 豁免规则与工程队分区：只写 `docs/knowledge/`、`docs/tasks/evidence/` 和本任务书交付报告区）
2. 读本任务书全文，确认模式/模型已就位
3. 确认在 `D:\MyProject\neuro-book` 主工作区（master 分支）：`git rev-parse --show-toplevel` 应输出该路径

## 范围与边界

- 允许改动：仅新建 `docs/knowledge/模块地图.md`；本任务书疑问区与交付报告
- 禁止改动：任何源码、配置、master 分支提交、BOARD（由前线独家维护）、其他任务书
- 只读静态分析：允许用 grep/ls/wc 统计，不允许跑会写文件的命令

## 执行步骤

1. 读 `docs/modules/monorepo-boundaries.md` 与根 AGENTS.md"仓库结构与文件路由"节，产出 12 包职责一句话总览表（以现有文档为准，不自己重新发明）
2. 扫描 `packages/neuro-book/app/`：`pages/` 递归列出路由树（目录结构 → 路由路径）；`components/` 列组件清单（文件名 + 行数）
3. 复杂度统计：组件按行数排序取 Top 20；统计每个组件被 import 的次数；行数 > 300 的组件标注"重构候选"
4. 写 `docs/knowledge/模块地图.md`：① 12 包总览表 ② 页面/路由清单 ③ 组件复杂度 Top 表 + 大文件清单 ④ "从数据看出的观察"一节（哪些模块最重、路由结构暗示的 IA 问题）

## 产出文件

- `docs/knowledge/模块地图.md`（唯一交付物，自包含，中文）

## 验收标准

- 模块地图覆盖 12 个包 + 主应用全部页面路由 + 组件复杂度数据，数字可由 grep 复核
- 全程无源码/配置改动（`git status` 只显示新增的模块地图与任务书变更）

## 完成定义（AGENTS.md 硬标准，文档任务变体）

任务完成 = ① 产出文件已写入仓库工作区 ② 任务书交付报告已填（做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）③ 任务书内声明"待审"。BOARD 状态更新与 git 提交由前线在审查通过后执行。**仅在聊天中报告结果不算完成。**

## 参考材料

- `docs/modules/monorepo-boundaries.md`（包边界正文）
- 根 `AGENTS.md`"仓库结构与文件路由"节
- `docs/reference/NEXT-PLAN.md`（前任对复杂度病根的判断："在信息架构层，不在视觉层"）

## 疑问区（工程队填写）

1. 被引用数用 `grep -w` 组件名统计（Nuxt 自动导入无显式 import 语句，这是可复核的近似口径），同名文件若有别名导出可能有一两处偏差，供 IA 讨论足够，精确 import 图需语言服务。

## 交付报告（工程队填写）

**做了什么**：纯静态分析产出 `docs/knowledge/模块地图.md`：12 包职责总览（依据 monorepo-boundaries.md 与根 AGENTS.md，未重新发明）、18 个页面路由清单、组件复杂度（372 个 .vue、69,389 行；分布表、Top 20 明细含被引用数、72 个 >300 行重构候选）、server 子域对照表、"从数据看出的观察"6 条。

**为什么**：为任务 008/009 IA 重构提供可复核的数据底座。

**自测结果**：核心数字（页面数 18、组件 372 文件/69,389 行、>300 行 72 个、novel-ide 占 73%）均由 find/wc/grep 直接得出，文档末尾附复核命令；全程只读，git status 无源码/配置改动。

**遗留问题**：精确 import 依赖图未做（grep 口径的局限已登记疑问区）。

**状态**：**待审**。

**本窗口会话 ID**：sess_f7630ad3-9078-4e74-b751-945479569d6e
