# 任务010-开发者知识摘要

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3-Flash`，推理档位 **high**。若当前会话未在此配置，开工第一句先提醒用户切换。
- **模式**：**目标模式 + 完全访问**——读文档+读配置+归纳成单一文档，目标明确。
- **工作区**：`D:\MyProject\neuro-book` 主工作区（master 分支）直接执行，**无 worktree**（文档类任务豁免）。工程队不执行任何 git commit，文件写好后由前线统一提交。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务010-开发者知识摘要：按任务书蒸馏仓库架构与开发要点，产出 docs/knowledge/开发者知识摘要.md，完成后在任务书末尾写交付报告
  ```

## 目标

把仓库的架构与开发要点蒸馏成一份快速上手卡，让未来新开的 Agent 窗口（或新协作者）读一份文档就能掌握全貌，不必重新爬仓库。

## 开工前置

1. 读 `D:\MyProject\neuro-book\AGENTS.md` 的"本 Fork 工作规矩"节（尤其工程队分区规则）
2. 读本任务书全文，确认模式/模型已就位
3. 确认在 `D:\MyProject\neuro-book` 主工作区（master 分支）：`git rev-parse --show-toplevel` 应输出该路径

## 范围与边界

- 允许改动：仅新建 `docs/knowledge/开发者知识摘要.md`；本任务书疑问区与交付报告
- 禁止改动：任何源码、配置、master 分支提交、BOARD、其他任务书
- 所有命令信息从 `package.json` 摘录，**不批量执行构建/测试**（跑命令是任务005 的事）

## 执行步骤

1. 读根 `AGENTS.md`、`docs/modules/monorepo-boundaries.md`、`PROJECT-STATUS.md`，归纳 12 包结构与职责（每包：一句话职责 + 关键依赖 + 入口）
2. 逐包读 `package.json` 的 scripts，做成"构建/测试/开发命令速查表"；已实测过的命令标注实测状态（`bun install`、`nuxt:prepare`、`generate`、`dev`、`test -- server/config` 见任务004 执行记录；全量测试以任务005 结果为准），未实测的明确标"未验证，摘自 package.json"
3. 核心依赖版本表：从根与 `packages/neuro-book/package.json` 摘 Nuxt/Vue/Tailwind/reka-ui/TipTap/Prisma 等版本
4. 关键文档地图：`docs/specs/`、`docs/modules/`、`docs/standards/`、`docs/testing/`、`docs/reference/` 每目录一句话 + 何时读
5. 写 `docs/knowledge/开发者知识摘要.md`，定位：**新窗口 Agent 的第一份读料**（自包含、中文、路径可点）

## 产出文件

- `docs/knowledge/开发者知识摘要.md`（唯一交付物）

## 验收标准

- 覆盖 12 包、命令速查表、依赖版本表、文档地图四块，每条命令标注实测/未验证状态
- 事实与 `package.json`、现有文档一致（抽查可复核）

## 完成定义（AGENTS.md 硬标准，文档任务变体）

任务完成 = ① 产出文件已写入仓库工作区 ② 任务书交付报告已填（做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）③ 任务书内声明"待审"。BOARD 状态更新与 git 提交由前线在审查通过后执行。**仅在聊天中报告结果不算完成。**

## 参考材料

- 根 `AGENTS.md`（仓库结构与文件路由、文档真相源两节）
- `docs/modules/monorepo-boundaries.md`、`PROJECT-STATUS.md`
- `docs/tasks/任务004-环境验证.md` 执行记录（已实测命令）

## 疑问区（工程队填写）

1. 依赖版本表中 Vue/Tailwind/reka-ui 在根 package.json 无直接声明（Nuxt 生态传递依赖），表中记录的是 node_modules 实际解析版本并注明口径；若前线希望只记声明版本请指正。

## 交付报告（工程队填写）

**做了什么**：产出 `docs/knowledge/开发者知识摘要.md`：12 包结构与职责表（含包名/版本/测试框架）、命令速查表（环境/测试/构建三类，实测命令标⭐并注明任务004/005 出处，未验证命令明确标注）、核心依赖版本表（含"主应用 CSS 是 UnoCSS 非 Tailwind"的关键纠偏）、文档地图（何时读什么）、环境坑位速记 7 条（合并 AGENTS.md 与任务004/005/007 新发现）。

**为什么**：让新 Agent 窗口读一份文档即可上手，不必重新爬仓库。

**自测结果**：12 包、命令、版本均从各 package.json 与实测记录直接摘录，可抽查复核；未批量执行任何构建/测试（任务书边界）；全程只读+新建一个文档。

**遗留问题**：无（版本口径说明见疑问区）。

**状态**：**待审**。

**本窗口会话 ID**：sess_f7630ad3-9078-4e74-b751-945479569d6e
