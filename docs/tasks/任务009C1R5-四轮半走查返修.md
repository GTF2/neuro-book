# 任务009C1R5-四轮半走查返修

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3` + high（前线 5.3 串行，续 R1-R4 惯例）。
- **模式**：目标模式（`/goal 任务009C1R5：修复四轮半走查七件，按任务书验收交付`）+ 完全访问。
- **给用户的现成命令**：`/goal 任务009C1R5：完成七件修复，按 docs/tasks/任务009C1R5-四轮半走查返修.md 验收交付`
- **工作流子代理模型**：不适用（5.3 串行）。
- **对口技能与命令**：无（部件级修复直改）。

## 目标

四轮半走查暴露七个真问题（功能/数据 bug 为主）修复，达可过终审状态。**批次1 收缩纪律（本轮起）：只修功能/数据 bug；纯视觉样式封版归单D 施工规格一次做；形态数值以观感为准、颜色走主题 token。**

## 七件

1. 刻度条重做（R4 形态方向做反：点状+选中竖长条→应为鱼骨）：竖排**横向短条**、贴消息流一侧对齐；长度=两头短中间长纺锤包络×密度调制；当前/选中条=最长+最亮双强调，其余按长度深浅渐变；条高小均匀等距；预览 fixed 贴鼠标+近缘翻面（保留）；点格直跳断言全绿。
2. 纯思考轮"工作 1 秒"三态：真无数据→「本轮」；有数据→真实跨度（<2 秒不显数字归「本轮」，不再出现 1 秒）；异常大→陈旧钳制（已有）。新旧会话各验。
3. Workflow 不可回答条目可关闭：「当前等待输入状态不可回答」条目给关闭动作（折叠为紧凑条可恢复，数据不动）；归位与计数统一仍归批次 2。验收=卡死条目能点掉。
4. 未保存圆点挪到保存按钮右上角。
5. 保存中态轻量化：轻禁用+行内小 spinner（不整键灰）；完成 1.5s「已保存」轻提示淡出。
6. 面板统一收尾：世界书面板同步保存链（dirty 点亮+圆点+保存熄灭，与正文面板同款）；属性面板头部名字下方路径全类型去掉（移悬停提示）。
7. 注入条目样式统一：聚合块展开后 system reminder 与 CUSTOM:USER 两副面孔→统一灰小字行（灰字+类型前缀+单行摘要），差异仅前缀文字。

## 裁剪

Workflow 面板移除点外收起（用户明确不要）——只留徽标点击开/关+Esc。

## 范围与边界

- 允许改动：`packages/neuro-book/` app 侧相关组件+i18n 双语+测试+`scripts/r4-dom-check.mjs`+本任务书。
- 禁止：master、新分支、029 合并、纯视觉打磨（归单D）。

## 验收标准

- 七件全落地；全量对账=3367±本批新增精确到个位；typecheck 零错；涉模板改动 dev 编译器实证（坑③）；新增文案走 i18n；修复带断言；DOM 断言（更新后）全绿；完工回报+一句话对账说明+终审清单。

## 疑问区（工程队填写）

（空）

## 交付报告（工程队填写）

（空。格式：做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）

---

## 前线交付报告（2026-09-22 晚，5.3 施工·task-009c-r3 续修）

### 做了什么（七件+裁剪）

1. **刻度条鱼骨重做**（AgentSessionScaleBar.vue）：渲染形态从"竖条高度波浪"反转为**竖排横向短条**——每行一条、`justify-end` 右对齐贴消息流一侧伸出；长度=`spindleRatio`（两端 0.35→中间 1.0 对称纺锤包络）×密度调制（3 格滑动平均 weight，0.45+0.55 区间），下限 0.18；**当前视口/选中条=满宽+accent 满亮双强调**，其余按长度深浅渐变（opacity 0.45-0.9）；条高固定 3px、行高 h-3 等距；预览 fixed 贴鼠标+近缘翻面保留；点击命中（按格元素矩形）与拖动节流保留。颜色全走主题 token。
2. **块头时长三态**（AgentWorkBlock.vue）：`MIN_DISPLAYABLE_SECONDS=2`——终态跨度 <2 秒（含单条消息的 0 秒）归「本轮」，不再显示假"已工作 1 秒"；≥2 秒真实跨度；运行中动态计时+超 1 小时陈旧钳制保留（R4）。
3. **不可回答待处理卡可收起**（AgentUserInputPrompt+AgentComposer）：blocked 红条右侧加「收起」按钮→emit dismiss→Composer `pendingDismissed` 折叠为紧凑条（警示图标+「有待处理输入（当前不可回答），已收起」+「展开」恢复），**输入栏同步恢复可用**；会话切换或待处理清空自动复位；数据不动（pendingSessions 原样）。取舍披露：口令的「稍后提醒」未单做——无自动重弹机制，紧凑条常驻即为提醒位，避免造假功能。
4. **未保存圆点挪位**：两个保存按钮（表单+project.yaml）内 `absolute -right-1 -top-1` 右上角圆点（dirty 且非保存中显示）。
5. **保存中轻量化**：按钮保存中=轻禁用（`opacity-70` 替代整键灰 `disabled:opacity-45`）+行内小 spinner 保留；成功=`savedFlash` 行内「已保存」绿字 1.5s 后 500ms 淡出（替代原 useNotification 大 toast，两处 toast 均撤）。
6. **面板统一收尾**：①WorkspaceLorebookDetailPanel 保存链与正文面板同款——isDirty 对称管道已有，补 `hasUnsavedBody` 并联进 `saveDisabled`、右上角圆点、轻量保存、行内已保存、空转守卫（`&& !store.hasUnsavedFileChanges`）；②FileDetailPanel 头部名字下方路径行删除，路径移到头部悬停 title（Lorebook/Character 头部本就无路径行）。
7. **注入条目统一**（AgentChatFlow.vue）：聚合块展开体与单条注入行展开体都不再嵌完整 AgentTextBubble——统一灰小字行（类型前缀+单行 60 字摘要+全文进 title；error 态红字），前缀三键落双语（系统/提醒/错误；有 systemLabel 时用数据自带标签如 CUSTOM:USER）。

**裁剪**：Workflow 面板点外收起移除（onClickOutside 整体删除，含 badgeRef）——关闭只留徽标点击开/关+Esc 两路；契约测试同步更新。

### 自测结果

- **全量对账：3370 过 = 3367+3 精确吻合**（新增 AgentWorkBlock 契约 4 用例、ScaleBar 测试两用例并为一件1 用例 −1）；17 败 REPOSITORY_ROOT 同族零增（历轮报错原文已核）。
- **typecheck**：REAL_EXIT=0、error TS 计 0。
- **dev 编译器实证（坑③）**：全新 dev 实例启动 200、SFC 编译零错（涉模板大改 ScaleBar/ChatFlow/Composer/UserInputPrompt/FileDetailPanel/LorebookPanel）。
- **DOM 断言 8/8 全绿**（全新 dev+desktop stub+欢迎页开面板全链）：鱼骨形态实测 14 条横向短条 minW 8px/maxW 32px（有长短差异）；**「已工作 1 秒」形态消失**（原 1 秒轮现显示「本轮」）；点格直滚端到端 8378→1465 保持；气泡 331px≤480；滚动条隐藏。
- **新旧会话各验**：旧=戈之书 64 号（8/8 断言载体）；新=《新小说1》会话 1（真实拆书会话）——时长三态共存实测（「已工作 1440 分 0 秒」+「本轮」、无 1 秒）、徽标「0 个流程等应答」=数据 waiting 0、0 页面错误。

### 一句话对账说明（回答追问）

R4 的"3362 基线比 R3 交付 3353 多 9"**全部是 R4 本批新增测试用例**（件1 时长 1+件2 分组 2+BUG030 双保险 2+ScaleBar 重写 3+件8 契约 1），逐文件可查、非 worker 吞批次（本轮 3370=3367+3 同口径续验）。

### 遗留

件3「稍后提醒」以紧凑常驻条承担（取舍已披露）；红框条目彻底归位与计数统一仍归批次 2；纯视觉打磨封版归单D（本轮起批次1 收缩纪律生效）。

### 终审清单（建议顺序）

1. **刻度条**：右缘一列横向短条右对齐、两头短中间长的纺锤轮廓（密度厚处更长）；当前视口那条最长最亮；点格直跳；悬浮预览贴鼠标。
2. **块头时长**：纯思考短轮显示「本轮」（不再有"已工作 1 秒"）；长轮真实跨度；运行中每秒跳动。
3. **待处理卡**：遇到「待处理 · 当前不可回答」红条→点「收起」→卡折叠成一行紧凑条、输入栏恢复可用→点「展开」可还原。
4. **属性面板（世界书条目+正文 index.md+project.yaml 三类）**：改字段→保存按钮右上角出现橙点→保存中按钮轻灰+小 spinner→完成显示绿色「已保存」1.5 秒淡出；头部名字下方无路径行（悬停名字可见路径）。
5. **注入聚合块**：展开「系统上下文注入 ×N」→每条=灰小字一行（前缀+摘要），报错条红字；单条注入展开同款。
6. **Workflow 面板**：徽标点击开/再点关；Esc 关；（点外部不再收起——按你的要求移除）。

本窗口会话 ID：sess_31b7a638-ff76-402e-8e23-ba9f57ff2268

---

## R5b 用户四条反馈返修（同日追加）

1. **刻度条澄清版重做**（用户原意：正常态等宽，波浪是 hover 交互）：静态全部等宽 BASE_RATIO=0.55；当前视口/选中条满宽 ACTIVE_RATIO=1 最长最亮；鼠标划过=距离衰减波浪（hover 格 0.95、半径 4 格线性衰减、远处不变），波浪随鼠标移动。删纺锤/密度长度逻辑。实测：静态非选中条全 18px 等宽、hover 格 18→30px。
2. **不可回答卡加「放弃」**：blocked 红条右侧「放弃」按钮=emit cancel 走既有 cancelPendingUserInput 链**真中断该等待**（不再永远挂着）；「收起」保留（折叠可恢复）。
3. **保存提示改右下角小气泡**（用户反馈行内闪现体感差）：撤 savedFlash 行内提示，保存成功=`useNotification().success` position bottom-right + duration 1.8s 自动消失；两面板（正文+世界书）同款。
4. **注入统一补漏**（用户截图实锤）：会话**开头**收拢行的展开体漏改（R5 只改了轮内两处）——补齐为同款灰小字行；顺带摘要剥掉 `<system-reminder>` 类裸 XML 标签头。实测《新小说1》会话 1 开头聚合块展开=5 行灰小字（System Reminder/Run Error 前缀、error 行红字）。

验收：全量 3370 持平零回归（无新增用例，断言内容更新）、typecheck 零错、dev 编译零错、DOM 断言 9/9（新增静态等宽+hover 波浪两条）。

---

## R5c 用户五条反馈返修（同日再追加，本轮）

用户反馈原文要点：①右下角气泡没看见，建议用系统自带小弹窗；②保存时 Agent 面板输入框上面还闪"保存文件"状态条，感觉没必要显示；③刻度条太宽、整体太宽；④鼠标放刻度上梗概小气泡去哪了；⑤hover 上下滑拉时出现灰色长条+蓝色选中条两个条，应该只有一条跟着鼠标跑。

### 根因与修复

1. **"闪一下的状态条"=AgentWorkspaceChanges 检查条**（非保存提示）：每次保存文件触发 inbox 刷新，`visible` 含 `loading` 条件使"正在检查变更"条闪现即逝。修复=loading 退出显示条件（`groups>0 || error` 才出现），"正在检查"分支删除（i18n checking 键暂留避免扩大 029 合并冲突面，收官统一清）。
2. **气泡没看见=blur 自动保存路径静默**（用户不走保存按钮时从不弹）：FileDetailPanel 的 `saveDraft` 原带 `options?.notify` 门控，`@blur` 调用无参=静默。修复=删 notify 参数，所有保存路径（按钮/blur）统一 `flashSaved()` 右下角气泡——与 Lorebook 面板既有行为对齐（那边本就无条件弹）。通知系统本身完好（NotificationViewport 全局挂载+z-9800+bottom-right 实现齐全，AgentWorkspaceChanges 接受变更等场景一直在用）。
3. **刻度条缩窄**：容器 w-9(36px)→w-5(20px)，基线条 9px、满宽 16px。
4. **单条跟随**（用户："原来那一条跟着鼠标跑"）：删灰色 hover 条（text-secondary 满宽 0.95），`focusIndex = hover ?? active` 唯一满宽位——hover 时满宽蓝条跟鼠标走（邻格距离波浪衰减），移出回选中格；颜色两档（accent/border-strong）。
5. **静态尾迹修复**（无头断言逮住）：R5c 初版静态时选中格的衰减尾迹也在渲染（9×10+11/12/14/16）——违反"正常态全等宽"（R5b 用户原话）。修复=无 hover 时非选中格一律 BASE。
6. **拖动时梗概卡消失=pointer capture 劫持**：pointerdown 后 `setPointerCapture` 使后续 pointer 事件重定向到 track，格子的 pointerenter 不再触发。修复=`updateHoverFromPointer` 唯一写入口，track 的 pointerdown/pointermove 也维护 hover（拖动全程波浪+预览卡跟随）；track 加 pointerleave 兜底清 hover。

### 验收

- **全量对账：3373 过 = 3370+3 精确吻合**（AgentWorkspaceChanges 契约 1+ScaleBar 用例净 1+frontmatter blur 契约 1）；17 败 REPOSITORY_ROOT 同族零增（历轮已核）。
- **typecheck 零错**；dev 编译零错（中途重启过 dev server：旧实例 vite watcher 失效导致 IAB 拿旧模块，磁盘真相以带时间戳强制 re-transform fetch 验证）。
- **DOM 断言 8/8**（r5c-dom-check.mjs，新 dev server）：容器 20px、静态等宽 uniq=[9,16]（满宽恰 1 条）、hover 单条跟随（唯一满宽=hover 格+离开回落）、track 侧拖动预览卡、无 checking 闪现。
- **IAB 真交互走查**（用户指定 control-browser 技能）：真鼠标悬停=预览卡贴鼠标出现（"好，②+A，齐活…"）；点格 2 直跳 7768→1294；hover 格内联 width 切 100%+accent 类（IAB 后台窗口 CSS transition 冻结致 rect 读数滞后，属观测假象，功能真实生效——以活跃渲染的 playwright 断言为准）。保存气泡 UI 链在无头/IAB 均不可达（新建文件后详情表单未随选择态打开），由契约测试锁 blur 路径+通知系统既有在用证据覆盖，视觉留用户复验。
- **形态自查**（用户指定 impeccable quieter/distill 参考）：颜色两档收敛（删灰条=减少颜色种类）、装饰性动效删除（checking 闪现）、功能性波浪保留短距 150ms 缓出无弹跳、尺度收敛 36→20px。符合"边框能删则删、间距代框、动效短距缓出禁弹跳"口径。

### 复验建议（四处）

1. 刻度条：正常全等宽窄条；鼠标划过=一条蓝色满宽条跟着鼠标跑+邻近稍短；移开回到当前位置满宽。
2. 保存任意文件（编辑后点别处触发自动保存，或点保存按钮）：右下角小气泡"已保存"1.8 秒消失；Agent 输入框上方不再闪"正在检查"条。
3. 按住拖动刻度条：梗概预览卡全程跟着鼠标。
4. 世界书条目编辑失焦：同样右下角气泡（该路径 R5b 起就有，本轮无改动）。

---

## R5c-d 预览卡两连修复（用户实锤"放上去什么都不显示"→修后实锤"变黄色"）

- **根因一（顶出视口）**：index.vue 面板容器 `contain-layout-paint`（contain: layout paint）创建包含块，预览卡 fixed 退化为相对面板定位——实测 style.left=1354 而渲染 rect 左缘=2555（视口 1600 外），偏移恰为一个面板宽。**自 R4 件3② 起卡就一直在屏幕外**，历轮验证只读 style 未读 rect 故漏检；R5b 用户"梗概气泡去哪了"的真根因其实是它（当时误判为仅拖动 capture 一因，两因并存）。
- **根因二（变黄）**：第一版修复 Teleport to body 后用户实锤卡变黄色——脱离 `.novel-ide-theme` 宿主使 CSS 变量回落 :root 的 sepia（NotificationViewport 既有同族坑，其源码注释有案）。
- **终修**：Teleport 到 `closest(".novel-ide-theme")` 宿主（contain 容器之外+主题变量作用域之内；ReferencePlainTextEditor/TipTapMarkdownEditor 的 popoverTeleportTarget 既有先例照抄）+ z-50（高于面板层、低于通知 9800）。
- **验收**：真鼠标 page.mouse.move 实测 rect=style.left 且 inViewport；卡底色=宿主主题色（白 rgb(255,255,255)）非 sepia（#fdf6e3）；ScaleBar 测试 8/8+DOM 断言 8/8+typecheck 零错。专项脚本 scripts/r5c-hover-check.mjs 留档。
- **教训入册**：fixed 弹层验证必须读 getBoundingClientRect 而非 style；面板内一切 fixed 弹层统一 Teleport 到 .novel-ide-theme 宿主（两约束：contain:paint 与主题变量作用域）。

---

## R5e 复验裁定（fa27e3bf，同日）

用户复验回执：刻度条全部通过（窄条/单条跟随/拖动梗概卡）；无闪条 ✓；**右下角保存气泡从没见过→裁定删除**。删除优于修补：用户从没见过它的深层原因=真实正文编辑链不经过两个属性面板的保存函数（R5c 的"路径统一"覆盖不到主编辑器保存），且保存反馈已有按钮 spinner+圆点熄灭承担（R4 件8 验收过的形态）——弱信息提示属噪音，按收缩纪律撤。

落地：两面板撤通知函数+调用+import；双语 `ide.workspace.common.saved` 死键清除（文件中部删除，不影响 029 尾部追加合并）；契约测试改锁"零通知调用+保存链保留"。i18n 死键清理顺带覆盖 ponytail 审计发现 4 的一角（其余死键组留 TOP1 清道夫小单）。

---

## R5f 用户三反馈返修（a05764ee，同日深夜）

用户截图（《新小说1》拆书会话真机）三反馈：①create_agent×5/invoke_agent×5 散在消息流没收一起（轮展开态也要聚合成一行）②收起轮块后子线程相关行还挂着 ③blocked 红条「放弃」按钮点了没反应。

### 根因与修复

1. **①+②同根**：create/invoke/detach_agent 在 R2 件6 立的「不折叠白名单」（代理生命周期类必须始终可见）——用户裁定覆盖旧规（拆书一次 5-10 个属过程噪音）。移出白名单+新增 agent 工作类别（create=创建/invoke=派发/detach=分离子代理，双语键+块头摘要类别）。foldable 后轮内 ≥3 同名全终态自动聚合（「创建子代理 ×10」实测），收起态自动进过程行过滤（②零额外改动）。真机会话实证：拆书会话一轮 10 个创建+10 个派发各聚一行。
2. **③真凶=僵尸 pending 数据（非按钮坏）**：实测 API——拆书会话 status=**interrupted** 且**全库 waiting=0**，但恢复 payload 仍带 3 条 pending 灌进面板=「待处理 1/3·当前不可回答」卡；点放弃时 `cancelPendingUserInput` 首行 `!activeInteraction.canAbort`（interrupted 无 abort 能力）**静默 return**。修复两层：前端加门=**仅 summary.status==='waiting' 展示 pending 卡**（与 canResolve/canAbort policy 对齐，恢复+live state 两处赋值同门）；放弃按钮补 `:disabled="submitting || !canAbort"` 与底部按钮同守卫（不可中断时禁用可见，不再假可点）。**后端僵尸 pendingApprovals GC 未动**——登记归批次 2 G1 统一待办库根治（同根因）。
3. 顺带：dev server 长跑后 HMR 重启 server 模块触发 lease 自冲突（全站 500"正被另一实例使用"owner=自己）——rm .nuxt/dev 重启解决，坑已入册。

### 验收

- R5f 无头断言 5/5（scripts/r5f-dom-check.mjs：进拆书项目→开面板→无僵尸卡→加载历史→「创建子代理 ×10/派发子代理 ×10」聚合行→收起消失）。
- 戈之书回归 8/8（刻度条/预览卡/闪条无回归）；agent 目录 303 测试全绿（useAgentSession 3 处 fixture 补 waiting 态+新增僵尸过滤用例；chat-work-blocks 46 工具断言改 39+7）；typecheck 零错。

### 复验点（拆书会话）

1. 轮展开：5 个创建/派发各聚一行「创建子代理 ×N」，点行可展开全部子卡。
2. 轮收起：子线程行跟着收进去。
3. 「待处理·当前不可回答」红条整卡消失（会话已中断，卡不该在）；同款僵尸卡不会再出现。

---

## R5g 聚合规则扩展（e4b6ebe3，2026-09-23 凌晨）

用户看了 R5f 真机效果后的追加裁定：聚合不该只优待子代理调用——**其他工具（read/bash/write…）连着 2 个、3 个也该收**；中间隔了思考/正文就断开单放；思考模式本身不收（本就不参与聚合）。

落地：TOOL_GROUP_MIN 3→2；收集条件从「同名连续」改「任意连续 foldable 工具」；toolGroup 加 kinds 分类计数——单一同名保持「中文名 ×N」形态（创建子代理 ×5 不变），混合组渲染人话「查阅资料 1 + 查询数据库 1」（复用块头摘要 CHAT_WORK_BLOCK_META 既有措辞）；失败标红/展开子行/收起过滤全链不变。

真会话实测（拆书会话）：混合行 5 种组合渲染（「修改文件 10 + 世界引擎 1 + 查阅资料 1」「执行命令 1 + 修改文件 1」「查阅资料 1 + 查询数据库 1」「查询数据库 1 + 查阅资料 1·1 失败」）、散卡清零、创建/派发子代理聚合行保持。agent 303 测试全绿（新增异名混合+思考隔断用例）、typecheck 零错。

---

## R5h 过程行字重统一（2026-09-23 凌晨）

用户截图实锤：过程行家族字重不统一——原版单条工具行（write/bash 等宽英文显粗）、我加的聚合行/注入行纯中文无字重加成「又细又不好辨识」。

修复：过程行家族四处中文标签统一 `font-medium`（注入聚合按钮/工具聚合按钮两分支/单条注入按钮/会话开头收拢行按钮），颜色灰系与 hover 不动；原版工具行的 mono 英文名保留（功能性等宽，不动原版）。实测子代理聚合行/注入行 fontWeight 500 生效。

R5g+R5h 一并验证：agent 目录 303 测试全绿、typecheck 零错。

## R5h 续：等宽字体全撤（用户二次实锤）

R5h 一轮只统一了字重没动字体——用户指出「写文件 ×4/改文件 ×3」还是不对，点名以「系统上下文注入」和思考行的**普通 UI 字体**为基准，"但凡不是这个字体的都改"。根因即用户第一轮就说"有点粗"的粗源=工具行英文名的 font-mono 等宽字体。

修复：AgentToolNode 过程行两处（收起态+展开态头）工具名去 font-mono 改 font-medium；聚合行同名分支同步去 mono。展开体内的 Arguments/Result 代码块 mono 保留（代码内容展示非过程行标签）。实测工具名 font-family 已入普通 UI 字体链、weight 500。agent 303 测试+typecheck 零错。

## R5h 续二：聚合行失败染色收窄（用户三次反馈）

用户截图实锤：「查询数据库 1 + 查阅资料 1」主体跟着失败计数整行染红——读成"合并的活全失败了"。修复：聚合行主体保持灰，只有「·N 失败」后缀染红（R4 件2 原版同名聚合行同款逻辑一并收窄）。测试 14 绿+typecheck 零错。

## R5i 失败轮红刻度（用户追加需求，2026-09-23）

用户需求：失败轮在右侧刻度条上要显示**红刻度**，鼠标掠过或点到它时照旧变蓝（交互焦点让位）。

落地：scaleSegments 构建时聚合格内任一轮 hasFailure → segment.failed；刻度条颜色三档=focus 蓝（accent）/失败格红（status-danger）/其余灰（border-strong），hover/选中 focus 逻辑不变（失败格 hover 即蓝）。真机实测（拆书会话）：加载历史后 7 格中 2 格红 rgb(220,38,38)，hover 红格变蓝 rgb(59,130,246)。agent 303 测试+typecheck 零错。

## R5j 聚合行补前置图标（用户追加，2026-09-23）

用户实锤：聚合行比其他过程行差一个图标位，文字起点不齐显怪。修复：聚合行 chevron 后加类别图标，**按组内主导类别取 CHAT_WORK_BLOCK_META 图标**（查阅=书/命令=终端/数据库=库/子代理=机器人…与块头摘要同源同风格）——内容不同图标随之变化，风格天然统一。真机实测三行（书本/数据库×2）渲染正确。agent 303 测试+typecheck 零错。

## R5k 红格悬浮卡含失败详情（用户追加，2026-09-23）

用户需求：红刻度格的悬浮梗概要写清有什么异常失败。落地三段链：buildRound 收集 failedToolNames（去重保序）→ scaleSegments 聚合格内失败工具 → 悬浮卡 summary 下方红字行「该段有 {count} 处失败：改文件、命令」（工具名走中文短名+i18n 双语键 scaleFailureNote，与红刻度呼应）。真机实测：悬浮卡文本「…该段有 2 处失败：改文件、命令」红行渲染。agent 303 测试+typecheck 零错。
