# 任务009C1R4-四轮走查返修

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3` + high（前线 5.3 巨石串行，续 R1-R3 惯例）。
- **模式**：目标模式（口令 `/goal 任务009C1R4：四轮走查返修+BUG030，按任务书验收标准交付`）+ 完全访问。
- **给用户的现成命令**：`/goal 任务009C1R4：完成四轮走查返修+BUG030 修复，按 docs/tasks/任务009C1R4-四轮走查返修.md 验收交付`
- **工作流子代理模型**：本任务不适用（5.3 串行）。
- **对口技能与命令**：无（部件级修复，直改）。

## 目标

R3 四轮走查判读未全过：BUG030（P1 数据安全）+十一件部件返修，全部在 `task-009c-r3` 分支续修（禁新开分支、禁合 master、029 合并继续挂起）。修完批次 1 与预生产包同待收官。

## BUG030（P1，先调查后修复，结论已出见下）

用户在文件树编辑某书 Project.yaml 删除 title 值后切书架，该书消失。

### 调查结论（2026-09-22，全链实读代码+用户数据实测）

- **磁盘数据完好**：`xin-xiao-shuo-3/` 目录 56 文件全在（含 agents/ 正文体系），仅列表不显示。`Project.yaml` 现状 `title:` 为空值（YAML null）。
- **消失机制（四环）**：①`project-lifecycle-manifest.ts:582-585` readManifest 把 `typeof title !== "string"` 归类 `normalizable`；②`inspectionFromRead`（:508-534）映射为 `status: "repairable"`（非 unsafe）；③`project-lifecycle.ts scan()`（:2185-2191）repairable 目录进 `candidates` 不进 `projects`；④`server/api/projects/index.get.ts:29-32` 列表响应**只返回 projects**，candidates 无任何 API 暴露、前端 ProjectPickerScreen 零消费——书彻底不可见。
- **后端已有自愈设计但未接线**：repairable 附带 `proposedManifest`（`normalizedManifest` :609-620，title 非法时兜底=项目目录名）+`ensure` 归一化写回能力（:208，仅 import/metadata-update 路径调用），发现扫描不消费。
- **既有拦截只护 API 侧**：`project-lifecycle.ts:958-965` updateMetadata 对空 title 抛 PROJECT_VALIDATION_FAILED——但用户走文件树直改 yaml（workspace-files 保存链路）绕过了它。
- **用户当下脱困**：手动把该书 `Project.yaml` 的 title 填回任意值（如 `title: 新小说3`）保存，书即回书架。

### 修复要求（双保险）

1. 书架条目 title 为空回退显示项目目录名（接线后端既有 proposedManifest 兜底，repairable 书以降级条目回列表）。
2. Project.yaml 保存链路对必填字段空值拦截+确认弹层（与件 9 的 project.yaml 表单化联动：C 类表单化后 title 必填校验）。

## R4 十一件（全文以用户口令为准，本节为执行索引）

1. 块头：纯思考轮时长（轮内首末消息时间戳，不依赖工具事件）+块头行内元素垂直居中同基线。
2. 展开区：①块头下所有展开行统一单层左缩进+左竖线（禁分级缩进，归属靠竖线连续+排序）；②连续同类工具≥3 条全终态聚合「{中文名} ×N」（含失败标红 ·M 失败，点击展开，进行中不聚合）；③连续系统注入聚合「系统上下文注入 ×N」（含异常「×5 ·2 异常」标记，展开逐条状态）。
3. 刻度条五修：①点格跳转失效回归（恢复 R2 的 8259→4774 点击直滚断言测试并全绿）②悬浮预览 fixed 贴鼠标跟随（偏移 12,12，近缘翻面）③格高两轨合一：非选中格=平滑波浪渐变（禁达选中格高），选中/当前视口格=强调格+高亮 ④整体与单格加宽（格宽≥8px 量级）⑤隐藏消息区原生滚动条（留回滚开关）。
4. 工具条：右侧档位/展开/编辑按钮与左侧行同一垂直中线；模型触发器宽度收缩 width:fit-content（面板 min-w-max 勿回退）。
5. 气泡上限 36rem→30rem(480px)；验证代码块/表格 480px 横向滚动不撑破。
6. Workflow 徽标改开关切换（点击开/再点收起，保留点外收起）。
7. 确认项维持现状：中途注入入轮不切轮已过审；注入归并并入件 2③不重复施工。
8. 保存按钮病灶：属性面板顶部保存按钮恒灰+无 dirty 提示——排查 isDirty 传导链，修复验收=输入→按钮点亮+未保存圆点→保存→熄灭全链；预研三候选（A/B/C）一并闭环；若证实正文自动保存则改移除禁用按钮+头部「已自动保存」（择一披露）。自动保存默认行为归单D 需求池本批不做。
9. 属性面板三形态普查统一：A 世界书系=基准；B 正文/手册/智能体系（028 表单化旧风格）统一到 A（头部图标+标签 chips+字段分组+字数统计移顶部）；C 极简系（project.yaml 等）表单化（title/name 字段+必填校验联动 BUG030）；先普查出三形态归属清单回报再统一；纯杂项最低统一=路径+可编辑+保存链一致；差距超样式级先回报再动工。目标字数显示归单D 本批不做。
10. Workflow 应答面板真机 bug（《新小说1》拆书实战首爆）：①先勘察卡死条目渲染位置（面板内 or 独立浮层/聊天流卡）——面板外则先归位②徽标计数与面板队列计数同源③不可回答条目不计入或明确呈现"需处理但当前不可回答"态④三路关闭全通（徽标开关/点外/Esc）⑤回报用户脱困路径⑥用用户真实会话验证正向链路。
11. 中文化漏网：文件树状态绿点 tooltip「Active」→「已生效」（与世界书面板同译法）走 i18n；同位置其他英文裸奔一并落键；补登词表执行方案三态表。

## 范围与边界

- 允许改动：`packages/neuro-book/` 内 app/server/shared 相关组件+`app/i18n/locales/` 双语+相关测试文件+`docs/tasks/` 本任务书。
- 禁止改动：master（施工全程 task-009c-r3）；`packages/nb-ui/`（红线）；底栏结构（单D 宪法 4 归 mock 流程）；待办统一体系架构（单D 收编）；BOARD 由前线独家维护。
- 架构注：本批只修不重构；「流程应答」独立面板为批次 1 过渡形态，单D 施工时收编驾驶舱。

## 验收标准

- 全量 vitest 对账：基线 3353（R3 后实数）±本批改动，逐项报实数；typecheck 零错。
- 涉模板结构改动必须 dev 编译器实证（坑③：vue-tsc 不报 SFC 结构错）。
- 新增文案走 i18n 落键（双语）；件 3① 恢复点格直滚断言测试；件 2 聚合/件 8 dirty 链/件 10 应答计数带单测。
- 偏差必披露；完工回报参谋部+出第四轮走查清单。

## 疑问区（工程队填写）

（空）

## 交付报告（工程队填写）

（空。格式：做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）

---

## 前线交付报告（2026-09-22，5.3 施工·task-009c-r3 续修）

### 做了什么

**BUG030（P1 数据安全）——调查+双保险修复+用户书已救回**

- 调查结论（全链实读+用户数据实测）：磁盘完好（`xin-xiao-shu-3` 56 文件全在，仅列表不显示；`Project.yaml` 的 `title:` 被清空成 YAML null）。消失机制四环：`project-lifecycle-manifest.ts:582` 把 title 非字符串归 `normalizable` → `inspectionFromRead` 折叠成 `repairable` → `scan()` 把 repairable 目录归 `candidates` 不进 `projects` → 列表 API 只返回 projects 且 candidates 无任何暴露=书彻底不可见。**后端原本就有自愈设计但被列表层断链**：repairable 附带 `proposedManifest`（title 兜底=目录名）+`prepareOpen` 打开时自动 ensure 归一化写回。
- 双保险修复：①`project-lifecycle.ts` scan()——字段级 repairable（issues 全为 FIELD_INVALID，即 yaml 存在可解析仅字段坏）降级入 projects（title 兜底目录名），用户点开书时 prepareOpen 的 ensure 自动把磁盘 yaml 修好；missing/corrupt 仍走 candidates（那类目录不能代表一本书）。②`write.put.ts` 新增 `assertProjectManifestTitle`——保存 project.yaml 时 title 空/缺失/非字符串直接 400 拒绝落盘（人话错误文案）。
- 测试：lifecycle 新增 BUG030 专项用例（空 title→列表含该书 title=目录名+ensure 自愈+正文完好）+两个既有断言随语义更新（93 过 1 skip）；write.put 新增 400 拦截用例（3 过）。
- **用户书已救回**：已把 `xin-xiao-shu-3/Project.yaml` 的 title 回填为 `xin-xiao-shuo-3`（磁盘现场修复），书架 headless 实证已回到列表（3 本齐）。

**R4 十一件逐条**

1. 块头时长：`buildRound` 改为首末消息时间戳——单条消息轮 durationMs=0（非 null），全缺时间戳才 null 回退「本轮」；`ChatRoundItem` 新增 `startedAtMs`；AgentWorkBlock 运行中用每秒 tick 的动态时钟（`nowTick`）算「已工作 X 秒」——纯思考轮也跳动。块头行统一 `items-center`+`leading-4` 同基线。
2. 展开区：①单层对齐——思考行行头去 pl-4 平齐块头、展开体统一 `ml-3 border-l-2 pl-3`（与工具行/注入行同款）；工具子行本就无二次缩进。②连续同名工具 ≥3 且全终态聚合「{中文名} ×N」（新增 `buildRoundEntries` 数据层纯函数，`TOOL_GROUP_MIN=3`；失败标红 `·M 失败`；进行中不聚合；点击展开全部子行）。③连续 system ≥2 聚合「系统上下文注入 ×N」（异常计数 `·M 异常` 红标+警告图标；展开逐条可见；单条保留 R3 已过审形态）。新增分组单测 2 用例。
3. 刻度条五修：①**回归根因定位**=R3 件4 的 `justify-center`（格少集中中段）使坐标均分换算错位——`gridIndexFromPointer` 重写为按格元素 `getBoundingClientRect` 直接命中（越界收敛两端），点击/拖动统一走它。②预览卡改 `fixed` 贴鼠标（pointermove 跟随+偏移 12,12+近屏幕右/下缘自动翻面），删旧的随格 translateY。③格高两轨合一——非选中格=6-14px 平滑波浪（3 格滑动平均禁孤立突起，颜色深浅 0.45-0.85 辅助），选中/视口格=20px 强调格+accent 色（非选中格上限 14px 恒低于选中格）。④容器 w-6→w-9、格条 w-2（8px）。⑤消息滚动容器加 `chat-scroll-hidden`（scrollbar-width:none），回滚开关=style 注释指明删规则即恢复。ScaleBar 测试重写为 8 用例（含命中回归锁）。
4. 工具条：四个图标按钮（图片/展开/模式/发送）统一 `h-7 w-7` 定高居中（与芯片 h-7 同高消除 2px 基线差）；模型触发器 `w-[200px]`→`w-fit`（min/max 保留；面板 min-w-max 未回退）。
5. 气泡：`max-w-[36rem]`→`max-w-[30rem]`（480px）。
6. Workflow 徽标开关：代码本就是 toggle，真凶=徽标不在面板元素内导致 onClickOutside 先关再 toggle 净效果恒开——徽标加 `badgeRef` 进 `ignore` 列表；补面板容器 Esc+徽标 Esc。三路关闭全通。
7. 确认项维持现状，未施工（按口令）。
8. 保存按钮：根因=watch 随正文重建 draft 使对称管道吸收正文差异（面板自身链路是通的，用户撞的是正文不经面板）。修复走路线 A——`hasUnsavedBody`（store.hasUnsavedFileChanges）并联进按钮禁用源与未保存圆点；saveDraft 提前返回条件补 `&& !store.hasUnsavedFileChanges`（防空转）。披露：勘察证实正文**不是**自动保存而是失焦保存（blur→save-request→saveCurrentWorkspaceFile，index.vue:2620），故不走「移除按钮+已自动保存」路线 B；自动保存默认行为归单D 需求池未做。
9. 属性面板普查+统一：①三形态归属清单——**A 基准**=WorkspaceLorebookDetailPanel（世界书非角色条目）+WorkspaceCharacterDetailPanel（角色）；**B**=WorkspaceFileDetailPanel 的 isContentIndexFile 分支（manuscript 卷/章 index.md、agents 手册、智能体系 index.md）；**C**=同面板非 index 分支（project.yaml、普通 md、杂项）。②B 对齐 A：头部加图标徽章（h-6 w-6 rounded-lg）+font-serif 标题；actions 加刷新按钮（新增 refresh emit+宿主 WorkspaceFilePanel 绑 @refresh）；信息卡 rounded-md→rounded-2xl+bg-input/20（A 分组口径）；manuscript 统计卡从表单尾移到表单头（打开即见）；TagInput 占位统一「添加后回车」口径。③C 类 project.yaml 表单化：title 必填（空=红框+提示+禁保存，文案联动 BUG030）+name（目录名只读）+summary；行级替换写回保序保留 cover 等未知字段；纯杂项最低统一=路径+可编辑状态+全局保存链（编辑器失焦保存），已满足未改。④差距未超样式级（B 分支 028 已表单化打底，本批纯视觉统一）。
10. Workflow 应答面板：①勘察结论——用户红框卡死条目=**AgentUserInputPrompt**（AgentComposer.vue:535 起整体替换输入栏的阻塞型应答卡，「等待你的处理 1/3」=它的 pendingTitle），不在流程应答面板内；**归位=归批次 2 统一应答协议**（H2 参谋部已裁定两套机制不互替+架构注本批不重构，本批不动）。②计数不同源同理=两套机制各自的计数（徽标=workflow waitingJobs；1/3=阻塞型 asks），面板内计数与徽标本就同源（waitingCount）；同源化归批次 2。③不可回答态：AgentUserInputPrompt 头部在 `!canResolve` 时改显「待处理 · 当前不可回答」（新键 pendingBlockedTitle），原因保留在既有红条——不再与队列计数打架。④三路关闭全通（见件6）。⑤**用户当下脱困路径：刷新页面即可关掉卡死面板**（pendingPanelOpen 非持久化，刷新即收起；阻塞型应答卡若会话仍在 waiting 会重新出现，属功能而非残留）。⑥真实会话验证：无头环境卡「正在打开作品 6/6 步」（018/021/R3 同族环境特性），正向链路验证移交用户真机走查（清单第 5 条），组件级计数/开合逻辑有测试锁。
11. 中文化：文件树状态点 tooltip `:title="node.status"` 裸英文改用既有 `statusLabel`（i18n，Active=已生效与世界书面板同译法）；文件树其余 tooltip 全量核查无裸英文残留。**偏差披露**：词表执行方案三态表补登（⛏→✅）受阻——该文件在 task-prefab 分支（029 合并挂起中），本批不动挂起分支，登记为 029 合并后顺手补一笔。

### 自测结果

- **全量对账**：vitest 3362 过/17 败/3 跳 = 基线 3353+9 新用例精确吻合（chat-work-blocks +2 分组用例+1 件1 用例、write.put +1、lifecycle +1、ScaleBar 5→8=+3、frontmatter +1）；17 败与基线同族（REPOSITORY_ROOT 环境性，数量零增）。
- **typecheck**：REAL_EXIT=0、error TS 计 0（中途 3 个新代码类型错已修：测试 error 字段类型/while 窄化/projectYamlDirty 窄化）。
- **dev 编译器实证（坑③）**：dev server 起立 200、SFC 编译零错（本次涉模板大改的 ChatFlow/ScaleBar/TextBubble/FileDetailPanel/PendingPanel 均无编译错）。
- **DOM 断言**：无头 playwright 卡「正在打开作品 6/6 步」环境特性（018/021/R3 同族），全链 DOM 断言熔断移交用户真机走查；意外拿到 BUG030 书架现场实证（xin-xiao-shuo-3 回列表）。组件级断言全绿（ScaleBar 8/8 含点格命中回归锁、分组 2/2、件8 契约 1/1）。
- **BUG030 现场验证**：书架列表 3 本齐（含事故书）。

### 遗留问题

1. 件10①②红框条目归位与两套应答计数同源化——归批次 2 统一应答协议（H2 既定裁定，BOARD 已登记）。
2. 件11 词表执行方案三态表补登——待 029 合并后补（见偏差披露）。
3. 件8 的「自动保存作为默认行为」——归单D 需求池（本批未做，走查确认失焦保存+手动保存双入口可接受）。
4. 无头环境卡会话恢复（6/6 步）——老环境特性，本批再现一次，不影响用户真机。

### 四轮走查清单（建议顺序）

1. **刻度条**：点格直跳（中段格点击→消息滚到对应轮）；悬浮预览贴鼠标跟手+近屏幕边缘翻面；非选中格=矮波浪（无一格与选中格同高）；整体变宽；消息区原生滚动条已隐藏（滚轮仍可滚）。
2. **块头与展开**：纯思考轮也显示「已工作 X 秒」（运行中每秒跳动）；块头行内箭头/时长/模型徽章/摘要同一中线；块头下所有展开行左缘平齐+展开体统一左竖线；连续同类工具 3+ 收成「{中文名} ×N」（可点开）；连续注入收成「系统上下文注入 ×N」（有报错时带「·M 异常」红标）。
3. **属性面板**：选中 manuscript 卷/章 index.md——头部图标徽章+刷新按钮+字数统计在顶部；在正文编辑区打字→属性面板保存按钮点亮+未保存圆点→点保存→熄灭；选中 project.yaml——title 表单（清空 title→红框提示+保存禁用）。
4. **工具条**：模型框宽度贴合内容（右侧无空白）；右侧按钮与左侧同一中线；用户气泡变窄（480px 上限，长消息内代码块横向滚动不撑破）。
5. **Workflow 面板（用《新小说1》拆书会话）**：徽标点击开→再点收起；点面板外收起；Esc 收起；面板内计数与徽标一致；「当前不可回答」时头部标题变化。
6. **书架**：三本书都在；随便打开一本确认正常。

本窗口会话 ID：sess_31b7a638-ff76-402e-8e23-ba9f57ff2268
