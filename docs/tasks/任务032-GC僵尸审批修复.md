# 任务032-GC僵尸审批修复

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：GLM-5.3-Flash 执行（2026-09-25 用户终裁授权硬上，参谋部复核通过；原「GLM-5.3 旗舰 max、禁 Flash」表述作废），推理档位按会话现行配置。
- **模式**：目标模式 + 完全访问（**涉码**，主仓库分支 `task-032`，串行默认，无 worktree）。独立小单，不被任务031 阻塞，可与 G1 穿插排期（同一 5.3 窗口串行，一次只跑一单）。
- **给用户的现成命令**（让用户直接粘贴，省得他想）：
  - `/goal 任务032-GC僵尸审批修复：按任务书完成 harness 恢复投影僵尸审批修复，逐条断言交付`
- **对口技能与命令**：
  - 代码/diff 审查 → `ocr-review`；过度工程检查 → `ponytail-review`
  - 测试跑法见验收标准；不需要 UI 技能（纯后端投影修复）

## 目标

修掉 harness 恢复投影里的「僵尸审批」：会话已中断（interrupted）但恢复/读取投影仍无条件带出 pending 审批列表，前端恢复后被死卡灌屏（R5f 实战病灶：放弃按钮无反应的真凶）。用户已拍板=独立小任务先修（核销表 §6-2/§9），不等 G1。

## 根因定位（前线已查实，2026-09-25 阶段0审计）

- `packages/neuro-agent-harness/` 的 `neuro-agent-harness.ts` L2487-2496/L2742：恢复/读取投影直接把 `findPendingApprovalCalls` 扫描结果投影进 payload 的 pending 列表，**不按 invocation 状态过滤**。
- `resolveActiveInvocation`（L7710-7738）与 `hydrateWaitingInvocation`（L7741-7759）对 activeInvocation 有 waiting 门，但管不住 payload 里已写入的 pending 列表。
- 前端 waiting 门（`useAgentSession.ts` L366/L537，R5f a05764ee 已加）是兜底不是根治——本任务把根治做在后端投影。

## 范围与边界

- **允许改动**：`packages/neuro-agent-harness/` 内投影与恢复链路相关代码+新增/更新测试；`packages/neuro-book-contracts/` 仅在类型必须扩展时允许（披露）。
- **禁止改动**：前端 waiting 门兜底逻辑（useAgentSession.ts——保留为纵深防御，验收含其不回归）；`app/` 其他前端文件；master 直接提交；nb-ui。
- **修法路径二选一（施工时按代码实际裁定，披露选择理由）**：①payload 投影按 invocation 状态过滤（interrupted/非 waiting 不投影 pending）②中断时销号残留 pending tool call。两条都过同一验收断言。
- **不确定时**：停下，在任务书「疑问区」登记，待前线答复。

## 验收标准（逐条留命令输出）

1. 新增 harness 断言：interrupted 会话的 recovery payload `pendingUserInputs=0`（或等价结构）——先红后绿。
2. waiting 会话的恢复投影**不受误伤**：正常 waiting 会话 pending 列表照常投影（既有测试全绿+必要新用例）。
3. harness 包 vitest 全量绿（从该包 package.json 查脚本；harness 全量挂起=已知环境特性，按任务005 基线报告的分片绕过法跑，分片内全绿即可并披露）。
4. `bun run typecheck` 零错。
5. 前端 agent 目录回归：R5f 断言 5/5 与 agent 目录既有测试零回归（前端零改动应天然满足，跑了留数）。
6. 交付报告：修法路径选择+理由、改动文件清单、自测证据、遗留问题、本窗口会话 ID，追加在本任务书交付报告节。

## fixture 与真实后台边界

- 测试用合成会话 fixture（构造 interrupted/waiting 两种 invocation），不真实调用 Provider/Model。
- 真机会话验证不强制（R5f 病灶场景已由断言覆盖）；如做 dev server smoke，遵守密钥边界。

## 参考材料

- `D:\ProgramData\.zcode\workspace\default\out\单D-旧任务008与批次2挂账核销表-v1.md` §2 ③（根因证据与拍板）
- `docs/tasks/BOARD.md` 009单C批次1 行 R5f 段（病灶实战记录）
- 任务031（G1）——本任务先修投影单点后，G1 的聚合层读取侧按干净数据设计，两单正交。

## 疑问区（施工窗口填写）

（空）

## 交付报告（施工窗口填写）

**交付（2026-09-25，分支 task-032 @ 9f2f93a4，状态：待审）**

**做了什么**：harness 恢复投影单点修复——`neuro-agent-harness.ts` 两处 DTO 投影点（`getSessionLiveState` L2487 附近 live state、`buildSessionRecovery` L2742 recovery）的 `pendingUserInputs` 投影加 `activeInvocation` 门：会话中断/悬置/终态（activeInvocation 为 null）时，盘上残留的未应答审批不再投影进 recovery 与 live state。数据零改动（不动盘上消息与 resolution）。

**修法裁定（二选一授权已生效）**：选**方案①投影按状态过滤**，弃方案②（中断时销号）。理由：①②要写会话数据（补 resolution/改历史消息），须覆盖 abort、进程崩溃、服务重启多条中断路径，漏一条即复发，且「审批 toolCall 无回音」本身是历史事实，写销毁损历史；①是读路径单点修复，与既有 waiting 门（resolveActiveInvocation/hydrateWaitingInvocation）同源判定，两处投影点即全部消费面（grep 证实 `projection.pendingApprovals` 仅此两处消费者；`session_state_changed` 事件的 state 也来自 getSessionLiveState，自动一致）。

**根因核对（行号漂移已按现状核实）**：审计基准行号 L2487-2496/L2742 精确命中；resolveActiveInvocation 实际 L7707（审计 L7710）、hydrateWaitingInvocation 实际 L7723（审计 L7741），轻微漂移不影响结论。补充发现：abortInvocation 正常链路会补写 abort resolution（既有「WaitingUser + abort」用例证实），故僵尸的真实来源=进程崩溃等未写 resolution 的路径——方案①的读侧门对残留来源不敏感，全部兜住。

**自测结果（验收 6 条逐条）**：
1. ✅ 新增断言先红后绿：用例「终态会话残留的未应答审批不投影进 recovery/live state（僵尸审批防线·任务032）」——先红（旧代码把 zombie-call-1 投影进 recovery.pendingUserInputs，断言输出实锤病灶）→ 修复后绿。
2. ✅ waiting 会话不受误伤：既有「WaitingUser」用例（断言 recovery.pendingUserInputs[0] 存在）与 black-box 全文件 203/203 全绿。
3. ✅ harness 分片：`bun run test -- server/agent/harness` = 22 文件/362 测试全绿（本机直跑全绿，未触发任务005 登记的「harness 全量挂起」环境特性）。
4. ✅ typecheck：`bun run typecheck` exit=0 零错。
5. ✅ 前端零改动自然回归：agent 目录 35 文件/304 测试全绿（含 R5f 断言族）。
6. 交付报告=本节。改动文件清单：`neuro-agent-harness.ts`（两处门+注释）、`neuro-agent-harness.black-box.test.ts`（+1 用例 48 行）。

**复核三处修订注明**（参谋部复核意见，开工前已改，commit d4c1990b）：①模型口径改「GLM-5.3-Flash 执行（授权硬上）」②抬头改「施工窗口填写」③（032 无 i18n 项，该条仅涉 031）。

**偏差披露**：①ocr-review/ponytail-review **已补跑（2026-09-25，对两分支全量 diff）**：ocr-review 规则集逐条对照 harness diff=**0 发现**（严格等/null 守卫/无 any/无嵌套三元/无敏感面全过）；ponytail-review 唯一可收项=live state 门与 recovery 三元化形态对称（纯外观 -2 行，零行为差，**登记审查时裁量**，不为外观改动追加返修轮）。②真机 smoke 未做——任务书豁免（病灶场景已由断言覆盖，前端零改动）；病灶会话真机佐证已随任务031 G 段 smoke 完成（interrupted 会话恢复无僵尸卡，evidence/任务031-G1-smoke/）。③测试用例名含中文（仓库既有先例）。

**遗留问题**：无阻塞项。可选加固（不排期）：R5f 前端 waiting 门可待 G1 聚合层落地后评估是否降级为纯展示逻辑。

**本窗口会话 ID**：sess_f0132212-4e41-4155-9a58-7b0190ed6b84
