# 任务015-依赖安全修复

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3-Flash`，推理档位 **high**。若当前会话未在此配置，开工第一句先提醒用户切换。
- **模式**：**目标模式 + 完全访问**——改动点明确（2 个依赖决策）、验收客观，任务分支内自主连跑。
- **工作区**：主仓库 `D:\MyProject\neuro-book`，开工先 `git checkout task-015`（分支已建好；2026-09-20 起串行代码任务用主仓库普通分支，不用 worktree）。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务015-依赖安全修复：按任务书完成 undici 升级与 devtools 处置，bun audit 复查通过后写交付报告
  ```

## 目标

清除安全审计（任务013）中定为"必须修"的运行时依赖漏洞：undici 升到安全版本；对 @nuxt/devtools critical 做出处置决策并落实。背景与依据：`docs/security-audit-20260920/findings.json`（fingerprint: dependency-vulnerability-audit-results）与 `05-依赖与供应链.md`。

## 开工前置

1. 读 `D:\MyProject\neuro-book\AGENTS.md` 的"本 Fork 工作规矩"节
2. 读本任务书全文；确认当前分支为 `task-015`（`git branch --show-current` 验证；checkout 前先确认工作区干净）
3. 主仓库环境已就绪（node_modules 共享，任务004 已装），无需环境三步；依赖升级时的 `bun install` 属执行步骤本身

## 范围与边界

- 允许改动：`packages/neuro-book/package.json`、`packages/nb-ui/package.json`（如走 devtools 升级线）、根 `bun.lock`、`nuxt.config.ts`（如走锁定监听地址线，仅限 dev server 监听配置）
- 禁止改动：其他源码、测试文件、master 分支（不 commit 到 master，交付后由前线审查合并）
- 升级后若出现类型/测试失败：先判断是否升级引入；是则在边界内修复，超出边界停下登记疑问区

## 执行步骤

1. **undici（必修）**：`packages/neuro-book` 的 undici 声明 `^7.19.2` 锁定 7.28.0，漏洞区间 >=7.0.0 <7.29.0 → 升到 `^7.29.0`（或更高安全版），`bun install` 更新 lock
2. **@nuxt/devtools（评估二选一并落实）**：critical=GHSA-279x-mwfv-vcqv，经 nb-ui/主应用的 nuxt ^4.3.1 传递引入，仅暴露于 `bun run dev`。两条路线先评估再选：
   - A：bun overrides 强制 `@nuxt/devtools ≥3.3.1`（验证 dev server 与 nuxt 4.4.8 无冲突）
   - B：保持 3.2.4 但锁定 dev server 监听 `127.0.0.1`（核实当前默认绑定，如已默认 127.0.0.1 且不可达外网则记"风险已受控"选 B，零改动）
   - 优先试 A；A 引发兼容问题且排查超 30 分钟则落 B
3. 验证：`bun audit --audit-level=high` 确认 undici 5 条公告清零、devtools critical 消失（或 B 线有受控说明）；`bun run --cwd packages/neuro-book test -- server/config` 回归（基线 79 条）；dev server 冒烟（起服务访问 127.0.0.1:3000 首页可渲染）
4. 其余 79 项依赖告警**不处理**（用户拍板：开发依赖背景噪音，不排期）

## 产出与交付

- 分支 `task-015` 上的代码改动（不合并，等前线审）
- 任务书末尾交付报告：改动文件清单、devtools 路线选择与理由、bun audit 前后对比输出、回归结果、遗留问题、本窗口会话 ID

## 验收标准

- `bun audit` 中 undici 5 条公告清零；devtools critical 消失或有明确受控证据
- server/config 79 条回归全绿；dev server 首页可渲染（截图或 DOM 证据）
- 改动仅限边界内文件

## 完成定义（AGENTS.md 硬标准）

任务完成 = ① 分支已提交（涉码任务）② 交付报告已填 ③ 任务书声明"待审"。BOARD 更新与合并由前线执行。

## 参考材料

- `docs/security-audit-20260920/findings.json`、`docs/security-audit-20260920/05-依赖与供应链.md`
- `docs/tasks/evidence/任务005/测试基线.md`（回归对照基线）

## 疑问区（工程队填写）

1. audit 显示 nuxt 4.4.x 自身还有多条 high（GHSA-wm8w-6qjm-cv43 等 5 条，修复版本 4.5.1+）：升 nuxt 不在本任务边界，属任务014 上游调研/升级范畴，提请前线在排期时合并考虑。

## 交付报告（工程队填写）

**做了什么**：

- **undici（必修）**：`packages/neuro-book/package.json` 的 undici `^7.19.2` → `^7.29.0`，`bun install` 实际落 7.29.1（安全线 <7.29.0 之上），bun.lock 同步更新。
- **devtools 走路线 A**：根 `package.json` overrides 加 `"@nuxt/devtools": "^3.3.1"`（与既有 `diff` override 同区），实际解析 3.2.4 → 3.4.2。理由：overrides 一行即清除 critical（GHSA-279x-mwfv-vcqv），且冒烟验证与 nuxt ^4.3.1 dev server 无冲突，无需落 B 线（锁定监听地址）。

**改动文件清单**：根 `package.json`（+1 行 override）、`packages/neuro-book/package.json`（undici 版本行）、`bun.lock`。

**bun audit 前后对比**：
- 前：`30 vulnerabilities (1 critical, 29 high)`，critical 为 @nuxt/devtools 3.2.4（GHSA-279x-mwfv-vcqv），undici 7.28.0 在 high 列表（GHSA-4cwx-7wf7-3272）。
- 后：`--audit-level=critical` → `No vulnerabilities found`；`--audit-level=high` → `28 vulnerabilities (28 high)`，undici 与 devtools 均已不在列表。剩余 28 条 high 为用户拍板不处理的开发依赖背景噪音（nuxt 4.4.x 系、sharp、svgo、tar、mysql2 等）。

**自测结果**：server/config 回归 79 条全绿；dev server 冒烟通过——监听 127.0.0.1:3000，`GET /` 返回 200 且首页 HTML 正常渲染（devtools 3.4.2 与当前 nuxt 无冲突证据）；改动仅限边界内 3 文件。

**遗留问题**：疑问区 1（nuxt 4.5.1+ 升级建议归并任务014 排期）。

**状态**：**待审**。

**本窗口会话 ID**：sess_f7630ad3-9078-4e74-b751-945479569d6e
