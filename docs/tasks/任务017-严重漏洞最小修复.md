# 任务017-严重漏洞最小修复

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：`account:bigmodel-individual-coding-plan/GLM-5.3-Flash`，推理档位 **high**。若当前会话未在此配置，开工第一句先提醒用户切换。
- **模式**：**目标模式 + 完全访问**——三处修复点均有精确位置与包内先例可对齐，验收客观，worktree 隔离内自主连跑。
- **工作区**：worktree `D:\MyProject\worktrees\task-017`（分支 `task-017`，涉码任务不豁免）。
- **给用户的现成命令**（直接粘贴）：
  ```
  /goal 任务017-严重漏洞最小修复：按任务书完成 S1 保留路径拦截与 S2/S3 围栏标志位三处最小修复，补单测并回归，写交付报告
  ```

## 目标

按安全审计（任务013）REPORT.md 的**最小改动路线**修复两项🔴严重发现：S1（项目 workspace 文件即代码——`.nbook/` 系统目录未列为写保留路径，恶意项目包导入即投毒）与 S2+S3（AI 围栏两道锁——`execute_world` 无审批、bash 工具只读绕过）。用户已拍板：只做最小修复，结构性中期方案本次不做。

## 开工前置

1. 读 `D:\MyProject\neuro-book\AGENTS.md` 的"本 Fork 工作规矩"节（绝对路径，worktree 内向上搜索可能读不到）
2. 读本任务书全文；确认在 `D:\MyProject\worktrees\task-017`（`git rev-parse --show-toplevel` 验证）
3. worktree 环境三步：`bun install` → `bun run --cwd packages/neuro-book nuxt:prepare` → `bun run --cwd packages/neuro-book generate`
4. 精读 `docs/security-audit-20260920/01-代码执行面.md` 的 1.1/1.2/1.3 三节（本任务的完整漏洞分析与修法出处）

## 范围与边界

- 允许改动：
  - server 侧通用文件写端点与项目 zip 导入（upload-project）相关文件——`.nbook/` 保留路径校验（用 grep 从 `resolveContainedFilePath` 调用侧定位，审计指出写端点 `write.put.ts` 无保留名规则）
  - `server/world-engine/world-engine-tools.ts`（execute_world 工具定义，审计定位 :41 附近）
  - `server/agent/tools/file-tools.ts`（bash 工具定义，审计定位 :367 起）
  - 上述三处对应的新增/修改测试文件
- 禁止改动：`packages/llmlint/skill/`、其他包、master；**明确不做**结构性中期方案（workflow 求值移出宿主进程、环境变量白名单、真隔离 vm/子进程——审计列为中期项，本次范围外）
- 修复若牵出更大的设计问题：停下登记疑问区，不扩大范围

## 执行步骤（三处修复，每处都有包内先例对齐）

1. **S1 止血——`.nbook/` 保留路径**：文件写端点与 zip 项目导入在 containment 校验之外增加"目标路径含 `.nbook/` 段即拒绝"的保留校验。项目内已有保留名先例：`normalizeProjectRoot` 的保留名机制（路径穿越防护体系）——优先复用同一模式，别发明新机制。
2. **S2 一行——`execute_world` 补 `approvalRequired: true`**：对齐同目录 `workflow-tools.ts:70` 的既有先例（内联 workflow 已带此标志且有审批展示）。
3. **S3 一行——bash 工具补 `mutatesWorkspace: true`**：对齐 `file-tools.ts` 内 write/edit/apply_patch 的既有标志（:219/256/317），使其进入只读模式审批体系（判定点 `neuro-agent-harness.ts:5549`）。
4. **每处补单测**：S1 写 `.nbook/...` 被拒（写端点 + zip 导入两路各一）、正常路径不受影响；S2/S3 断言工具定义标志位为 true。测试风格对齐所在目录既有测试。
5. 回归：`bun run --cwd packages/neuro-book test -- server/config`（基线 79 条）+ 你新增的测试全绿。

## 产出与交付

- 分支 `task-017` 上的代码改动（不合并，等前线审）
- 任务书末尾交付报告：三处修复的 diff 摘要、每处对齐的先例、新增测试清单与结果、回归结果、遗留问题、本窗口会话 ID

## 验收标准

- 三处修复落地且各有单测覆盖；`git diff --stat` 改动仅限边界内文件
- 回归全绿；dev server 冒烟可起（不必截图，日志证据即可）

## 完成定义（AGENTS.md 硬标准）

任务完成 = ① 分支已提交（涉码任务）② 交付报告已填 ③ 任务书声明"待审"。BOARD 更新与合并由前线执行。

## 参考材料

- `docs/security-audit-20260920/01-代码执行面.md`（1.1/1.2/1.3——漏洞全貌与修法）
- `docs/security-audit-20260920/findings.json`（结构化记录与复现步骤）
- `docs/tasks/evidence/任务005/测试基线.md`（回归对照基线）

## 疑问区（工程队填写）

（空）

## 交付报告（工程队填写）

（空。格式：做了什么/为什么/自测结果/遗留问题/本窗口会话 ID）
