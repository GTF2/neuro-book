# 测试基线报告（任务005）

> 生成时间：2026-09-20 02:05 前后；执行环境：Windows 10.0.2610 / Bun workspace / 主工作区 master（commit 550ec838 之后、未引入新提交的状态）。
> 口径说明：vitest 包以 `Tests` 行为准（条数）；bun test 包以 `(pass)` 计数为准（条数）。时长为该包独占运行的实际耗时。

> **2026-09-20 更新注记（任务016 交付后）**：llmlint 的"24 文件收集失败"经任务016 诊断为环境性根因（web 子项目缺 `bun install` 与 `nuxt prepare` 两个本机生成步骤，非缺 tsconfig），补齐后 **vitest 36/36 文件 346 条 + bun test 80 条全绿**（346+80 即 llmlint 新基线口径，前线已独立复跑确认）。固化方案：见 `packages/llmlint/README.md` 测试节的一行说明。下表 llmlint 行为当时原始记录，保留作对照。

## 总览

12 个包全部执行。**10 个包全绿；主包 neuro-book 17 条失败（两类根因）；llmlint 24 个测试文件因 tsconfig 缺失无法收集；neuro-agent-harness 全量模式挂起（分片补齐，521 条全绿）。**

全仓合计（条数口径）：**约 4727 通过 / 17 失败 / 9 跳过**（主包 3302+17+3、harness 521、其余包见下表）。

## 每包明细

| 包 | 命令 | 测试文件 | 测试条数 | 通过 | 失败 | 跳过 | 时长 | 日志 |
|---|---|---|---|---|---|---|---|---|
| neuro-book（主包） | `bun run test`（vitest 全量） | 433（6 failed / 425 passed / 1 skipped） | 3322 | 3302 | **17** | 3 | 266s | 主包全量-第2次.log |
| neuro-agent-harness | `bun test`（全量挂起，分片合并） | 94 | 521 | 521 | 0 | 0 | 全量 N/A；分片 <5s | 包-neuro-agent-harness.log、…-重试timeout30s.log |
| neuro-book-contracts | `vitest run` | 6 | 32 | 32 | 0 | 0 | 1s | 包-neuro-book-contracts.log |
| neuro-book-manager | vitest | 42 | 339 | 336 | 0 | 3 | 10s | 包-neuro-book-manager.log |
| neuro-book-test-support | vitest | 2 | 9 | 9 | 0 | 0 | <1s | 包-neuro-book-test-support.log |
| nb-memory | `bun test` | 7 | 86 | 86 | 0 | 0 | 2s | 包-nb-memory.log |
| nb-history | `bun test` | 11 | 49 | 49 | 0 | 0 | 19s | 包-nb-history.log |
| nb-workflow | `bun test` | 13 | 103 | 103 | 0 | 0 | 1s | 包-nb-workflow.log |
| nb-ui | vitest（先 `nuxt prepare playground`） | 15 | 243 | 243 | 0 | 0 | 4s | 包-nb-ui.log |
| llmlint | `bun run test`（= registry:build + test:vitest + test:bun） | 36（24 收集失败 / 12 通过） | 119 | 119 | **24 文件级收集失败** | 0 | 2s（早退） | 包-llmlint.log |
| owned-process | vitest | 3 | 18 | 15 | 0 | 3 | 2s | 包-owned-process.log |
| file-snapshot-cache | vitest | 3 | 44 | 44 | 0 | 0 | <1s | 包-file-snapshot-cache.log |

## 主包 17 条失败摘要（不修，作为基线）

根因 A（12 条）：**profile-compile-worker 系列** —— worker 子进程 ESM 解析失败
`ERR_MODULE_NOT_FOUND: Cannot find module '...\server\agent\profiles\profile-compile-worker-runtime' imported from profile-compile-worker-entry.ts`。
- `profile-compile-worker.test.ts` 9 条：后台编译、并发编译、watcher 并发、compileAll stale 检测、in-process 发布、staging cleanup、全量编译、Registry sink 回调、director artifact 断言，均为上述解析失败的连锁断言失败（`result.ok` false、manifest undefined 等）。
- `profile-compile-worker-lifecycle.test.ts` 1 条：`ProjectNotOpenError` 实例断言失败（`expected Error to be an instance of ProjectNotOpenError`，疑似双模块实例/解析路径相关的 instanceof 失配）。
- `profile-compile-worker-preview.test.ts` 1 条：dry-run preview 断言 `expected false to be true`。

根因 B（5 条）：**profiles DSL 测试要求显式环境变量**
`Error: Import.path 缺少显式 NEURO_BOOK_REPOSITORY_ROOT。Product Runtime 不允许从 checkout 或 import.meta.dirname 推断仓库根`（`profile-dsl.ts:1792`）。
- `rp-profiles.test.ts` 2 条、`simulation-director-profiles.test.ts` 2 条、`world-engine-profile.test.ts` 1 条。
- 从代码推断：设置 `NEURO_BOOK_REPOSITORY_ROOT=D:\MyProject\neuro-book` 可能消除此类失败；本任务未做任何环境改造，仅记录。

## llmlint 收集失败摘要

24 个测试文件在 vitest 收集阶段报 `[TSCONFIG_ERROR] Failed to load tsconfig for 'web/app/utils/agent-abort-state.ts'（及 web/server/... 等多文件）: Tsconfig not found`（vite:oxc 插件）。已加载的 119 条测试全部通过。`bun run test:bun` 因 `&&` 链在 test:vitest 失败后未执行。属配置/环境问题，非断言失败。

## neuro-agent-harness 挂起记录

- `bun run test`（= `bun test --parallel=1`）与 `bun test`（无参数）**两次全量均在同一位置挂起**（日志停在 `tests\wait-follow-up-drain.test.ts` 全部通过之后，`waiting-control-process.test.ts` 尚未开始；每次已通过 476 条、0 失败；观察 20+ 分钟无输出）。`--timeout 30000` 参数未生效。
- 剩余 9 个文件单独跑全部通过：8 文件 41 条 + `wait-for-invocation.test.ts` 4 条。
- 疑似与环境相关（子进程型测试在全量串行上下文中阻塞；本机 Agent 沙箱有拦截子进程的已知先例，见 AGENTS.md 环境坑位）。**未验证具体根因。**

## 锚点对照

- 前任 275 条基线（5 目录子集）：本次主包全量 3322 条，口径完全不同，无可比性；前任所跑 5 目录均在本次全量范围内。
- 任务004 实测 server/config 79 条全绿：本次主包全量中 server/config 未出现在失败清单，与"该子集全绿"一致。

## 其他观察

- 主包两次全量（间隔约 5 分钟）结果有轻微波动：第 1 次 `3310 passed (3330)`，第 2 次 `3302 passed (3322) + Errors 1 error`。失败 17 条两次一致；差值 8 条疑与一次未处理 error 中断某文件执行有关。基线建议以第 2 次（有完整日志）为准。
- 测试全程未改动任何已跟踪文件（多次 `git status` 核对，仅有其他窗口写入的 BOARD.md 修改与闲时任务待建文件，非测试产物）。
- 未跑 `test:real-model` 及其他 `test:*` 变体（按任务书要求）。
