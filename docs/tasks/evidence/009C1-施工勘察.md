# 009单C批次1 施工勘察笔记（2026-09-21，5.3 开工用）

> 等待参谋部确认期间的前期勘察落盘，防会话压缩丢上下文。行号基于 master `8538c059`。

## 1. 消息流数据链（作文流改造的主线）

- **展开器**：`agent-message.ts:253 toChatNodes()`——`ChatNode` 仅两种：`{kind:"text", message}` / `{kind:"tool", message, toolCall}`。一条 AI 消息可展开为 text+多个 tool 节点；tool 有 task 去重逻辑（只留最后一个 task_create/set_status）。
- **AgentWorkBlock 聚合插入点**：`toChatNodes` 输出侧或 ChatFlow 渲染侧，把**连续 tool 节点**聚成块（块头=过程行）。旧库图纸 `44eeb913` 的 chat-work-blocks.ts 同职能（按工作类型聚合）——开工时 `git -C "D:\MyProject\neuro-book失败fork开发但可以学习经验\neuro-book" show 44eeb913` 学聚合规则，按新库重写。
- **渲染容器**：`AgentChatFlow.vue:345-390`——`v-for chatNodes`，text→AgentTextBubble、tool→AgentToolBubble（64 行薄包装，内含 AgentToolNode.vue 121 行）。空态三形态在 394-421（main/unselected/compact）。

## 2. AgentTextBubble.vue（608 行）现状要点

- 分流：`message.type` 三态 system（397 行，跑挂可切分支）/ user / ai——**user 与 ai 共用正文模板**（465 行起"消息正文"段），作文流需在此按 type 分流布局。
- **AI 正文现状=卡片**：`rounded-2xl border bg-[var(--chat-ai-bg)] px-4 py-3 shadow-sm`（约 520 行）——作文流改造=去 border/底色/阴影/圆角，融入文档流。
- 思维链已有折叠（491-515 行"Assistant 思维链"段，可折叠+摘要行）——过程行形态可参考此交互。
- hover 功能按钮现状在消息头部（465-484 行 copy/edit/retry/branch 一排）——需迁移到 hover 显示+位置按总纲（用户右下/AI 左下）。
- 编辑器 AgentHistoryMessageEditor 内嵌（530 行起）。

## 3. AgentChatSurface.vue（4501 行）顶栏区

- **顶栏七按钮**：4255-4315 行（＋/📎/👥/🌿/▮/💬/✕，各按钮行号见规格包 §1 实查记录）。
- 删除✕=4312 行 `emit('close')` 按钮；ⓘ 容器新建挂顶栏右侧。
- 消息流挂载：4360 行起 `<AgentChatFlow :messages="renderNodes">`；AgentSessionScaleBar 挂载点=ChatFlow 外层容器右缘（相对定位）。

## 4. AgentComposer.vue（756 行）工具条区

- 模型选择器：640 行 `<AgentSessionModelControls>`（含弹层内 reasoningEffort select，116-120 行）——**思考档外置件挂其右侧**，数据通道=sessionModelDraft.reasoningEffort。
- 底部状态条：703-756 行（gauge 芯片/累计/缓存/费用/连接/相位/模式徽标）——保留重排；缓存环挂模型选择器左侧（hover=props.cumulativeCacheCompactLabel，点击=复用 open-context-inspector）。
- 附件入口收编进输入框左下：现有附件面板联动在 436-450 行区（TipTap pending 输入）。

## 5. Flash 三组件（工程队 task-009c-f1）

契约已锁规格包 §4+任务书 `任务009C1F-Flash三组件+i18n.md`；挂载归 5.3（本笔记 3/4 节挂载点）。

## 6. 5.3 施工顺序核对（规格包 §6.2）

1. AgentTextBubble（用户右气泡/AI 零框分流）→ 2. AgentWorkBlock 新建+agent-message.ts 聚合 → 3. AgentChatFlow（接入块渲染）→ 4. AgentComposer（工具条收编+挂 Flash 件）→ 5. AgentChatSurface（删✕/建ⓘ/撤迁按钮+挂 ScaleBar）→ 6. 测试对比基线（3320过/17败/3跳）→ 7. headless 七屏截图（规格包 §5.2.3）→ 8. tag 009c-batch-1。

## 7. 环境提醒（本会话实测）

- dev server 在跑（15:24 启动带 OOM 防护）；改前端 .vue 走 Vite HMR，改 server 代码须 rm -rf .nuxt/dev（本批基本不涉 server）。
- headless 截图须用真实书籍既有会话（ge-zhi-shu 有数据；123/xin-xiao-shu-2 已清空勿用）。
- typecheck 约 2-3 分钟；全量 vitest 约 4.5 分钟。
