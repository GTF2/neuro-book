# 任务041-基板样张人工复核返修

## 运行配置（开工前设置，本节由前线填写完整）

- **本会话模型**：前线指挥部窗口自干（不派工程队——00 总览裁定「基板+两样张归前线收口权」），会话模型以当前窗口实况为准。
- **模式**：目标模式 + 完全访问（out/ 非 git 目录用断言+指纹守边界；仓库侧仅治理文档直提 master）。
- **给用户的现成命令**（让用户直接粘贴，省得他想）：
  - `/goal 任务041-基板样张人工复核返修：按任务书执行人工复核返修单 v1 第 1 批（基板+两样张+契约分裂组定稿），按验收标准交付`
- **对口技能与命令**（必填；没有合适的写"无"）：`ponytail`（修法最小化自查）；断言复跑用既有 9 套脚本（见参考材料），不引新依赖。

## 目标

落实《单D-人工复核返修单 v1》第 1 批：修复人工复核全量核查中**基板（路1，26 条）与两样张（路4，24 条）**的缺陷，**P1 契约分裂 5 组定稿应然值**（对照 Ant Design 组件规范 + Material Design 动效 + WAI-ARIA 实践，先写入规格文本再统一回改，含二级页 5 张与速查卡），spinner 统一为 Material 不确定型圆环，P2 九条按 00 总览建议值执行（P1A 项移交工程队批），全套断言零回归。

唯一清单（只读输入，不改）：
- `out/审计/单D-人工复核全量缺陷核查-00总览与分诊-v1.md`（105 条总账+P0 18 条表+P2 9 条表）
- `out/审计/单D-人工复核核查-路1-基板-v1.md`（26 条）
- `out/审计/单D-人工复核核查-路4-两样张-v1.md`（24 条+契约形态清单 14 项）

## 契约分裂 5 组应然值（定稿，写入规格后照此回改）

| 组 | 应然值（定稿） | 业界对照 |
|---|---|---|
| 1 选中态 | 左 2px `--accent` 线 + fw600 + `aria-selected` ✓（U+2713 `::after` 右缘）——线指示位置、✓ 指示已选 | Ant Menu 选中态视觉分层；WAI-ARIA `aria-selected` 语义；Material list 选中 check |
| 2 ✓ 绑定 | 跟 `aria-selected` 走（非 `.active` 键盘高亮）——基板 JS 改 | ARIA 1.2：aria-selected=已选中项；高亮=aria-activedescendant |
| 3 下拉面板 | 默认向下 `top:calc(100%+6px)`，空间不足翻转向（JS 夹取）；radius 8px；max-width 280px；max-height 256px+overflow:auto；右对齐变体 `.drop-panel.end` | Ant Select 默认下弹+自动翻转+maxHeight 256 |
| 4 触发器/选项标尺 | 以基板接线现状为准则：触发器 28px/radius 7px/font 11px/padding 4px 10px/gap 6；选项 padding 6px 8px 6px 10px/radius 6px/行高≈30；chev 11×11 `.15s`（删基板 12px 内联） | 取全站多数现状为基线，样张①回改 |
| 5 确认层 | `right:0` + width min(300px,80vw) + 入场 `.p0-in`（基板删 slidein）+ 淡出 cubic-bezier(.4,0,.2,1) + 左 2px danger 线收起时保留（只透明上下边） | 破坏性确认贴触发钮；标准曲线全站统一 |

**spinner 应然值（参谋部拍板）**：Material 不确定型圆环=SVG circle（r=9，stroke-dasharray 伸缩 1.4s ease-in-out）+容器 rotate 1.4s linear；`<i class="spin">` 换 `<svg class="spin">`（P1-JIBAN 断言按 `.spin` 类查，不受影响）。全站唯一实例=基板 P1-α 07。

**P2 九条执行值**：①整件拒绝独占行尾；②02 格改标签「live（主键实底）」；③P1A 危险钮=**移交工程队批**（登记不施工）；④=P1-1 执行；⑤文案按实测「收起=两段式 340ms（内容 0-160ms 淡出+容器 120-340ms 合拢）/展开=容器 220ms+内容至 300ms」+总纲 L52 措辞清理；⑥暗主题浮层底换 `--canvas`（亮主题维持 `--paper`）；⑦D1 契约表以基板为准改或登记偏离；⑧typeahead 加 1s 超时窗口+注释标注；⑨两样张页壳统一 920px/上边距 24px/stage-h 13px。

**基板其余条目处置**：缺陷 6（图标混排→SVG 箭头统一）/7（4.6-4.8 拆三 stage）/8（P1-γ 分隔条改细线+1.2 补键盘）；疑似 9-19、21-25 全修（其中 17 emoji 图标=锁/菜单补 SVG，✓✕⚠ 维持字符——契约清单已定格 U+2713 且路4 实测全站一致，换 SVG 属全局推翻，登记偏离）；路4 B 级 10-20 全修；C 级 21（关闭无过渡）维持写入契约、22→P2-8、23（确认层无点外）维持写入契约、24→P2-9。

## 范围与边界

- 允许改动：
  - `out/图纸/`：单D-设计基板.html、单D-路由自绘选择器样张.html、单D-showhide-helper样张.html、单D-二级-设置/收件箱/世界书/剧情大纲/角色档案.html、单D-组件速查卡.html、_图纸索引-人工复核.html（仅主题切换文案同步）
  - `out/规格/`：单D-设计基板-v3.md（契约定稿节+锚点回填）、单D-总纲-v2.md（L52 收窄口径措辞）
  - `out/工具/`：拷入样张②套件 `_verify_showhide.mjs`（修 SRC 指向 图纸/ 子目录）；新增针对性断言
  - `out/审计/`：改前基线与改后复跑输出留档、交付报告（或审合记录追加）
  - 仓库 `docs/tasks/`：本任务书+BOARD.md
- 禁止改动：master 仓库源码（packages/ 等）；P1A/B/C/D 四包样张页与其断言脚本（路2/路3 归工程队批）；批次1 四件（ChatFlow/chat-work-blocks/TextBubble/ScaleBar）行为逻辑（刻度条 BASE=38→55 属数值对齐修正，accv 断言预期即 55，非重写）；基板断言脚本本身（f3/f3b/accv/P003/P004/P1-JIBAN/g1 探针——只跑不改）。
- 不确定时：停下，在任务书"疑问区"登记，待参谋部答复。

## 步骤建议

1. 改前基线：9 套断言全跑留档（f3/f3b/accv/P003/P004/P1-JIBAN/g1 探针/样张① 30/样张② 7）
2. 契约定稿写入 `out/规格/单D-设计基板-v3.md`（新节）+总纲 L52
3. 基板修复（P0 5 条先行→契约回改→疑似/P2）
4. 样张①修复→样张②修复
5. 二级页 5 张+速查卡+索引文案回改
6. 全套断言复跑逐条留输出+零回归核对
7. 交付报告+审合记录+BOARD

## 验收标准

- P0 功能失效全修：基板 5 条（右栏塌缩/拖拽粘滞/spin/4.8 死链/拒因静默）+样张 A 级 9 条，逐条可演示
- 契约 5 组应然值已写入规格文本并全站回改（基板+样张①②+二级页+速查卡）
- 断言零回归：f3 全绿+f3b 全绿+accv 恰 7 存量（同一批旧断言，零新增）+P003 14/14+P004 7/7+P1-JIBAN 17/17+g1 探针 7/7+样张① 30/30+样张② 7/7；改前基线与改后输出全部留档
- 针对性新增断言：契约 5 组关键项（选中态/✓绑定/面板方向翻转/确认层对齐曲线/spin 可见性）
- 交付报告含：逐条处置表（26+24 全量对账）、改动文件清单、指纹版本链、遗留问题

## 参考材料

- 断言脚本：`out/工具/_verify_f3.mjs`、`_verify_f3b.mjs`、`_accv_jiban_v3.mjs`、`_verify_p003-jiban.mjs`、`_verify_p004-jiban.mjs`、`_verify_p1jiban.mjs`（后四者+g1 探针跑法=`cd packages/neuro-book && bun <脚本>`）；样张①套件=`out/工具/单D-备料单①复验脚本-v1.mjs`（+注入件成对）；样张②套件=临时根 `_verify_showhide.mjs`（拷入工具/修路径）；g1 探针=`packages/neuro-book/scripts/g1-p0-04-verify.mjs`
- 跑法坑（记忆）：playwright-core 系必须包内 bun 跑（node 直跑 ERR_MODULE_NOT_FOUND）；Edge dump-dom 系 node 直跑
- 业界对照源：Ant Design 组件规范（Select/Popconfirm/Menu）、Material Design Progress indicators（indeterminate circular）、WAI-ARIA 1.2（aria-selected/activedescendant）

## 疑问区（工程队填写）

（前线自干，无）

## 交付报告（前线自干，2026-09-26）

### 做了什么

人工复核返修单 v1 第 1 批全量完成：**基板 26 条+两样张 24 条逐条处置+契约分裂 5 组定稿回改（含二级页 5 张+速查卡）+spinner Material 圆环+P2 九条**（P2#3 P1A 项移交第 2 批工程队）。定稿先落规格（基板-v3.md 新增 §10 定稿节+总纲 L52 口径清理），再统一回改。完整档案=`out/审计/单D-人工复核返修审合记录-v1.md`（指纹版本链/逐条对账/断言账/移交清单）。

关键改动：
- 基板 167,586 B：P0 五条全修（右栏 flex:1/拖拽 .dragging 停过渡/spin=SVG Material 不确定型圆环/4.8 下拉补 chevron+「编辑方案」改 disabled 按钮/拒因未选 disabled）；契约 5 组回改（选中态=左线+600+aria-selected ✓ 全站三系统、✓ 绑定 aria-selected、面板默认向下+翻转+256 内滚、chev 11px+.15s、确认层删 slidein 统一 .p0-in）；刻度条 BASE 38→55 对齐规格（accv 一条存量 FAIL 转绿实证）；疑似 16 条全修（图标 SVG 化/kind 图标映射/stage 拆分/字号归并 10.5px×18 处/inert 可视化/整格命中区等）；P2 基板项（整件拒绝独占行尾+danger 语义升级/02 标签改 live/暗浮层底 --canvas）。
- 样张①（15,327 B）：标尺回改基板准则（28/7/11）、combobox role、typeahead 过滤 disabled+1s 超时、面板翻转/max-height、主题 .seg 页头右侧、页壳 920/24/13。
- 样张②（23,098 B）：确认层 right:0+300px+标准曲线、panel-hint 中性色、.btn:hover 灰底、收窄终态 margin 归零+左线保留、文案改两段式实测口径（340ms）、measure 时机与基板同步、主题 seg 文案统一。
- 二级页 5 张+速查卡：选中态补 ✓（drop-opt 系）、暗浮层底、禁用视觉 45% 归一、速查卡三合一改双要素；索引页主题文案同步。

### 为什么

契约分裂 5 组取值=00 总览建议值+业界对照（Ant Select 下弹翻转/maxHeight 256、ARIA 1.2 aria-selected 语义、Material indeterminate 规格）——详见规格 §10 各行「业界对照」列。基板接线现状立为标尺准则（7/8 件多数派）。两处独立裁定已在审合记录 §六登记偏离理由（✓✕⚠ 维持字符=契约定格+路4 正面项；样张② stage 无框=宪法6② 卡中卡禁令正确解）。

### 自测结果

10 套断言改前基线+改后复跑全量留档（out/审计/两份 txt）：

| 套件 | 改前→改后 |
|---|---|
| f3 / f3b / P003 / P004 / P1-JIBAN / g1 探针 / 样张① / 样张② | 全部 24/32/14/7/17/7/30(28+6)/满绿→**零回归** |
| accv | 恰 7→**恰 6 存量**（-1 转绿=BASE 55 修复实证；剩 6 条同一批 v1 旧断言语义冲突，禁改口径不变） |
| recheck041（新增） | **20/20**（契约 5 组关键项+P0 修复实锤） |

新坑实锤一条（Edge 虚拟时钟首帧 recalc 时序致 f3b 假 FAIL，修法+判别法已记审合记录 §六.1）。

### 遗留问题

1. 第 2 批移交工程队：00 总览 P0 表 #6-#18+路2/路3 全量+P2#3+D1 样张侧+is-selected/seg/契约五组在四包样张的回改（清单见审合记录 §五）。
2. accv 存量 6 条维持登记（断言脚本禁改）。
3. 真机人工复核三项（缩放/窄屏/读屏）继续移交用户日常复验。

本窗口会话 ID：sess_31b7a638-ff76-402e-8e23-ba9f57ff2268（前线指挥部）
