# 任务009C1R2-批次1二轮返修

> 状态：**待审**（2026-09-22 前线施工交付，分支 `task-009c-main` @ 009C1R 完工态之上）。
> 任务源：用户二轮走查+三轮补图判返（/goal 口令 2026-09-22）。七件全部 5.3 施工。

## 运行配置

- 负责窗口：前线指挥部（全部巨石文件归 5.3；件2/件3 max 档）
- 模式：目标模式 + 完全访问
- 分支：`task-009c-main`（续批次 1 分支）

## 交付报告（前线填写，2026-09-22）

### 七件落地

**件1 绿环数据源+定位**：环比例改 `contextUsage.usedTokens / limitTokens`（Surface `cacheRingRatio`，与底部 context gauge 同源同值；不再用累计 cacheRead）；hover 面板首行=「上下文占用 X / Y（Z%）」（新增 props contextUsageLabel/contextPercentLabel），其余五行缓存明细保留；面板定位从居中改 `bottom-full left-0` 左缘对齐绿环向右展开（修左缘切字）。

**件2 刻度条照旧库重做**：格=2px 细横线（`h-[2px] rounded-full`），长短按体量分级（weight=轮内节点数三档：≥6 全宽/≥3 2/3/其余 1/2），无底轨无边框无色块；当前位置线加亮加长（w-full+text-main）；**AgentScaleOutlinePanel 中面板组件及其 2 条测试整体删除**，点格=直接 seek 滚动定位（pointerdown 即定位+拖动节流），hover 预览卡跟格；底部入口开 SessionTreeDialog 保留；容器右缘 24px 不侵占正文。

**件3 一轮工作块整体化（max 档）**：`chat-work-blocks.ts` 聚合重写——聚合边界=相邻 user 消息之间全部节点收进 `{kind:"round"}` 轮次块（含多段正文+思考行+全部工具调用）；`AgentWorkBlock.vue` 重写为轮块头「已工作 X 分 X 秒 ˅」（灰小字+时长+模型徽标+收起态计数摘要，箭头行首）；AI 身份锚定行由块头替代（`suppressIdentity` 控制位，残留身份行 DOM 断言=0）；悬浮按钮组只在轮尾最后一段正文后（`isActionsHost`，`suppressActions` 控制位）；过程行零缩进（AgentToolBubble pl-6 删）；间距=轮间 16px/轮内 8px（列表 mt-4+轮内 mt-2，旧 mt-6 大空白消除）；工具行箭头统一行首（AgentToolNode 收起态 chevron 移首位，展开态头部保持）；连续 system 注入≥2 收拢「系统上下文注入 ×N」点击展开（`injections` 项+展开 Map）；思考行时序不动（TextBubble 内部，由 `showThinking` 随轮展开态显隐）；历史轮默认收起过程行（正文恒显），运行中/失败轮默认展开，用户 toggle 后固定。

**件4 档位按模型过滤**：`EnabledModelOptionDto` 加 `thinkingLevelMap`（复用 PiThinkingLevelMapSchema）+`config-service.ts` 组装处透传（另补 AgentProfileModelFields/useModelSettingsDraftSession 两处构造点）；`AgentThinkingLevelSelect` 按当前选中模型 map 过滤选项（map 空/null 回退全量七档），**「跟随Profile」选项删除**（回退走 Profile 设置页）；显示值=requested ?? effective（当前生效档）；`FormSelect` 加 `displayLabel` 覆盖 prop，模型触发器只显 modelId（下拉列表保留完整「厂商/模型」）。

**件5 用户气泡自适应**：`w-[65%]` → `w-fit max-w-[65%]`（字少随字缩、字多封顶、不随面板伸缩）；折叠/展开联动不变。

**件6 工具分组全覆盖**：映射表扩至 46 工具全量（explore 18+edit 13+command 1+database 1+web 2+world 1 新类别+不折叠 10），`i-lucide-orbit` 图标；块摘要改计数形态（类别人话+计数 ≤3 类+「等 N 类」；工具细分计数 ≤3+「共 N 次」入 hover title；edit 带「X 个文件」；save/write 族首个目标名 title 带出，argsJson title/name 兜底）；复用 filePaths/toolCounts 零新数据；MIN_BLOCK_SIZE 等规则语义随轮聚合迁移（轮粒度收起）；i18n 双语 36 工具短名+6 块头键块尾追加。

**件7 底部栏清爽化+Workflow 收编**：「+」通用导入按钮与文本读入功能整体删除（PDF 归后续小单）；图片按钮挪工具条最左第一位；模型框/档位件 `FormSelect` 加 `bare` 芯片形态（无边框纯文字+箭头+hover 底色）；底部统计条从独立居中行改输入框容器底部内缘右对齐小字（数据同源不变）；`AgentWorkflowPendingPanel` 常驻大块改一行紧凑徽标（Inbox+「N 个流程等应答」+角标）点击弹居中 Dialog 承载原有应答卡（select/text/同意否决与提交零改动），「每个流程分别应答」说明移入弹层头部。

### 怎么测的

- **vitest 全量**：**3350 过 / 17 败 / 3 跳 = 基座（009C1R 完工态 3342）− 2（outline-panel 测试删除）+ 7（chat-work-blocks.test.ts 新文件）+ 1（CacheRing）+ 1（ThinkingLevelSelect）+ 1（ChatFlow 模板断言）精确吻合**；17 败全为 server/agent/profiles worker 系基线环境失败零增。
- **typecheck**：零错（含 DTO 扩字段连锁修复 3 处构造点）。
- **dev 编译器实证（坑③）**：清 .nuxt+缓存重启，curl 三件全 200；编译产物含 AgentWorkBlock/injections/ringRatio（新代码非陈旧缓存）；正则计数器误报已知，以编译产物为准。
- **DOM 断言 12/12 PASS**（playwright 无头 + chromium-1234，1600×620 视口，会话 64 真实数据）：①环比例 7.7%≈context gauge ②hover 面板 [1252,1460] 视口内完整 ③点中格 scrollTop 8259→4774 直接滚动+中面板=0 ④块头 7/残留身份行 0 ⑤过程行行首=chevron-right ⑥档位选项=[关闭,低,中,高]（模型 map 过滤生效）且无跟随Profile ⑦模型触发器文本=glm-5.3-flash（无「 / 」）⑧w-fit 气泡 7 ⑨46 工具归类 vitest 断言覆盖 ⑩工具条首位图片钮/无「+」 ⑪芯片 2 个均无边框 ⑫Workflow 无常驻大块。截图三张归档 `docs/tasks/evidence/009C1R2-返修截图/`。
- **对账裁定执行确认**：本报告基座 commit 与替换/新增分列（参谋部 2026-09-21 整改要求）。

### 偏差披露

1. **件2 刻度线长短分级未逐消息体量**：旧库按单条消息体量分级；新实现按格（聚合段）内节点总数三档分级（weight），视觉等价、粒度更粗。走查不满意可改按字节数连续分级。
2. **件3 块头时长精度**：durationMs 取轮内首尾消息时间戳差（秒级精度，流式重投影时刷新）；无实时跳动 interval（旧库"已工作 37 秒"跟手效果未复刻）。
3. **件3g 注入收拢边界**：system 注入不打断轮连续性（夹在轮中间的注入参与收拢、归属其后的轮）；单条 system 不收拢保持原卡片。
4. **件4 map 语义**：thinkingLevelMap 以 keys∩七档过滤；用户配置 map 含非标枚举键时忽略。显示值=生效档；requested≠effective 的中间态直接显示 requested。
5. **件7c Workflow 弹层**：会话 64 无待应答数据，DOM 断言仅覆盖"无常驻大块"负向形态；徽标→弹层→完成一次应答的正向链路待真机（需要真实 workflow 产生待应答）。
6. **失败轮默认展开**（hasFailure）沿旧规则，但轮粒度下"失败"=轮内任一工具失败，展开后正文+全部过程行可见。
7. **旧 AgentWorkBlock 聚合语义变更**：原"连续同类工具聚合块"概念被轮次块取代；同文件旧测试不存在（44eeb913 移植版测试从未建），新测试 7 用例覆盖轮聚合+46 映射。

### 遗留问题

- 块头摘要 hover title 的工具短名在 zh 语境下的可读性待真机走查。
- 件7b 统计条"并入输入框容器底部内缘"现为容器内贴底右对齐小字（未嵌入 border 框内侧——Composer 的 border 结构在 AgentComposerInput，统计条与工具条同层；视觉等效，如需真嵌入需重构输入容器层级）。
- Workflow 徽标正向链路（件7c）待有真实待应答数据时真机验证。

## 登记不施工（BOARD 同步两行）

1. 两套应答机制（AgentUserInputPrompt 主会话阻塞型 vs Workflow 后台并行型）职能不能互替——合并会丢"后台作业运行时主会话可继续聊天"能力；统一应答协议归批次 2。
2. PDF 等格式导入=后续小单（已有台账）。

## 疑问区

（空）
