# 05 依赖与供应链

> 结论一句话：**补跑完成：81 项漏洞（1 critical / 29 high / 44 moderate / 7 low）。真正要盯的只有一个 critical（@nuxt/devtools 开发工具链 RCE）和 undici（唯一进产品运行时的直接依赖）；其余集中在构建/测试工具链。两个依赖补丁复核过，干净。**

## 5.1 审计结果（2026-09-20 实测，Bun 1.4.2 / bun audit）

- 完整机读存档：[bun-audit-raw.json](bun-audit-raw.json)（`bun audit --json` 输出）。
- 汇总：**81 vulnerabilities (1 critical, 29 high, 44 moderate, 7 low)**。

### critical（1 项）

| 包 | 版本 | 公告 | 暴露面 |
| --- | --- | --- | --- |
| @nuxt/devtools | 3.2.4（<3.3.1） | GHSA-279x-mwfv-vcqv：未认证 DevTools RPC 可在开发者主机执行任意命令 | **仅 `bun run dev` 开发服务器场景**；经 `packages/nb-ui` 的 `nuxt ^4.3.1` 传递引入 |

### high 里值得单独看的

- **undici 7.28.0**（唯一进产品运行时的直接依赖，`packages/neuro-book/package.json:160` 声明 `^7.19.2`）：5 条公告，含 high 跨用户信息泄露/解析崩溃（GHSA-4cwx-7wf7-3272）+ CRLF 注入等 4 条 moderate。修复区间 ≥7.29.0。
- svgo（2 条 high，构建链 SVG 清洗绕过）、tar 7.5.20（high，构造长路径 DoS）、vitest 4.1.10（moderate 路径穿越/任意文件读，GHSA-82fw-gwwq-j7x9，**仅测试环境**）——全部在构建/测试工具链，不随产品分发给用户运行时。

## 5.2 修复建议（升级属源码改动，留给前线排任务）

```bash
bun audit fix        # 声明范围内升级（约解决大半 moderate/low）
```

再手工三处：① undici 升 `>=7.29.0`；② nuxt 升级或 bun overrides 强制 `@nuxt/devtools >=3.3.1`；③ vitest `>=4.1.11`。预计全部可控，无破坏性 major 跨越。

## 5.3 供应链现状（已复核）

- **锁文件**：根目录单一 `bun.lock` 全覆盖，无双锁漂移；根 package.json 无可疑 postinstall。
- **依赖补丁**：`patches/` 两个——`nitropack@2.13.4.patch`（错误堆栈解析加 source-map consumer 缓存与 destroy 清理）、`proper-lockfile@4.1.2.patch`（mtime 精度不支持时提前回调错误）。**通读复核：均为正经 bug 修复，无恶意注入。**
- **安装源**：registry 配置无私有镜像/可疑源。
- 上游自带发布链 security audit 门禁与 sha256 清单（PROJECT-STATUS 可见），供应链意识较好。

## 5.4 环境记录

- 首轮审计因本机网络 TLS 全断失败（UNKNOWN_CERTIFICATE_VERIFICATION_ERROR），网络恢复后补跑成功——过程差异只影响时间，不影响结论。
- Bun 1.4.2 / Node 22.22.2 / gh 2.101.0。
