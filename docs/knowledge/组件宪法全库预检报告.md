# 组件宪法全库预检报告

- 生成日期：2026-09-22
- 性质：单D 预研产物（闲时任务生成，待前线走查）
- 证据来源：六份扫描材料（宪法五条款扫描 C-1~C-5 + 弹窗滥用扫描）的编辑整合；全部 `文件:行号` 证据来自材料内实读（grep/rg 候选 + 逐文件 Read 判读），本文未新增勘察。弹窗各表中位置省略前缀 `packages/neuro-book/app/components/`。

---

## 扫描范围与方法说明

- **范围**：`packages/neuro-book/app/`（pages/components/composables/utils 全部 .vue/.ts）+ `packages/nb-ui/src/`（nb-ui dist 为产物未扫）。
- **方法**：各条款先以 grep/rg 关键词定位候选（如 `<select`、`underline`、`border-l/b-t-N`、`collapsible|expand`），命中后逐文件 Read 判读功能语义与条款适用性；测试文件、注释文字、状态装饰类命中剔除。全程只读，未改任何文件，未做浏览器视觉确认（各节"未验证项"已注明）。

### 汇总表

| 条款 | 违反数 | 合规结论 |
|---|---|---|
| C-1 禁原生 select | 23 | 不合规：19 处主产品 + 4 处 preview 路由页均渲染原生 `<select>`，弹出面板无法走主题变量；需统一换 FormSelect |
| C-2 可按必带容器 | 4 | 不合规：4 处操作按钮为纯文字 + hover 下划线裸样式，需补容器 |
| C-3 选中线（左 2px 均匀线） | 7 | 不合规：6 处 3px/半高/圆头超标线 + 1 处底部粗线冒出；合规基线形态已存在可对齐 |
| C-4 底部栏一体成型 | 7 | 不合规：3 处输入区/工具条实色分界+背景差 + 4 处轻量按钮 hover 无底块 |
| C-5 展开层级缩进 | 6 | 不合规：工具/过程行展开内容普遍再套 rounded+border+bg 卡片框，违反「去卡片框」 |
| 弹窗滥用 | 49 | 不合规：模态弹窗约 65+ 实例，破坏性确认约 16 处合规，其余 49 处按决议应迁工作区页（20）/左栏或内联（29，含 6 处 prompt 轻输入） |

---

## 一、C-1 禁原生 select

**自绘下拉统一入口（整改目标组件）**：`packages/nb-ui/src/components/form/FormSelect.vue`——基于 Reka Select 原语（SelectTrigger/SelectContent/SelectItem），弹出面板经 `.nb-ui-popover-surface`/`.nb-ui-menu-surface` 走主题变量（65% 磨砂纸面+边框+阴影+悬停高亮+选中对勾），即条款要求的合规自绘形态；可搜索场景用同目录 `Combobox.vue`（自绘浮层，Combobox.vue:10 注释自述「相比原生 select 的 FormSelect」）。

### 违反清单（23 处，全部为真实渲染的原生 `<select>`）

所有命中均套了触发器主题 class（边框/底色），但 option 弹出面板是浏览器原生渲染，无法走主题变量——不满足条款「弹出选项面板（纸面底+边框+浅影+悬停高亮+选中态）全走主题变量」。**整改建议统一为换 `FormSelect`，各条不再重复。**

主产品组件（19 处）：

| # | 位置 | 违反点 |
|---|---|---|
| 1 | `packages/neuro-book/app/components/novel-ide/plot/thread-panel/PlotThreadDetailPanel.vue:385` | 状态选择：`<select v-model="draft.status">` 内联 4 个 `<option>`（draft/active/written/revised） |
| 2 | `packages/neuro-book/app/components/novel-ide/plot/thread-panel/PlotThreadDetailPanel.vue:399` | 章节挂载选择：`<select v-model="draft.chapterId">` 遍历 props.chapters |
| 3 | `packages/neuro-book/app/components/novel-ide/ProjectPickerScreen.vue:820` | 会话恢复目标项目选择：`<select v-model="recoveryTargets[session.sessionId]">` |
| 4 | `packages/neuro-book/app/components/novel-ide/workspace/WorkspaceCreateFileDialog.vue:169` | Lorebook 类型选择：`<select v-model="lorebookType">` 遍历 LOREBOOK_ENTRY_TYPES |
| 5 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationBuilder.vue:103` | subject 选择：`<select :value="builder.subjectId">` |
| 6 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationBuilder.vue:106` | JSON Pointer path 快捷选择：`<select :value="builder.path">` |
| 7 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationBuilder.vue:114` | op 选择：`<select :value="builder.op">` |
| 8 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationBuilder.vue:118` | boolean value 选择：`<select v-else-if="builderValueMode === 'boolean'">` |
| 9 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationBuilder.vue:122` | enum value 选择：`<select v-else-if="builderValueMode === 'enum'">` |
| 10 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationBuilder.vue:125` | ref value 选择：`<select v-else-if="builderValueMode === 'ref' && refValueOptions.length">` |
| 11 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineMutationListControls.vue:25` | mutation 载入索引选择：`<select :value="mutationLoadIndex">` |
| 12 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineObjectValueEditor.vue:50` | object 字段 boolean 值：`<select v-else-if="objectFieldValueMode(row.key) === 'boolean'">` |
| 13 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineObjectValueEditor.vue:54` | object 字段 enum 值：`<select v-else-if="... === 'enum'">` |
| 14 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineObjectValueEditor.vue:57` | object 字段 ref 值：`<select v-else-if="... === 'ref' && ...length">` |
| 15 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEnginePreviewActions.vue:106` | Create Subject 的 type 选择：`<select v-model="subjectForm.type">` |
| 16 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEnginePreviewMutationBuilder.vue:76` | subject 选择：`<select :value="builder.subjectId">` |
| 17 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEnginePreviewMutationBuilder.vue:80` | path 选择：`<select :value="builder.path">` |
| 18 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEnginePreviewMutationBuilder.vue:87` | op 选择：`<select :value="builder.op">` |
| 19 | `packages/neuro-book/app/components/novel-ide/world-engine/WorldEngineSubjectCreator.vue:124` | Create Subject 的 type 选择：`<select v-model="form.type">` |

preview 路由页（4 处，定位说明见本节待人工复核第 1 条）：

| # | 位置 | 违反点 |
|---|---|---|
| 20 | `packages/neuro-book/app/pages/diff-workbench.preview.vue:176` | diff 语言覆盖：`<select v-model="languageOverride">` |
| 21 | `packages/neuro-book/app/pages/workflow.preview.vue:449` | Project Workspace 选择：`<select v-model="selectedProjectRoot">` |
| 22 | `packages/neuro-book/app/pages/workflow.preview.vue:456` | Workflow 默认模型：`<select v-model="selectedModelKey">` |
| 23 | `packages/neuro-book/app/pages/world-engine.preview.vue:1176` | 项目根选择：`<select v-model="selectedProjectRoot">` |

**排除项（grep 命中但不构成违反）**：`packages/neuro-book/app/utils/world-engine-workbench-preview.test.ts:342`、`:921`、`:1076`——防回归断言 `expect(...).not.toContain("<select")`，说明 world-engine workbench 主路径曾被整改并有测试守门，此处是守门代码非运行 UI；`packages/nb-ui/src/components/form/Combobox.vue:10`、`packages/nb-ui/src/components/components.test.ts:277`——注释/测试描述文字提及「原生 select」，非实际使用。

### 待人工复核

1. **preview 页是否纳入整改**：三个 preview 页位于 `pages/` 下，按 Nuxt 约定注册为真实可访问路由（如 `/workflow.preview`），故计为违反；但命名属内部调试台，整改优先级是否降级请人工裁定。
2. **`<datalist>` 边界**：`WorldEngineMutationBuilder.vue:111`、`WorldEnginePreviewMutationBuilder.vue:84` 用原生 `<datalist>` 给 path 输入框提供候选，弹出列表同为浏览器原生样式。条款原文只禁 `<select>` 未列 datalist，故不计违反；若要彻底统一主题弹出层，需人工决定是否扩条款。
3. **长列表取舍**：ProjectPickerScreen（novels 列表）与 world-engine.preview（项目列表）选项可能较长，换 FormSelect 时建议确认长列表滚动表现（FormSelect 已有 188px 视口+悬浮滚动条设计，从 FormSelect.vue 头注释读得，未实测）。

---

## 二、C-2 可按必带容器

### 违反清单（4 处）

1. **`packages/nb-ui/src/components/feedback/Notification.vue:53`**
   - 现状：`<button class="shrink-0 text-sm font-medium text-[var(--nb-notification-tone)] hover:underline cursor-pointer" @click="emit('action')">`——通知主操作按钮纯文字 + hover 下划线，无框无底色。
   - 违反点：操作按钮（触发 action 回调）用链接样式，无任何容器。
   - 整改：加 tone 色边框/底色小按钮（如同文件 56 行关闭按钮的 `hover:bg-[color-mix(...)]` 容器模式）。
2. **`packages/nb-ui/src/components/form/Listbox.vue:241-248`**
   - 现状：分组标题内「切换全选」按钮 `class="text-[10px] text-[var(--accent-main)] hover:underline cursor-pointer"`，点击 `handleGroupSelectAll(group)` 执行批量全选/反选。
   - 违反点：批量操作按钮纯文字 + hover 下划线，无容器。
   - 整改：改为带 `border rounded hover:bg` 的小尺寸按钮，保留 10px 字号。
3. **`packages/neuro-book/app/components/novel-ide/profile/NovelIdePassportProfilePanel.vue:461`**
   - 现状：设备码关联流程「取消」按钮 `class="text-xs text-[var(--text-muted)] hover:underline"`，点击 `cancelLink`；同区块 457/460 行按钮均有 border 容器，唯独此按钮裸文字。
   - 违反点：取消操作按钮纯文字下划线样式。
   - 整改：对齐同区块 460 行样式（`rounded-md border border-[var(--border-color)] px-3 py-1.5 hover:bg-[var(--bg-hover)]`）。
4. **`packages/neuro-book/app/components/novel-ide/profile/NovelIdePassportProfilePanel.vue:539`**
   - 现状：备份密钥行「导出恢复码」按钮 `class="inline-flex h-6 items-center gap-1 px-1.5 text-[var(--accent-text)] hover:underline"`，点击 `openKeyDialog('export', key.keyId)`；仅 padding 无边框底色。
   - 违反点：打开对话框的操作按钮为裸文字 + hover 下划线（同区块 526/529 行同类按钮均有 border）。
   - 整改：对齐 526 行样式（`h-6 + border rounded-md hover:bg-[var(--bg-hover)]`）。

### 待人工复核（2 处，跳转语义介于链接与操作之间）

- `packages/neuro-book/app/components/novel-ide/plot/planning/PlotPromiseDetailPane.vue:200`——节拍行场景标题按钮，平时纯文字、hover 才出下划线，点击 `emit('selectScene')` 跳转场景。功能为导航跳转（近辅助链接），但平时无可按视觉提示；同文件 PlotDecisionAdrDetail:145 的同类跳转给了完整容器，模式不一。
- `packages/neuro-book/app/components/novel-ide/plot/planning/PlotDecisionAdrDetail.vue:132`——`scene://` 引用按钮常显 `underline decoration-dotted`，点击跳转场景。dotted 下划线是引用惯例样式，但承担跳转操作且与 145-149 行「取代链」按钮（border+bg 容器）不一致。

**已核查排除（不违反，附依据）**：`Breadcrumb.vue:27-35`（a 标签 href 导航链接，hover:underline 属允许辅助链接）；`NovelIdePassportProfilePanel.vue:449/471`（a 标签外链 target=_blank，允许）；`Dialog.vue:376-412`（取消/确认按钮内联 style 带 border+background 胶囊容器）；`Pagination.vue:34,54-57`（非当前页码 hover:bg 容器反馈，当前页 accent 底色）；`DesktopTitleBar.vue:541-602`（OS 标题栏菜单惯例，hover:bg 容器反馈）；`JsonViewer.vue:267-274`（mode/icon 按钮 CSS 带 `border: 1px solid`）；`Button.vue:39-52` 各变体含 ghost（均有底色或 hover 底色 + 完整按钮形体，DatePicker/DateRangePicker 的 ghost 用法同）；`AlertDialog.vue:84-94`（经 Button 组件 secondary/primary/danger 变体）；`WorkspaceFileNode.vue:303-312`（span 上的 pending 状态点状下划线是状态装饰，非可点元素）；`NotificationViewport.vue:146`（富文本消息内容内 a 链接 underline，属内容链接）；`MarkdownSelectionMenu.vue:367`、`StructuredTextEditor.vue:119` 等（富文本编辑器 underline 命令/快捷键，非按钮样式）；`TipTapMarkdownEditor.vue`/`AgentMarkdownContent.vue` 的 underline CSS（编辑器内容区渲染样式，非操作按钮）。

---

## 三、C-3 选中线

**条款依据**：`docs/design/009-单D-UI总攻蓝图.md:106`——选中态指示线=左侧 2px 均匀线；禁单侧局部加粗；选中语义由线的颜色+文字强调色承担。

**合规基线（条款标准形态，已落地）**：

- `packages/neuro-book/app/components/novel-ide/agent/AgentModeSessionSidebar.vue:212`：选中=`shadow-[inset_2px_0_0_var(--accent-main)]`+accent 底色——左 2px 均匀线+颜色承担。
- `packages/neuro-book/app/components/novel-ide/agent/AgentUserInputPrompt.vue:259`：单选项选中同款左 2px inset 线。
- `packages/neuro-book/app/components/novel-ide/agent/AgentSessionTreeDialog.vue:552`+`:113-121`：行常驻 `border-l-2`，未选中 `border-l-transparent`、选中染 accent——左 2px+颜色切换。
- `packages/nb-ui/src/components/navigation/FileTree.vue:253`：选中纯 `bg-accent-bg text-accent-text`，无单侧线。

### 违反清单（7 处）

**A. 左侧选中指示线宽 3px、非 2px 均匀（宽度+形态违反）**

| # | 位置 | 违反点 | 整改建议 |
|---|---|---|---|
| 1 | `packages/neuro-book/app/components/novel-ide/settings/AgentProfileNavList.vue:79` | 默认设置入口选中态渲染 `absolute left-0 top-1/2 h-1/2 w-[3px] rounded-r-full bg-accent` 左指示条：3px≠2px 且仅半高+圆头，非「均匀线」，属单侧局部加粗 | 改为全高 `inset_2px_0_0` 左 2px 均匀线（同 AgentModeSessionSidebar 基线） |
| 2 | `packages/neuro-book/app/components/novel-ide/settings/AgentProfileNavList.vue:106` | Profile 列表项同款 3px 半高圆头左指示条（注释自称「激活状态左侧指示条」），违反点同上 | 同上 |
| 3 | `packages/neuro-book/app/components/novel-ide/settings/NovelIdeModelSettingsPanel.vue:478` | Provider 列表选中项同款 `w-[3px] h-1/2 rounded-r-full` 左指示条，违反点同上 | 同上 |
| 4 | `packages/neuro-book/app/components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewMutationEditor.vue:1565` | subject 列表选中（`activeSubjectId===subjectId`，aria-pressed）用 `shadow-[inset_3px_0_0_var(--we-accent)]` 左线：3px≠2px | inset 阴影改 `2px 0 0` |
| 5 | `packages/neuro-book/app/components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewSidebar.vue:396` | subject 多选选中（`selectedSubjectSet.has`）同款 `inset_3px_0_0` 左线：3px≠2px | 同上 |
| 6 | `packages/neuro-book/app/components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewSliceCard.vue:303` | 聚焦主题卡片（`focusedSubjectId===group.subjectId`）同款 `inset_3px_0_0` 左线：3px≠2px | 同上 |

**B. 选中语义靠底部粗线冒出（位置+粗细变化违反）**

| # | 位置 | 违反点 | 整改建议 |
|---|---|---|---|
| 7 | `packages/neuro-book/app/components/novel-ide/rag/NovelRagPanel.vue:494-496` | Events/Memory/Search 三 tab 未选中无任何底线，选中时冒出 `border-b-2 border-[var(--accent-main)]`，文字仅 `text-main` 非强调色：选中指示=底部单侧 2px 粗线出现（0→2px 粗细变化承担语义），线在底部非左侧、无占位还会顶动布局 | 文字改 accent 强调色+底色承担语义，底线改恒占位透明↔accent 切换（或按宪法改左 2px 线） |

### 待人工复核

**tab 底部 2px 指示线边界案例**——以下均为底部 2px、宽度合规、语义主要由线色+文字强调色承担（机制合规），但指示线位置不在「左侧」，tab 场景是否属 C-3 射程需人裁决（且 nb-ui 属「上游领地不许改」红线）：

- `packages/neuro-book/app/components/novel-ide/settings/NovelIdeModelEditDialog.vue:216`——恒 `border-b-2` 占位，选中 accent 色 vs transparent，文字 accent-text。
- `packages/neuro-book/app/components/novel-ide/settings/theme/ThemeEditorDialog.vue:316`——同占位式（`-mb-px border-b-2`）色切换。
- `packages/neuro-book/app/components/workflow-preview/WorkflowRunVisuals.vue:86-89`——同占位式色切换。
- `packages/neuro-book/app/components/novel-ide/plot/workbench/PlotWorkbenchDialog.vue:350`——选中渲染 `absolute bottom-0 h-[2px]` accent 底线，文字同步 accent-main。
- `packages/neuro-book/app/components/novel-ide/rag/NovelRagInspectorMain.vue:62`、`:67`——同上模式。
- `packages/nb-ui/src/components/controls/Tabs.vue:96`——基础组件库 Tabs 用 `after:bottom after:h-0.5`（=2px）底线，注释自称「选中项指示线」（Tabs.vue:85）；若判违宪触及 nb-ui 上游领地边界。

**状态/高亮类左线 3px（非严格「选中态」，按指示线统一管辖则超宽）**：

- `packages/neuro-book/app/components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewMutationEditor.vue:1479`——isCurrent「当前 mutation」上下文卡 warning 色 3px 左线。
- `packages/neuro-book/app/components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewMutationEditor.vue:1646`——isHighlightedMutation 变更行呼应高亮 warning 3px 左线。

**已排除候选（判读为非选中态，不违反）**：思考/工具/注入展开的常驻 `border-l-2` 缩进线（`AgentChatFlow.vue:527/571`、`AgentTextBubble.vue:407`、`AgentToolNode.vue:100` 等）属宪法第 5 条「展开层级缩进」合法形态；blockquote/警示/信息块左 3px 线（`TipTapMarkdownEditor.vue:1442/1561/1589`、`AgentMarkdownContent.vue:181`）为内容语义非选中；错误消息左线（`AgentTextBubble.vue:431`）为状态指示且本身左 2px；`AgentTraceDetail.vue:132` 的 tab 为 1px 边框融合式（无加粗）；`WorkspaceFileNode.vue:260-272` 的 2px/3px 线均为拖放指示非选中；`MarkdownSelectionMenu.vue:634` 底缘内阴影属圆形字母徽章装饰；`MarkdownSelectionMenu.vue:574`、`DiffWorkbench.vue:247` 激活态纯底色+文字色；nb-ui 其余组件（`SegmentedControl.vue:159`、`ToggleGroup.vue:64`、`Stepper.vue:62`、`RadioGroup.vue:66`、`Table.vue:99` 等）选中均靠底色/文字色，无单侧线。`border-l-4`/`border-r-N`/逻辑属性全库零命中。

---

## 四、C-4 底部栏一体成型

**适用界定**：条款对象是「输入区（编辑器/composer）与紧贴它的工具条」；对话框/弹层 footer（Dialog、Drawer、Listbox 操作栏、DatePicker 快捷条）与「输入区+工具条」结构不同，本次不计入确认违反，列入待复核。

**合规基线**：nb-ui `Button.vue:39-53`、`IconButton.vue:37-45` 全 variant 均有 hover 底块；`FormSelect.vue:125`、`Combobox.vue:139` 选项行基类均带 `hover:bg-[var(--bg-hover)]`（粗筛 hover:(text|opacity) 后逐个读原文排除的误报）；composables/utils/config 等 .ts 目录仅 `useDialog.ts` 注入对话框按钮 class（270-273 行，accent 主按钮 hover:opacity 属重按钮，不在条款「默认轻量」范围；272/273 行有 hover:bg），无底部栏相关违规。

### 违反清单（7 处）

| # | 位置 | 现状 | 违反点 | 整改建议 |
|---|---|---|---|---|
| 1 | `packages/neuro-book/app/components/novel-ide/NovelPromptBar.vue:330` | Inline AI 控制区（工具条）与上方 `ReferencePlainTextEditor borderless`（315-327 行，圆角已清零待拼接）之间有 `border-t border-[var(--border-color)] bg-[var(--bg-sidebar)]` | 输入区与工具条同容器但**实色分界线+背景色差**，且编辑器 borderless 拼接准备被这条线抵消，自相矛盾 | 去 border-t 与 bg-sidebar，改用 py 间距分层，工具条与输入区同底色无缝 |
| 2 | `packages/neuro-book/app/components/novel-ide/agent/AgentUserInputPrompt.vue:305` | 待输入面板 footer 工具条（终止/提交按钮）与上方输入、选项区之间 `border-t border-[var(--border-color)] bg-[var(--bg-panel)]/50` | 输入类面板的底部工具条与内容区**实色分界+背景差** | 去 border-t 与背景差，靠 pt 间距分层 |
| 3 | `packages/neuro-book/app/components/common/form/StructuredTextEditor.vue:331` | 编辑器工具栏（B/I 等格式钮+模式切换）与下方编辑区之间 `border-b border-[var(--border-color)] bg-[var(--bg-input)]/40` | 输入区与工具条同容器（319-327 行壳体）但**实色分界+背景色差**（工具栏在顶部，同属输入区/工具条关系） | 去 border-b 与 bg-input/40，与编辑区同底无缝 |
| 4 | `packages/neuro-book/app/components/common/form/TagInput.vue:49` | tag 删除按钮 `hover:text-[var(--status-danger)] hover:opacity-100`，无任何 hover 背景块 | 轻量图标按钮 hover 无底块 | 补 `rounded hover:bg-[var(--status-danger-bg)]`（对齐 NovelPromptBar.vue:300 同类按钮惯例） |
| 5 | `packages/nb-ui/src/components/form/TagInput.vue:87` | 同上写法 `hover:text-[var(--status-danger)] hover:opacity-100`，无 hover 背景 | 同 #4 | 同 #4 |
| 6 | `packages/neuro-book/app/components/common/form/Combobox.vue:120-122` | 输入框内 chevron 展开按钮 `p-1 text-[var(--text-muted)] hover:text-[var(--text-main)]`，无 hover 背景 | 输入区内的轻量按钮 hover 无底块（同文件 FormSelect 触发器 95 行有 `hover:bg-[var(--bg-hover)]`，惯例已存在） | 补 `rounded hover:bg-[var(--bg-hover)]` |
| 7 | `packages/neuro-book/app/components/novel-ide/agent/AgentComposer.vue:586` | composer 图片缩略图角标删除按钮 `hover:text-[var(--status-danger)]` + `group-hover:opacity-100`，无 hover 背景 | composer 工具条附属的轻量按钮 hover 无底块（同文件 601 行重试按钮有 `hover:bg-[var(--bg-hover)]`） | 补 `hover:bg-[var(--status-danger-bg)]` |

### 待人工复核

- `packages/neuro-book/app/components/novel-ide/agent/AgentComposer.vue:644`——主 composer 工具条与输入区之间残留 `border-t border-[var(--border-color)]/50` 半透明线：同容器✓，但「/50 弱分界」是否已达「去明显分界线」标准属设计裁量（009C1R3 三轮走查后存留，疑为有意妥协）。
- `packages/neuro-book/app/components/novel-ide/NovelPromptBar.vue:282`——引用条与输入区上沿之间实色 `border-b`（引用条偏状态展示，是否算「工具条」待定）。
- `packages/neuro-book/app/components/novel-ide/agent/AgentUserInputPrompt.vue:282`——note 输入区上缘 `border-t`（输入区与滚动内容区分界，非工具条）。
- `packages/neuro-book/app/components/novel-ide/agent/AgentComposer.vue:563`——可用性 banner 的 `border-b`（条件性状态条，语义性分隔）。
- `packages/neuro-book/app/components/markdown-studio/TipTapFrontmatterPanel.vue:196`——frontmatter 编辑器与上方操作行之间 `border-top: 1px solid`（编辑器附属面板内部）。
- `packages/neuro-book/app/components/novel-ide/agent/AgentWorkflowPendingPanel.vue:256`——`w-fit` 链接式文本按钮 hover 仅变色无底块（链接样式是否豁免「按钮」条款待定）。
- 弹层/对话框 footer 类 `border-t`：`packages/neuro-book/app/components/common/Dialog.vue:269`、`DialogWindow.vue:164`、`OriginalImagePreviewDialog.vue:50`、`packages/nb-ui/src/components/feedback/DialogWindow.vue:162`、`Drawer.vue:102`、`form/DatePicker.vue:96`、`DateRangePicker.vue:104`、`Listbox.vue:355`——均非「输入区+工具条」结构，按条款字面未计入；若宪法意图扩展到一切底部操作条则需重判。

**未验证项**：本节为静态代码判读，未运行浏览器做视觉确认；「明显分界」的视觉强度以类名语义（实色 vs /50 透明）推断。

---

## 五、C-5 展开层级缩进

**基线（符合处，作为判读参照）**：

- 工具过程行展开容器：`packages/neuro-book/app/components/novel-ide/agent/AgentToolNode.vue:100`——`ml-3 border-l-2 border-[var(--border-color)]/50 pl-3`，12px 左缩进+左竖线，收起/展开头部平齐（83、92 行），容器无卡片框。✓
- 注入收拢行展开容器：`packages/neuro-book/app/components/novel-ide/agent/AgentChatFlow.vue:527`、`AgentChatFlow.vue:571`——同款 `ml-3 border-l-2 pl-3`。✓

### 违反清单（6 处）

以上述基线为基准，工具展开内容（block 组件与 fallback，均渲染在 AgentToolNode.vue:102 的竖线容器内）普遍再套 `rounded+border+bg` 卡片框，违反「去卡片框」：

1. **`packages/neuro-book/app/components/novel-ide/agent/AgentToolNode.vue:111`（及 119）**
   - 违反点：Arguments/Result 的纯文本 fallback 是 `rounded border bg-[var(--bg-main)] p-2` 卡片块——展开内容套卡片框。
   - 整改：去 rounded/border，改为 `bg-[var(--bg-subtle)]` 或裸 pre 承接代码内容。
2. **`packages/neuro-book/app/components/novel-ide/agent/AgentWriteFileBubble.vue:44`**
   - 违反点：Content Preview 主体包在 `rounded-xl border bg-[var(--bg-main)]/50 p-4` 大卡片里——展开主体即卡片框（且 rounded-xl 最重）。
   - 整改：去掉容器框，内容直接流入竖线缩进区，保留 `bg-subtle` 底色即可。
3. **`packages/neuro-book/app/components/novel-ide/agent/AgentEditFileBubble.vue:61`、68、77、87**
   - 违反点：Old/New 双栏、Diff Preview、Result 共四层 `rounded border` 卡片。
   - 整改：Old/New 改纯底色分栏（红/绿 bg 不变），去边框圆角。
4. **`packages/neuro-book/app/components/novel-ide/agent/AgentApplyPatchBubble.vue:65`、71**
   - 违反点：Patch Preview 与 Applied Diff 均为带标题条的 `rounded border` 卡片。
   - 整改：标题行+裸内容流，去框。
5. **`packages/neuro-book/app/components/novel-ide/agent/AgentWorkflowBubble.vue:325`、372、402、456、480**
   - 违反点：摘要卡、后台 job 卡、可视化折叠卡、metadata/result 折叠卡共五个 `rounded-lg border p-3` 卡片堆叠（347 行注释自称「按钮式折叠卡片」）——工具展开内容整体卡片化，与竖线缩进双轨呈现；内部折叠（348、403 行）展开仅 `border-t` 无缩进竖线。
   - 整改：外层卡片改标题行+竖线缩进分区，内部折叠段统一挂左竖线。
6. **`packages/neuro-book/app/components/novel-ide/agent/AgentRequestUserInputBubble.vue:77`、109**
   - 违反点：Question 卡（`rounded-lg border bg p-3`）与 Answer/Decision 卡（109-110 行按状态上色加边框）同上套卡片框。
   - 整改：Question 主体去框，用大标题+缩进竖线；Answer 保留状态底色、去边框圆角（91 行选项小卡属内容元素，不在本条）。

注：`AgentToolNode.vue:78` 既有注释「展开才成卡片细节」表明卡片形态是 009C1R 的旧设计意图，与宪法 C-5「去卡片框」方向相反，故按宪法判违反。

### 待人工复核（4 项）

- `packages/neuro-book/app/components/novel-ide/agent/AgentTextBubble.vue:390`、407——思考区外层 `pl-4`（收起行整体缩进 16px，与工具/注入收起行「平齐」不一致）；展开容器 407 行 `border-l-2 pl-4` 无 `ml-*`，竖线挂在收起行原位而非缩进 12px 后。字面上「12-16px 缩进+竖线」勉强可算满足（pl-4=16px），但与 AgentToolNode/AgentChatFlow 的 `ml-3+竖线+pl-3` 形态不统一，「收起平齐」参照系存疑。若严格执行「统一」需改造成 ml-3/4+border-l+pl 形态。
- `packages/neuro-book/app/components/novel-ide/agent/AgentWorkspaceChanges.vue:220`、251——Composer 文件变更摘要：整体 `rounded-lg border` 卡片，展开体（251 行 `workspace-body-shell`→253 行 `border-t`）无左缩进无竖线。它是输入区面板而非会话流过程行，条款是否覆盖需人定。
- `packages/neuro-book/app/components/novel-ide/agent/trace-viewer/AgentTraceDetail.vue:165`——trace 查看器 thinking 块 `border-l-2 pl-2`（8px<12px 且无容器缩进）。trace 是独立诊断面板非会话过程行，条款是否覆盖需人定。
- 各工具组件的 error/警告框（如 `AgentToolNode.vue:125`、`AgentWorkflowBubble.vue:364`）——`rounded+border` 但带 status-danger/warning 语义色，可能是有意保留的状态提示样式，判违反会误伤。

**未覆盖说明**：`packages/nb-ui/` 零命中：其 Collapsible（`packages/nb-ui/src/components/layout/Collapsible.vue:24-40`）是 reka-ui 纯行为包装，不承载思考/工具/注入场景样式；Accordion/Tree 等为通用导航组件，不属 C-5 条款场景。pages/ 与 composables/ 中无会话过程行展开实现（`useCollapsible.ts` 为纯状态逻辑，无样式）。

---

## 六、弹窗滥用

**基础设施（判读基准）**：

- `packages/neuro-book/app/components/common/Dialog.vue:237-242`——模态弹窗（teleport + 遮罩 + Esc），业务弹窗统一底座。
- `packages/nb-ui/src/components/feedback/Dialog.vue:293-317`——同上，nb-ui 侧模态底座；`DialogWindow.vue:8-13` 为**非模态**浮动窗（无遮罩可拖动）。
- `packages/neuro-book/app/composables/useDialog.ts:380-438`——JS API：alert/confirm/prompt/choose/chooseCards，全部走模态 Dialog。
- `app/components/common/DialogWindow.vue:5-11` 亦为非模态浮动窗（实读确认）。

**非模态浮动窗对照（不属于本次滥用）**：AgentContextInspectorDialog（`agent/context-inspector/AgentContextInspectorDialog.vue:13` 注释明说选非模态因用户要边看边发消息）、AgentJobsDialog（`jobs/AgentJobsDialog.vue:7`）、ThemeEditorDialog（`settings/theme/ThemeEditorDialog.vue:4`）。AgentWorkflowPendingPanel 注释写「弹居中 Dialog」（`agent/AgentWorkflowPendingPanel.vue:252`）但实现已是内联展开面板（264-267 行），注释过时，实际合规。

### A. 非破坏性·重内容 → 按决议迁工作区页（20 处）

| 位置 | 用途 | 迁移建议 |
|---|---|---|
| `novel-ide/NovelIdeSettingsDialog.vue:771`（size=full，含 10 个 section 分栏） | 全局设置 | 决议点名：改 VS Code 式工作区页 |
| `novel-ide/NovelIdeProfileDialog.vue:18`（size=xl） | 账号/云备份 | 并入设置工作区页一个 section |
| `plot/workbench/PlotWorkbenchDialog.vue:300`（size=full） | 情节工作台 | 工作区页（IDE 主区内 tab） |
| `world-engine/WorldEngineWorkbenchDialog.vue:1834`（size=full） | 世界引擎工作台 | 同上 |
| `rag/NovelRagInspectorDialog.vue:296`（size=full） | RAG 检查器 | 同上 |
| `profile-template-editor/UserProfileWorkbenchDialog.vue:15`（size=full） | Profile 编辑工作台 | 同上 |
| `agent/AgentSessionTreeDialog.vue:487`（1560px） | 会话树浏览 | 工作区页 |
| `agent/AgentSessionDialog.vue:211`（1040px） | 会话列表/归档/恢复 | 工作区页或左栏面板 |
| `history/WorkspaceHistoryInboxDialog.vue:174`（size=xl，左列表右 diff） | 文件历史收件箱 | 工作区页 |
| `agent/trace-viewer/AgentTraceViewerDialog.vue:189`（size=xl） | LLM trace 查看器 | 工作区页 |
| `workspace/WorkspaceCharacterDetailPanel.vue:423`（1280px） | 角色档案 | 工作区页打开 |
| `profile-template-editor/ProfileTemplatePreviewDialog.vue:49`（1380px） | Prompt 预览调试 | 工作区页 |
| `common/OriginalImagePreviewDialog.vue:36`（size=xl） | 原图预览 | 工作区页（VS Code 图片即标签页打开）；被 `AgentAttachmentCard.vue:103`、`AgentSessionAttachmentPanel.vue:106` 复用 |
| `workspace/WorkspaceLocationProfileDialog.vue:202` | 地点卡编辑 | 重表单，工作区页打开 |
| `workspace/WorkspaceRuleProfileDialog.vue:196` | 规则卡编辑 | 同上 |
| `settings/ModelDiscoveryDialog.vue:33`（800px） | 模型发现/手加 | 并入设置页模型 section |
| `settings/ModelLibraryDialog.vue:25`（800px） | 模型库启停 | 同上 |
| `settings/NovelIdeModelEditDialog.vue:181` | 模型编辑（4 tab） | 同上 |
| `ai/FormAnnotationDialog.vue:87`（760px） | AI 表单批注 | 跟随宿主表单内联 |
| `common/diff/DiffWorkbenchDialog.vue:96`（92vw×82vh） | diff 工作台 | 冲突场景（见 C 类）合规；同步警告 diff（`NovelIdeToolPanel.vue:471`）等工作区页打开 |

### B. 非破坏性·轻内容 → 左栏展开或内联（29 处，含 6 处 prompt 轻输入）

| 位置 | 用途 | 迁移建议 |
|---|---|---|
| `plot/chapter-panel/PlotChapterEditorDialog.vue:166` | 章节编辑表单 | 左栏/详情区展开 |
| `plot/planning/PlotDecisionEditorDialog.vue:322` | 决策编辑 | 同上 |
| `plot/planning/PlotDecisionDecideDialog.vue:98` | 决策拍板（三段+候选） | 同上 |
| `plot/planning/PlotPromiseEditorDialog.vue:152` | 承诺编辑 | 同上 |
| `plot/planning/PlotPromiseBeatDialog.vue:128` | 节拍打点 | 同上 |
| `plot/thread-panel/PlotThreadEditorDialog.vue:440` | 线/场编辑 | 同上 |
| `plot/NovelPlotPanel.vue:1909` | 新建卷 | 内联小表单 |
| `rag/NovelRagPanel.vue:577` / `:585` | 新增/编辑 Event、Memory | 左栏内联 |
| `pages/admin/users.vue:278` / `:308` / `:328` | 建用户/编辑/重置密码 | 管理页内联抽屉 |
| `profile-template-editor/ProfileTemplateVisualEditor.vue:2370` | 新建 TSX Profile | 页内表单 |
| `common/low-code-form/LowCodeResourcePresetField.vue:388` | 资源新建/重命名 | 内联输入 |
| `workspace/WorkspaceCreateFileDialog.vue:129` | 新建文件/目录/lorebook | 左栏内联输入行（VS Code 式） |
| `novel-ide/ProjectPickerScreen.vue:839` | 项目封面设置 | 选择器卡片内直接换图 |
| `common/LucideIconPickerDialog.vue:43` | 图标选择器 | popover 内联于表单字段 |
| `novel-ide/NovelIdeToolPanel.vue:443` | 下载确认（非破坏） | 直接执行+toast |
| `novel-ide/NovelIdeToolPanel.vue:448` | 同步警告列表 | 左栏面板 |
| `settings/NovelIdeModelSettingsPanel.vue:612` | 模型配置问题列表 | 设置区内联展开 |
| `markdown-studio/MarkdownStudioTutorialAgentDialog.vue:35` | 教程占位 | 非模态侧栏/首访引导 |
| `pages/index.vue:302` | alert「关于 NeuroBook」 | 工作区页关于区 |
| `pages/index.vue:1256` | 单按钮 choose「正在保存请等待」 | toast 通知 |
| `useDialog().prompt` 轻输入：`workspace/WorkspaceFilePanel.vue:226`（重命名）、`agent/AgentChatSurface.vue:3437`（会话重命名）、`markdown-studio/MarkdownSelectionMenu.vue:38`、`TipTapMarkdownEditor.vue:84`、`workspace/WorkspaceCharacterPanel.vue:13`、`pages/index.vue:2204`（新建章节等） | 命名/路径输入 | 就地内联编辑（重命名）或左栏输入行 |

### C. 破坏性/安全确认（合规，保留模态）

- **删除类**：`plot/NovelPlotPanel.vue:1939`、`rag/NovelRagPanel.vue:593`、`settings/NovelIdeModelSettingsPanel.vue:664`（删 Provider）、`NovelIdeSettingsDialog.vue:1305`（删主题）、`LowCodeResourcePresetField.vue:414`、`WorkspaceFilePanel.vue:246,253`（含递归删除二段确认）。
- **不可逆/安全操作**：`workspace/WorkspaceFileConflictDialog.vue`（写冲突，丢弃一方内容）、`profile/BackupRecoveryCodeDialog.vue:239`（恢复码/密钥轮换，含丢失警告 241-245）、`plot/planning/PlotDecisionLedgerTab.vue:631`（作废决策，强制原因）、`rag/NovelRagInspectorDialog.vue:385`（RAG debug 操作）、`pages/index.vue:1201,1231,1264`（关 tab/切项目放弃草稿）、`AgentChatSurface.vue:1602,3324,3483`（弃草稿/分支/重发）。

**小结**：模态弹窗总计约 65+ 实例，其中破坏性确认约 16 处合规；其余 49 处承载设置、工作台、查看器、表单与轻输入，按决议应分别迁工作区页（20）/左栏或内联（29，含 6 处 prompt 轻输入）。最大 offender 是 `NovelIdeSettingsDialog.vue:771`（决议直接点名）与四个 size=full 工作台弹窗。

### 待人工复核（本节为编辑从材料裁定性表述归纳，材料原文未设此小节）

- `DiffWorkbenchDialog.vue:96` 双场景归类：冲突场景判合规、同步警告 diff（`NovelIdeToolPanel.vue:471`）判迁移——同一组件两用，拆分方案需人工定。
- C 类「合规保留模态」清单（约 16 处）为扫描员判读，破坏性/安全性边界（如 `NovelRagInspectorDialog.vue:385` 的 RAG debug 操作）未经人工复核。
- `AgentWorkflowPendingPanel.vue:252` 注释过时（自称弹居中 Dialog，实现已是内联面板 264-267 行）——注释清理属随手项，随整改处理。
- 迁移目标（工作区页/左栏 tab）依赖单D 三态状态机方案落地，A/B 类 49 处的最终挂点需与状态机设计对表后再排施工。

---

## 遗留与待复核（文末总汇）

1. **C-1**：preview 页（`diff-workbench.preview.vue:176`、`workflow.preview.vue:449,456`、`world-engine.preview.vue:1176`）是否降级整改优先级，待人工裁定。
2. **C-1**：`<datalist>`（`WorldEngineMutationBuilder.vue:111`、`WorldEnginePreviewMutationBuilder.vue:84`）是否扩条款纳入，待人工决定。
3. **C-1**：FormSelect 长列表滚动表现未实测（188px 视口+悬浮滚动条为头注释自述），ProjectPickerScreen 与 world-engine.preview 换用时需验证。
4. **C-2**：`PlotPromiseDetailPane.vue:200`、`PlotDecisionAdrDetail.vue:132` 两处跳转语义按钮（链接 vs 操作）归属待人裁。
5. **C-3**：tab 底部 2px 指示线 6 处（含 `nb-ui Tabs.vue:96` 上游领地边界）是否属条款射程，待人裁。
6. **C-3**：状态/高亮类 3px 左线 2 处（`WorldEngineWorkbenchPreviewMutationEditor.vue:1479,1646`）是否按指示线统一管辖，待人裁。
7. **C-4**：`AgentComposer.vue:644` 的 /50 半透明线是否达「去明显分界」标准（疑为 009C1R3 有意妥协），属设计裁量。
8. **C-4**：弹层/对话框 footer 类 border-t 8 处（Dialog/DialogWindow/Drawer/DatePicker/DateRangePicker/Listbox）是否扩展条款重判，待人裁。
9. **C-5**：`AgentTextBubble.vue:390,407` 思考区缩进形态与其他过程行不统一，「收起平齐」参照系存疑。
10. **C-5**：`AgentWorkspaceChanges.vue:220,251`（输入区面板）、`AgentTraceDetail.vue:165`（诊断面板）、工具 error/警告框（`AgentToolNode.vue:125`、`AgentWorkflowBubble.vue:364`）是否覆盖/误伤，待人裁。
11. **弹窗**：DiffWorkbenchDialog 双场景拆分、C 类合规边界、A/B 类 49 处与三态状态机挂点对表，均待人工复核（见第六节待复核小节）。
12. **整体**：六份扫描均为静态代码判读（C-4 材料明确注明未运行浏览器视觉确认；「明显分界」视觉强度以类名语义推断），整改后需真机验收。
