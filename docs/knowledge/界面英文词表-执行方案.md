# 界面英文词表·执行方案

> **基线**：worktree `D:/MyProject/worktrees/task-prefab` 分支 `task-prefab` @ `2ae879d9`（2026-09-22 核对）。
> **输入**：`docs/knowledge/界面英文词表.md`（139 条，2026-09-20 快照）+ `packages/neuro-book/app/i18n/locales/zh-CN.ts`（2782 行）与 `en-US.ts`（2781 行）现状逐键核对 + 词表点名的全部 `组件:行号` 逐行回读（脚本扫描，证据见文末）。
> **与词表的重大差异**：词表所称"58 条假中文"（i18n 键 zh 值仍英文）在本基线**已基本修复**——脚本全量扫描 zh-CN.ts 仅剩 8 条纯 ASCII 值，其中与词表相关的只有 `settings.frontend.english: "English"`（语言名，按口径保留）。其余已修键在下表标 **已完成**，不再重复施工。
> **剩余工作量集中在**：组件模板硬编码英文（约 40 处）与少量 i18n 混排残留。
>
> 状态图例：⛏ 待执行 · ✅ 已完成（无需动作）· 🔒 保留（口径决定不译）· ⚠ 改动会挂测试断言，须同步改断言。

## 通用口径（继承词表，执行时照此取词）

Agent→AI 助手；RAG→检索增强（RAG）；Embedding→向量嵌入（Embedding）；POV→视点（POV）；API/Token/JSON/YAML/Markdown/Schema 保留英文；Thread→剧情线；Scene→场景；Session→会话；Profile→配置档；Workflow→工作流。

---

## 一、全局框架

| # | 键/文本 | 现值（英文） | 应改值 | 位置 | 状态 |
|---|---|---|---|---|---|
| 1 | aria-label | `Application menu` | 应用菜单 | `packages/neuro-book/app/components/DesktopTitleBar.vue:337` | ⛏ |
| 2 | aria-label ×3 | `Minimize` / `Maximize` / `Close` | 最小化 / 最大化 / 关闭 | `DesktopTitleBar.vue:436-438` | ⛏ |
| 3 | 模板文本节点 | `Studio` | 创作工作室 | `packages/neuro-book/app/pages/index.vue:2583` | ⛏ |
| 4 | 头像首字母 | `U`（账号名首字，数据渲染） | 取账号名首字 | 运行时数据，非硬编码 | 🔒 待人工确认① |
| 5 | Dialog title | `Workflow 待处理` | 工作流待处理 | `packages/neuro-book/app/components/novel-ide/agent/AgentWorkflowPendingPanel.vue:262` | ⛏ |

## 二、工作台（Markdown Studio）

| # | 键/文本 | 现值（英文） | 应改值 | 位置 | 状态 |
|---|---|---|---|---|---|
| 6 | 模板文本节点 | `MARKDOWN STUDIO` | 写作台（或保留作产品化名，二选一定死） | `packages/neuro-book/app/components/markdown-studio/MarkdownStudioWelcome.vue:149` | ⛏ |
| 7 | 欢迎页空态 | "先进入 manuscript…" 混排 | 现已为中文（`markdownStudio` 域，`zh-CN.ts:2735`），manuscript 目录名保留 | `zh-CN.ts:2735` | ✅ |
| 8 | i18n 键 `ide.workspace.fileTree.newMarkdown` 等 | "新建 Markdown" | 已中文，Markdown 保留 | `zh-CN.ts:1128`、`zh-CN.ts:2709` | ✅ |
| 9 | i18n 键 `settings.*` 打开/关闭 Agent 面板 | "打开 Agent 面板" | 建议统一为"打开 AI 助手面板"（低优先，随口径批次） | `zh-CN.ts:932-933` | ⛏（口径统一） |

## 三、角色档案面板

| # | 键/文本 | 现值（英文） | 应改值 | 位置 | 状态 |
|---|---|---|---|---|---|
| 10 | 角色列表状态徽标 `DRAFT` | 原直显 frontmatter 原值 | 现走 `getWorkspaceLorebookStatusLabel()` →"草稿中/待定/已生效/已归档" | `packages/neuro-book/app/components/novel-ide/workspace/WorkspaceCharacterPanel.vue:281`、`workspace-entry-meta.ts:118-129` | ✅ |
| 11 | 文件树状态圆点 title | `:title="node.status"` 直显 `draft/pending` 原值 | 改走状态翻译（同第 10 条的 label 函数） | `packages/neuro-book/app/components/novel-ide/workspace/WorkspaceFileNode.vue:326` | ⛏（词表"待人工确认②"残留） |
| 12 | frontmatter 字段 residence/occupation/identity/origin/appearance | 数据字段名 | 界面占位符已走 `t("ide.workspace.character.*")`，字段名保留不直显 | `WorkspaceCharacterDetailPanel.vue:540-543`（界面）、`:288-292`（数据） | ✅ |

## 四、Agent 会话面板

i18n 假中文（✅ 已完成，键现值核对）：

| # | 键 | 现值（zh） | 词表建议 | 位置 |
|---|---|---|---|---|
| 13 | `agent.chatSurface.sessionTreeTitle` | "会话树" | 会话树 | `zh-CN.ts:2151` |
| 14 | `agent.sessionTree.title` | "会话树" | 会话树 | `zh-CN.ts:2268` |
| 15 | `agent.session.sidebarTitle` | "AI 助手会话" | 同 | `zh-CN.ts:2027` |
| 16 | `agent.session.active` / `.profile` / `.leader` | "活跃" / "配置档" / "总指挥" | 同 | `zh-CN.ts:2036` / `:2045` / `:2053` |
| 17 | `agent.systemPrompt.title` | "系统提示词" | 同 | `zh-CN.ts:1949` |
| 18 | `ide.agentJobs.refRunId` | "运行 ID" | 同 | `zh-CN.ts:1098` |
| 19 | `ide.workspace.fileDetail.current/total/size/chapters/files/validate` | "当前/总计/大小/章节数/文件数/校验" | 同 | `zh-CN.ts:1275-1285` |
| 20 | `agent.contextInspector.segment.system/tools/historySet/modelContext/appending` | "系统提示/工具/历史集/模型上下文/追加集" | 同 | `zh-CN.ts:2205-2212` |

硬编码残留（⛏ 待执行）：

| # | 文本 | 现值 | 应改值 | 位置 |
|---|---|---|---|---|
| 21 | 徽标 | `LEAF` | 叶节点 | `packages/neuro-book/app/components/novel-ide/agent/AgentSessionTreeDialog.vue:620` |
| 22 | 详情字段 ×5 | `Entry ID` / `Parent` / `Type` / `Children` / `Status` | 条目 ID / 父节点 / 类型 / 子节点 / 状态 | `AgentSessionTreeDialog.vue:633,637,641,645,649` |
| 23 | 卡片头 ×2 | `Patch` / `Applied Diff` | 补丁 / 已应用差异 | `AgentApplyPatchBubble.vue:66,72` |
| 24 | 卡片头 ×3 | `Old String` / `New String` / `Diff Preview` | 原文 / 新文 / 差异预览 | `AgentEditFileBubble.vue:62,69,78` |
| 25 | 卡片头 ×3 | `Arguments` / `Result` / `Error` | 参数 / 结果 / 错误 | `AgentToolNode.vue:119,127,134` |
| 26 | 模式标签 | `overwrite` | 覆盖 | `AgentWriteFileBubble.vue:40` |
| 27 | 参数徽标 | `wait`（`parsedArgs.wait`） | 保留（参数原文） | `AgentWorkflowBubble.vue:333` |
| 28 | 字段名 | `keyId` | 密钥 ID | `BackupRecoveryCodeDialog.vue:250` |

i18n 混排残留（⛏ 待执行，Session→会话）：

| # | 键 | 现值 | 应改值 | 位置 |
|---|---|---|---|---|
| 29 | `agent.modelPanel.selectSessionModel` | "选择 Session 模型" | 选择会话模型 | `zh-CN.ts:1886` |
| 30 | `agent.modelPanel.sessionModelParams` | "当前 Session 模型参数" | 当前会话模型参数 | `zh-CN.ts:1887` |
| 31 | `agent.sessionTree.close` | "关闭 Session Tree" | 关闭会话树 | `zh-CN.ts:2270` |
| 32 | `agent.sessionTree.sessionListTitle` | "Session 列表" | 会话列表 | `zh-CN.ts:2152` |
| 33 | `agent.systemPrompt.open/close/refresh/loading/empty` ×5 | "查看 System Prompt" 等 | System Prompt→系统提示词 | `zh-CN.ts:1950-1955` |

## 五、剧情面板（plot 系，硬编码重灾区，⛏ 除注明外全部待执行）

| # | 文本 | 应改值 | 位置 |
|---|---|---|---|
| 34 | `Inspector` | 检视器 | `packages/neuro-book/app/components/novel-ide/plot/PlotInspector.vue:13`；`plot/PlotTreePreviewWorkspace.vue:124` |
| 35 | `Meta` / `Writing Tip` / `Refs` | 元信息 / 写作提示 / 引用 | `PlotInspector.vue:25,38,44` |
| 36 | `Refs`（线程编辑器、定位器） | 引用 | `plot/thread-panel/PlotThreadEditorDialog.vue:575`；`plot/PlotLocatorView.vue:186` |
| 37 | `Chapters` / `Chapter Rhythm` | 章节 / 章节节奏 | `plot/PlotChapterView.vue:50,76` |
| 38 | `Thread` | 剧情线 | `plot/PlotTreeView.vue:71` |
| 39 | `Main Thread` / `Main` / `Mainline Root` | 主线 / 主线 / 主线根 | `PlotTreeView.vue:75`；`plot/PlotThreadGroupNode.vue:62`；`plot/PlotTreeRootNode.vue:14` |
| 40 | `Plot Timeline` / `Thread` / `Scene` / `Chapter` / `Draft Tail` | 剧情时间线 / 剧情线 / 场景 / 章节 / 草稿尾部 | `plot/PlotTimelineView.vue:37,46,50,54,58` |
| 41 | `Scenes` | 场景列表 | `plot/PlotWorkbenchSidebar.vue:298` |
| 42 | `Pinned` | 已固定 | `PlotWorkbenchSidebar.vue:205` |
| 43 | `Scene Group` | 场景组 | `PlotThreadGroupNode.vue:96` |
| 44 | `THREAD` | 剧情线 | `plot/PlotThreadScenePanel.vue:197` |
| 45 | `Story / Phase` | 故事 / 阶段 | `PlotLocatorView.vue:82` |
| 46 | `StoryPhase` | 故事阶段 | `plot/PlotTimelineCanvas.vue:37` |
| 47 | `Threads` ×2 | 剧情线列表 | `PlotLocatorView.vue:126`；`plot/PlotThreadView.vue:57` |
| 48 | `Purpose` / `Scene Stats` | 目的 / 场景统计 | `PlotThreadView.vue:145,149` |
| 49 | `Thread / Scene / World Anchor` | 剧情线 / 场景 / 世界锚点 | `NovelPlotPanel.vue:1731` |
| 50 | `REF ATTRIBUTES` | 引用属性 | `plot/RefEditorPopover.vue:126` |
| 51 | `Plot Workspace Preview` / `Phase` / `Thread` / `Scene` | 预览页头（低优先级，随"预览页移出视野"一并处理） | `plot/PlotWorkspacePreview.vue:341,350,354,358` |
| 52 | `YAML Working Draft` | YAML 工作草稿 | `plot/FormAnnotationDialog.vue:127`（YAML 保留） |

## 六、世界引擎（World Engine 系）

| # | 文本 | 应改值 | 位置 | 状态 |
|---|---|---|---|---|
| 53 | `ide.header.worldEngine` | "世界引擎" | `zh-CN.ts:922` | ✅ |
| 54 | `World Engine`（检视器头） | 世界引擎 | `plot/workbench/PlotWorkbenchInspector.vue:402` | ⛏ |
| 55 | `World Engine Workbench` | 世界引擎工作台 | `world-engine/WorldEngineWorkbenchDialog.vue:1850` | ⛏ ⚠ |
| 56 | `Mutation Builder` ×2（正式版+Preview 版） | 变更构建器 | `WorldEngineMutationBuilder.vue:89` | ⛏ ⚠（测试两处断言） |
| 57 | `Create Subject` | 创建主体 | `WorldEngineSubjectCreator.vue:118` | ⛏ ⚠（测试两处断言） |
| 58 | `Actions` / `Query` | 操作 / 查询 | `world-engine/WorldEnginePreviewActions.vue:96,176` | ⛏ |
| 59 | `Edit Timeline` | 编辑时间线 | `WorldEngineMutationEditorHeader.vue:21` | ⛏ ⚠ |
| 60 | `World State` / `Subjects` / `State Query` | 世界状态 / 主体 / 状态查询 | `WorldEnginePreviewStatePanel.vue:77,117,179` | ⛏ ⚠（World State、State Query 有断言） |
| 61 | `Schema Shortcuts` | 模板速捷（Schema 保留口径→建议"Schema 速捷"） | `WorldEngineMutationEditor.vue:1050` | ⛏ |
| 62 | `Selected Slice` | 已选切片 | `WorldEngineSliceInspector.vue:37` | ⛏ |
| 63 | `Slices` / `Subject States` | 切片列表 / 主体状态 | `plot/workbench/WorldEngineContextPanel.vue:113,128` | ⛏ |
| 64 | `Project` / `Schema`（Preview 面板头） | 项目 / Schema（保留） | `WorldEnginePreviewProjectPanel.vue:56,78` | ⛏ |
| 65 | `true/false/undefined` 选项 | 字面量 | `WorldEngineObjectValueEditor.vue:51` | 🔒 |
| 66 | `Subject file proposals` | 主体文件提案 | `WorldEngineWorkbenchPreviewInspector.vue:435` | ⛏ ⚠ |
| 67 | 草稿标签 `events.jsonl draft` / `memory facts` / `state.md review` 等 | 开发者向草稿标签 | `workbench-preview/WorldEngineWorkbenchPreviewInspector.vue:466,497,526` 等 | ⛏（低优先，词表口径"暂缓"） |

## 七、配置中心

i18n 假中文（✅ 已完成，键现值核对）：

| # | 键 | 现值（zh） | 位置 |
|---|---|---|---|
| 68 | `settings.section.embedding.label` | "向量嵌入（Embedding）" | `zh-CN.ts:204-206` |
| 69 | `settings.scope.global.description` | "工作区根目录 .nbook/config.json" | `zh-CN.ts:171-173` |
| 70 | `settings.panels.embedding.baseUrl` / `.apiKey` | "接口地址（Base URL）" / "密钥（API Key）" | `zh-CN.ts:499-500` |
| 71 | `settings.panels.web.keyConfigured` / `.noKey` / `.timeoutMs` | "已配密钥" / "未配密钥" / "超时（毫秒）" | `zh-CN.ts:523-525,532` |
| 72 | `settings.editor.tabSizeTitle` | "缩进宽度" | `zh-CN.ts:418` |
| 73 | `settings.version.commit` / `.release` | "提交 {version}" / "发行版 {version}" | `zh-CN.ts:438-439` |
| 74 | `settings.panels.modelEdit.costInput/Output/CacheRead/CacheWrite` | 输入/输出/缓存读/缓存写（费用字段已走 i18n） | `NovelIdeModelEditDialog.vue:93-96`（模板侧） |

硬编码残留（⛏ 待执行）：

| # | 文本 | 应改值 | 位置 | 状态 |
|---|---|---|---|---|
| 75 | `Providers` | 服务商列表 | `settings/NovelIdeModelSettingsPanel.vue:460` | ⛏ |
| 76 | `API Base` | 接口地址 | `NovelIdeModelSettingsPanel.vue:543` | ⛏ |
| 77 | `Max Tokens` | 最大 tokens | `settings/NovelIdeModelEditDialog.vue:283` | ⛏ |
| 78 | `USD / 1M tokens` | 美元 / 百万 tokens（或保留，随费用字段口径） | `NovelIdeModelEditDialog.vue:342` | ⛏ |
| 79 | `TopK` | 保留（采样参数名） | `settings/AgentProfileModelFields.vue:168` | 🔒 |
| 80 | `Agent Profiles` | AI 助手配置档 | `settings/AgentProfileNavList.vue:91` | ⛏ |
| 81 | `Custom Provider` | 自定义服务商（provider 模板 name 字段，界面下拉直显） | `settings/useProviderTemplateSession.ts:18` | ⛏ |
| 82 | `settings.frontend.english` = "English" | 语言名保留 | `zh-CN.ts:260` | 🔒 |
| 83 | `ide.header.ragInspector` | "检索检视器（RAG）" | `zh-CN.ts:927` | ✅ |

## 八、RAG / 记忆面板

| # | 文本 | 应改值 | 位置 | 状态 |
|---|---|---|---|---|
| 84 | `Subjects` | 主体 | `rag/NovelRagPanel.vue:441` | ⛏ |
| 85 | `Events` / `Memory` / `Search`（页签 ×3） | 事件 / 记忆 / 搜索 | `NovelRagPanel.vue:494,495,496` | ⛏ |
| 86 | `Project` | 项目 | `rag/NovelRagInspectorDialog.vue:312` | ⛏ |

## 九、Pi 请求记录（观测面板）

全部硬编码在 `packages/neuro-book/app/components/novel-ide/agent/trace-viewer/AgentTraceDetail.vue`：

| # | 文本 | 应改值 | 位置（同文件行号） | 状态 |
|---|---|---|---|---|
| 87 | 页签 `System` / `Messages` / `Tools` / `Payload` | 系统提示 / 消息 / 工具 / 载荷 | `AgentTraceDetail.vue:39,40,41,42` | ⛏ |
| 88 | 字段 `Base URL` / `Stop reason` / `Turn` / `Mode` | 接口地址 / 停止原因 / 轮次 / 模式 | `AgentTraceDetail.vue:57,63,72,73` | ⛏ |
| 89 | 字段 `API` / `HTTP` / `TTFT` / `Invocation` / `Profile` | 保留（技术/性能字段；Profile 可随口径改"配置档"） | `AgentTraceDetail.vue:56,60,62,70,71` | 🔒/⛏ |
| 90 | 请求类型值 `health-check` / `model-check`、状态值 `error`、列表 `turn` | 建议加中文映射层（健康检查/模型检查/错误/轮次），或保留原值 | 数据值直显 | ⛏（低优先） |

## 十、登录页 / 管理后台

| # | 文本 | 应改值 | 位置 | 状态 |
|---|---|---|---|---|
| 91 | `bun run create-admin` / `config.yaml` | 命令与文件名保留；配套说明已走 t() 中文文案 | `pages/login.vue:111,116`（说明文案 `auth.setupAdmin*`、`auth.disableAuth*`） | ✅ |
| 92 | `auth.enabled` 配置键 | 保留 | `NovelIdeSettingsDialog.vue:906` | 🔒 |

## 十一、开发者预览页（低优先级）

| # | 项 | 口径 | 位置 |
|---|---|---|---|
| 93 | 14 个 `pages/*.preview.vue` 页头英文（`plot.preview.vue`、`world-engine.preview.vue`、`diff-workbench.preview.vue` 等） | **不翻译**；建议整体移出用户视野（对齐架构导览"调试页混排"整改），翻译是次要动作 | `packages/neuro-book/app/pages/*.preview.vue` |

---

## 执行注意（改前必读）

### 1. 测试断言直接钉死了组件英文字符串——改值必须同步改断言

`packages/neuro-book/app/utils/world-engine-ide-entry.test.ts`（源码包含性断言，改第 56、57、59、60、66 条对应组件时必须同步）：

- `:322` `expect(previewActions).toContain("Create Subject")`、`:1205` `expect(subjectCreator).toContain("Create Subject")`
- `:323` `expect(previewActions).toContain("Write Slice")`
- `:342` `expect(previewMutationBuilder).toContain("Mutation Builder")`、`:1105` `expect(mutationBuilder).toContain("Mutation Builder")`
- `:358` `expect(previewStatePanel).toContain("World State")`、`:359` `.toContain("State Query")`
- `:918` `expect(workbenchPreviewInspector).toContain("Subject file proposals")`
- `:1145-1146` `expect(objectValueEditor).toContain("Object Value"/"Object Fields")`
- `:1169` `expect(mutationEditorHeader).toContain("Edit Timeline")`
- `:80-82` 断言 `index.vue` 包含中文提示"World Engine 正在保存 Slice…"等——这些中文里嵌的 `World Engine`/`Slice` 若按口径改"世界引擎/切片"，断言也要同步。

`packages/neuro-book/app/utils/world-engine-workbench-preview.test.ts`：`:467` `toContain("Draft Queue")`；`:688`、`:795`、`:979` 是 `not.toContain` 防回潮断言（"Subject System"/"Mutation Editor"/"Review Focus"），重构文案时注意别误触。

### 2. 测试断言钉死了 zh-CN 词条值——动 i18n 值先搜测试

全仓 `*.test.ts` 有 **62 处** `toBe("中文")` 直接断言 locale 值（如 `AgentCacheRing.test.ts:32-37`、`AgentSessionScaleBar.test.ts:38`、`chat-work-blocks.test.ts:56` `toBe("世界引擎")`）。改第 29-33、54、69、83 条这类 i18n 值前，先 `grep -rn "键路径\|现值中文" app --include="*.test.ts"`。`diagnostic-messages.test.ts:30` 只断言 `agent.contextInspector.segment` 键存在，改值不挂。

### 3. 双语两文件必须成对改

`zh-CN.ts` 与 `en-US.ts` 各 2299+ 键、行号基本一一对应（2782/2781 行）。任何 i18n 键改动（第 9、29-33 条）两边同改，避免再引入键缺失；本基线此前核查为 0 缺失，别破坏。

### 4. 批次建议（继承词表优先级，按现状修正）

1. **第一批（纯 i18n 值，低风险）**：第 9、29-33 条 + 第 11 条（`WorkspaceFileNode.vue:326` 一行）。全部单点改动，无测试钉死英文值。
2. **第二批（Agent 面板硬编码）**：第 1-5、21-28 条，无测试断言引用这些字符串（已 grep 验证），可直接改。
3. **第三批（剧情面板）**：第 34-52 条，量大但无断言引用（`novel-writing-mode-entries.test.ts` 只断言组件引用关系与中文提示语，已核对 `:46,67,80`）。
4. **第四批（世界引擎，带测试同步）**：第 54-66 条，**每改一处同步改 `world-engine-ide-entry.test.ts` 对应断言**，建议同一 commit 内完成。
5. **第五批（配置中心+RAG+Pi 面板）**：第 75-90 条。
6. 预览页（第 51、93 条）与开发者草稿标签（第 67 条）暂缓，等"调试页移出视野"整改一起做。

### 5. 施工红线（本方案执行时）

- 只在 `task-prefab` worktree、只提交到 `task-prefab` 分支；禁碰 master、禁 push。
- `old-string` 逐处编辑；批量替换先 dry run。
- aria-label（第 1-2 条）改中文会影响读屏一致性与潜在 e2e 选择器，改前 `grep -rn "Application menu\|aria-label=\"Minimize"` 确认无测试引用（本次已查组件内引用，e2e 层未在本仓库发现，风险低）。
- 词表"待人工确认①（YOU/U 头像）"仍未解决，执行时跳过第 4 条，等口径。

---

## 核对证据（本次执行时实际运行）

- 假中文全量扫描：node 脚本逐行解析 `zh-CN.ts`，纯 ASCII 值仅 8 条（`zh-CN.ts:260,593,613,1027,1289,2193,2202,2609`），其中词表相关仅 `:260`（"English"，保留）。
- 词表 `文件:行号` 引用逐行回读：37 处组件引用全部命中（`PreviewActions.vue`→`WorldEnginePreviewActions.vue`、`ContextPanel.vue`→`WorldEngineContextPanel.vue` 为词表短名）；其中 6 处行号漂移已按现基线重新定位（`AgentToolNode.vue` 92-107→119-134、`AgentProfileModelFields.vue` 167→168、`WorkspaceCharacterDetailPanel.vue` 306→288-292/540-543 等）。
- 测试断言扫描：`grep -rn "toContain\(|toBe\(" app --include="*.test.ts"` 两轮（英文字符串/中文值），命中清单见上"执行注意"。
- 一次性核对脚本：`docs/tasks/evidence/_check_refs.mjs`、`_check_refs2.mjs`、`_check_refs3.mjs`（交付时已删除，未入库）。
