# ponytail-audit 审计报告 — neuro-book fork（2026-09-22）

- 审计范围：fork 后 `packages/` 下我方改动（基点 `45906272` = upstream/master，master 领先 119 commits，packages/ 内 60 文件 +2829/-901）。重点排序：fork 新增组件/工具 > 测试脚手架 > 配置样板 > 上游代码。
- 只读审计，未修改任何文件。只管过度工程与复杂度；正确性 bug / 安全 / 性能不在此列。
- 判定基线：本 fork 的 server 端改动（lease 自愈、`.nbook` 写拦截、Worker 内存上限、sync 防御捕获）与 009 单C 的 UI 拆除方向（删弹层、删草稿态、删配置项）整体是**削减性的**，质量好；真正的猎物集中在"弹层拆除拆了一半"的残留和少量样板。

## 发现清单（按削减量从大到小）

### 一、fork 领地（可改）

1. `delete` inline editor 控制器里整套已拆除弹层的死函数：`setSessionModelDraft` / `setSessionModelPopoverOpen` / `toggleSessionModelPopover` / `applySessionModelSettings` / `resetSessionModelSettings`（L587-640）+ `sessionModelPopoverOpen` ref（L106）+ 三个导出。替代=无（index.vue 在 009C1R2 已摘掉全部事件绑定，NovelPromptBar 只剩 `update-session-model-selection` 通道，零调用方）. [packages/neuro-book/app/composables/useInlineEditorAgentController.ts]
2. `delete` `sessionThinkingResolvedLabel` computed（L196-212）及其唯一消费者 helper `thinkingLevelLabel`（L838 起）+ 导出（L706）。替代=无（主聊天线同名 label 已删，inline 线的 props 也已从 index.vue/NovelPromptBar 删除）. [packages/neuro-book/app/composables/useInlineEditorAgentController.ts]
3. `delete` `inlineSessionThinkingResolvedLabel` computed（L628-639）+ 只被它调用的 `thinkingLevelLabel`（L913 起）。替代=无（全文件 grep 零调用方）. [packages/neuro-book/app/components/novel-ide/agent/AgentChatSurface.vue]
4. `delete` 弹层体系遗留 i18n 死键（双语各一套，共 10 行）：`agent.composer.followProfile` / `agent.composer.sessionModelParams` / `agent.composer.sessionModelDescription` / `agent.chatSurface.followProfileCurrent` / `agent.chatSurface.requestedEffective`。替代=无（组件侧引用已随弹层与死 computed 消失；删死键不动双语结构）. [packages/neuro-book/app/i18n/locales/zh-CN.ts L1812/1844/1845/2052-2053；en-US.ts 对应行]
5. `shrink` `getWorkspaceTreeTypeLabelKey` 九连 if → 查表 Record 一次索引。替代=`const KEY_BY_TYPE: Record<string,string> = {location: "...", ...}; export const getWorkspaceTreeTypeLabelKey = (t: string) => KEY_BY_TYPE[t] ?? null`（36 行 → 约 12 行）. [packages/neuro-book/app/components/novel-ide/workspace/workspace-entry-meta.ts L157-181]
6. `delete` 单类型文件 `agent-session-model-controls.ts`（全文件 7 行，只导出 `AgentSessionModelDraft`）。替代=类型就近搬进仍持有该状态的两个文件（AgentChatSurface.vue / useInlineEditorAgentController.ts）——"只导出一个东西的文件". [packages/neuro-book/app/components/novel-ide/agent/agent-session-model-controls.ts]
7. `delete` 三处死 import：`import type {AgentSessionModelDraft}` 在 AgentComposer.vue:10、NovelPromptBar.vue:7、index.vue:15——各文件仅此一处出现，零使用。替代=无. [packages/neuro-book/app/components/novel-ide/agent/AgentComposer.vue；packages/neuro-book/app/components/novel-ide/NovelPromptBar.vue；packages/neuro-book/app/pages/index.vue]
8. `shrink` 注入灰小字行模板三份拷贝（L557-569 会话开头收拢行 / L592-604 轮内 injectionGroup / L640-651 轮中途单条），错误色判定表达式同步重复 3 次。替代=抽一个局部渲染片段或小组件复用；错误判定直接导出 chat-work-blocks.ts 已有的 `isSystemErrorNode`（现未导出）. [packages/neuro-book/app/components/novel-ide/agent/AgentChatFlow.vue]（009 单C 产物，**已有保留决议**——实现优化，不计净削减）
9. `shrink` 四个组件契约测试各自重复 `readFile(fileURLToPath(new URL("./X.vue", import.meta.url)))` 且每个 it 内重复 `await readFile`。替代=共享一个 `readComponentSource(name)` helper + describe 级读一次（4 文件共省约 30 行样板；源码字符串断言本身是走查返修的回归锚，保留断言只收 IO 样板）. [AgentCacheRing.test.ts；AgentThinkingLevelSelect.test.ts；AgentSessionScaleBar.test.ts；AgentChatFlow.template.test.ts]（**已有保留决议**范围内的走查锚，不计净削减）
10. `shrink` `visibleRoundEntries` 里 `injectionGroup`/`toolGroup` 两个 return false 分支合并为 `if (entry.kind !== "node") return false`。替代=单条件过滤（省 4 行）. [packages/neuro-book/app/components/novel-ide/agent/AgentChatFlow.vue]（同 8，**已有保留决议**，不计净削减）
11. `shrink` `flowItemSpacingClass` 整函数只为 `index === 0 ? "" : "mt-4"`。替代=模板内联三元（省 3 行；注意 template.test.ts 断言了该函数原文，需同步改断言——这是源码断言脆性的直接例证）. [packages/neuro-book/app/components/novel-ide/agent/AgentChatFlow.vue]（**已有保留决议**，不计净削减）
12. `stdlib` 手写 `basename`（剥尾斜杠+lastIndexOf 切分）。替代=一行 `filePath.replace(/\/+$/, "").split("/").pop() ?? filePath`（浏览器侧不引 node:path；省 2 行）. [packages/neuro-book/app/components/novel-ide/workspace/workspace-frontmatter-profile.ts L141-144]

### 二、上游领地（fork 维护红线：仅列不改）

13. `native` nb-ui `useFloatingScrollbar`：145 行 JS 手搓胶囊滚动条（几何计算+鼠标拖拽+双向渐隐），浏览器原生 `scrollbar-width`/`::-webkit-scrollbar` 样式可覆盖大部分场景，拖拽手感是唯一增量。上游-仅列不改. [packages/nb-ui/src/composables/useFloatingScrollbar.ts]
14. `yagni` nb-ui 134 个组件的完整设计系统，本 fork 实际消费面远小于全集；属"带着整个 UI 库 fork"的固有形态，无动作。上游-仅列不改. [packages/nb-ui/src/components/]

### 三、已核实无猎物的区域

- fork 的 server 端五件（lease 自愈 / `.nbook` 写拦截 / bash mutatesWorkspace / execute_world approvalRequired / sync 防御捕获）均为最小定向修改，无抽象无包装。
- 依赖面：fork 未新增任何 runtime 依赖（package.json 仅 undici 版本升级 + devtools override），无 dep 可砍。
- workspace 三面板已统一走 parse→createDraft→renderDraft 对称管道（026/028 收敛完成），无残留私有副本。
- 话术包（assets/workspace/.nbook/agent/skills 改动）与双语 i18n 结构：**已有保留决议**，不评估。

## net

`net: -150 lines, -0 deps possible.`（1-7 与 12 计入；8-11 属保留决议内优化另计约 -60 行）

## TOP5 建议优先处理的

1. **弹层拆除残留死代码簇（发现 1-4、6-7）**：一次 sweep 约 -120 行 + 删一个文件 + 清 5 组死键，全部零调用方、机械可验（grep + typecheck），风险最低收益最大。建议单开一个小任务（串行分支即可，半小时内）。
2. **`getWorkspaceTreeTypeLabelKey` 查表化（发现 5）**：-24 行，5 分钟改动，纯等价重写，现有 14 条测试可直接回归。
3. **AgentChatFlow 注入行模板三份拷贝收敛（发现 8）**：-25 行且消除三处重复判定的漂移风险；因在 009 保留决议领地，需前线确认后作为微调件并入下一轮 UI 返修。
4. **测试 readFile 样板合并（发现 9）**：-30 行样板，并为日后源码断言测试立共享入口；同样走前线确认（走查锚属保留决议范围）。
5. **上游 useFloatingScrollbar 观察项（发现 13）**：不改动，仅登记——若未来上游重构滚动条为原生 CSS 方案，rebase 时可顺势受益。
