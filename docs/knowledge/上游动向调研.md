# 上游动向调研（notnotype/neuro-book）

- **日期**：2026-09-20 夜间，临时工程队执行，为任务008（上游新 UI 评测）储备情报。
- **数据口径**：首轮分析基于本地快照内一手文档（RELEASE.md、vitepress changelog 投影、PROJECT-STATUS、`.agents/tasks|works`）；**当晚网络恢复后已用 gh api 补齐实测**（仓库元数据、Releases、逐 commit、open issues/PRs、nb-ui 公开仓），并重新 `git fetch upstream`（master 未前进，仍为 `45906272` = 2026-09-14 的 0.10.3-canary，只新增了发布 tags）。fork 基线 550ec838 已包含该快照，**仅落后 6 天**。

## 一句话结论

上游处于**"主线收敛 + 发布链加固"阶段**：产品主线已收敛到 Novel 写作模式 v1，当前周节奏修发布链与桌面 beta 缺陷；**传闻中的"新 UI 重构"= Task 146 shadcn-vue 底座迁移，走的是独立 nb-ui 仓、分 5 阶段推进，阶段 1.5（主题系统）已于 2026-08-15 收口，主仓业务界面尚未切换（阶段 4 才接入）**；fork 当前基线（550ec838）已包含 0.10.3-canary 全部内容，仅落后 6 天。

## 1. 发布节奏：限量 canary，无 stable

| 发布线 | 版本范围 | 时间窗口 | 节奏 |
| --- | --- | --- | --- |
| 0.5.x | 0.5.0 – 0.5.7 | 2026-06-30 → 07-06 | 密集 |
| 0.7.x | 0.7.0 – 0.7.10 | 07-10 → 07-13 | 每 1-2 天一版 |
| 0.8.x | 0.8.0 – 0.8.19 | 07-15 → 07-25 | 每 1-2 天一版 |
| 0.9.x | 0.9.0 – 0.9.7 | 08-02 → 08-25 | 每周级 |
| 0.10.x | 0.10.2（09-08）、0.10.3（09-14） | 08-25 之后 | 周~双周 |

- 所有版本都带 `-canary`（限量早期验证版），**至今没有 stable**。跳号正常（构建失败/内部候选不对外）。
- **GitHub Releases 实测（gh api）**：对外只有 **8 个 release**——v0.10.2、v0.10.1、v0.10.0、v0.9.6、v0.9.3、v0.9.2（唯一 `prerelease=false`）、v0.9.1、v0.9.0。即：changelog 记录的 0.8.x 全系、0.9.4/0.9.5/0.9.7、以及**最新的 0.10.3 都没有 release 资产**（0.10.3 可能待发或仅内部分发）。0.10.x 实际发版日：0.10.0（09-07）、0.10.1（09-08 早）、0.10.2（09-08 晚）——同日双发是发布链修补所致。
- 完整版本号读法：`0.8.19-canary.20260720.112718Z.1b4c9685` = 版本 + 通道 + UTC 发布时刻 + 代码快照。
- Manager 是独立版本线（`0.1.0-canary.NN`，最新公开 canary.57/58+）。
- 发布质量门禁很重：每次发版有真机验收 job（Podman 双路径、无 node_modules 冷启动、逐字节 A/B 重建比对等），0.9.6→0.10.3 的更新说明几乎全是发布链与桌面缺陷修复——**上游当前的主要精力在"把发布做稳"，不是加功能**。

## 2. 最近功能方向

按 PROJECT-STATUS（08-17）+ 0.9.6–0.10.3 更新说明归纳：

1. **Novel 写作模式 v1 = 产品主线**，进入体验打磨。默认链路：灵感探索 → Project/Lorebook → World Engine 初始化 → 剧情规划与状态推进 → 章节写作 → 写后回补与修订。RAG/RP/simulation 不进普通写作默认入口。
2. **World Engine**：核心模型/Workbench 完成，CodeAct 读写模式（`execute_world` 工具）已落地——对应本仓 `codeact-sandbox.ts`。
3. **Plot 两棵树**：承载树（章节呈现）+ 因果树（剧情组织），`StoryScene` 连接。
4. **Agent/Workflow**：Provider API / 自动模型发现已合并（PR #101）；Workflow 持久身份、`wf.ask`、对话分支投影、取消/错误投影（Task 138/139/111）。
5. **Product Runtime / Manager / 桌面**：五平台 Product 容器 + Windows Portable + 公开 GHCR 资产已验收；Windows x64 Electron 桌面 beta（安装/UAC/修复/卸载，#87/#88）；签名安装器、自动更新、macOS 实包、stable 都还没做。
6. **0.10.3（09-14，当前 tip）**：修 Inline AI 发送失效（#227）、Product Runtime EPIPE 退出（#230）、Manager 打包链（#231/#232）、新增真实模型 smoke 命令 `test:real-model`。

## 3. 新 UI 重构进展（任务008 核心）

### 3.1 结构：双轨 UI

| 轨道 | 载体 | 状态 |
| --- | --- | --- |
| 桌面 Workbench Chrome | 主仓 desktop + Task 143/144/145 | **已进主线**：自绘标题栏（15 个命令的桌面菜单合同）、48px Activity Bar、四区标题栏、Inline Editor Agent、Agent/IDE 切换；Edge headless/headed 验收过 |
| shadcn-vue 底座（真正的"新 UI"） | **独立 sibling 仓 nb-ui**（上游作者本机 `GithubProjects/nb-ui`，不并入主仓 packages/） | 阶段化推进中，主仓未接入 |

### 3.2 Task 146：nb-ui shadcn-vue 重构（关键事实）

- **用户已拍板**（上游作者自述）：UI 底座采用 **shadcn-vue**；nb-ui **直接全面重构，不做保守试探**；nb-ui 保持独立发布形态（neuro-book-site 依赖它）。
- **技术要点**：交互内核用 **Reka UI**（要真实 ARIA 语义 + 键盘/焦点测试证据）；颜色只准来自主题变量合同；图标方案阶段 0 先实测动态图标名。
- **⚠️ 公开渠道看不到重构代码（gh api 实测）**：GitHub 上的 `notnotype/nb-ui` 仓存在且公开（"Shared Vue/Nuxt UI primitives for NeuroBook-derived projects"），但**只有 3 个 commit、最后推送停在 2026-07-28**（initial + file tree primitive）——这是 Task 85 时代的旧抽取，**Task 146 的 shadcn-vue 重构成果没有 push**（真实工作在作者本机 sibling 仓）。任务008 想看阶段 1–1.5 的实际代码，公开渠道拿不到，只能依据主仓快照内的 Task 146 文档、`packages/nb-ui/themes/editorial/` 主题落点与视觉 e2e 快照。
- **阶段路线与进度**：阶段 0 图标策略 → **阶段 1 框架替换 → 阶段 1.5 主题系统（2026-08-15 rounds 5-13 已收口）** → 阶段 2 定制 NeuroBook 风格组件 → 阶段 3 主仓领域无关组件搬入重写（功能不得低于主仓现版） → 阶段 4 才接入主仓（此前主仓零业务改动）。
- **设计语言**："编辑室工作台"方向（2026-08-10 拍板）：**主题从 8 套砍到 5 套（D10 新阵容）、双区制、5 组新 token**。规范落在 nb-ui 仓 `docs/design-language.md`（默认主题设计语言+踩坑清单）与 `docs/authoring-themes.md`（主题格式）。
- **主仓可见的落点**：`packages/nb-ui/themes/editorial/`（主题 + manifest + vars.css，**含明暗双视觉 e2e 快照**）——编辑室方向已部分落进主仓的 nb-ui 包；主仓主题现状是 8 套/36 变量（`app/utils/theme/README.md` v2.1，theme-tokens.ts 为事实源）。
- 配套证据：独立选型实验仓 `neuro-book-ui-framework-exploration`（decision-matrix / interaction-qa / engineering-cost 三份报告）。

### 3.3 对任务008 的三个直接推论

1. **评测对象要选对**：上游"新 UI"主仓还没有（阶段 4 未到）。可实测的是 ① 0.10.3-canary 的 Workbench Chrome + Novel IDE（8 主题 v2.1）现状；② `packages/nb-ui/themes/editorial/` 在快照内的代码与视觉快照；③ fork 里的 6 份 `docs/reference/*.html` 前任参考稿可与 nb-ui design-language 对照——**评测不必等上游接入完成，反而应趁阶段 2-3 窗口给方向输入**。
2. **fork 定位**（AGENTS.md 同步策略）：上游周~双周 canary 节奏下，fork 每周 rebase 一次可跟上；当前基线很新，无追赶压力。
3. **安全联动**：本次审计（`docs/security-audit-20260920/`）发现"项目内文件可变代码执行"且写端点不拦 `.nbook`。若上游未来随新 UI 开放模板/工作坊分享，该风险会被放大——值得作为评测之外的反馈项。

## 4. Issue / PR 动态（gh api 实测，2026-09-20）

- 仓库体量：**stars 679，open issues+PR 共 76**，最近 push 2026-09-17（master 本身停在 09-14，后续 push 在其他分支/管理操作）。
- **Open PR（5 个）**：#237 工具结果统一上下文预算并调小上限（最新）、#222 bound context compaction、#176 会话头部按钮 Tooltip、#171 Novel IDE 工具面板与导航整理、#17 **RP 分支 0.8.19-rp 发布**（RP 线还挂着，未死）。
- **Open issues 焦点**（按最近更新）：
  - **Agent 上下文管理是当前痛点**：#236/#237（plot 工具全量返回可致请求超窗）、#222。
  - **UI 迁移主仓侧三件套（任务008 直接相关）**：#191 将主应用 UI 底座迁移到 nb-ui、#192 类 VS Code 的 Workbench 与 View Host 抽象、#195 UI 重构后适配 Desktop Workbench 与 Title Bar。
  - 架构演进：#193 独立 neuro-agent-harness 重设计研究、#117 解耦 NeuroBook Harness、#194 插件与扩展控制面、#233 测试分层治理、#152 产品内教程。
  - 遗留 bug：#123 桌面版升级后租约失去所有权退出、#221 源码视图回跳富文本、#220。
- **逐 commit 实测（近 30 条）**：09-08–09-14 的工作与 RELEASE.md 完全对应（w00014 Inline AI 修复 → PR #235、#229 Manager bundle、#228 EPIPE）；09-14 之后 master 无新提交。另见 `chore(governance): register editor workbench spec migration and component docs work`（09-14）——编辑器工作台 Spec 迁移与组件文档工作已登记，UI 线持续在推。

## 5. 网络复核结果（2026-09-20 晚已执行）

原计划的复核清单全部跑完：仓库元数据、Releases 列表、逐 commit 历史、open issues/PRs、nb-ui 公开仓、`git fetch upstream`——结果已并入上文各节。后续例行盯梢沿用同样命令即可（见 git 历史里的上游盯梢定时任务，或手动跑）。

## 附：口径局限

- 本地 upstream/master 仍是单快照（squash 同步），逐 commit 历史靠 GitHub API 补齐（近 30 条），更早的 commit 级统计仍以发布线时间线为准。
- nb-ui 独立仓的重构进度**公开渠道不可见**（公开仓停更 07-28），阶段 2–4 实时状态只能靠上游发布/文档间接推断。
- issue 的开闭状态为当日快照，随后会漂移。

## 6. 正式 008 触发口径（2026-09-20 前线裁定）

新 UI/新主题须**覆盖到工作台主页面**（对应 issue #192 Workbench/View Host 抽象落地）才触发正式评测（任务008）；单页覆盖（如 editorial 主题目前只应用到 world-engine 预览页）仅够"预评"不触发。每日盯梢（见附录台账）的判读同此口径：#191/#192/#195 状态变化或 UI 相关提交/release，只有体现 Workbench 主页面落地特征时才报"触发条件满足"，否则只记动态并注明"仅够预评"。流水线本身已由任务018 演习就绪（模板：`008评测演习报告.md`）。

> **登录态变量补充（2026-09-20 任务019 走查发现，不改主判据）**：editorial 米色主题的实际分布与登录态相关——**未登录视图=editorial 米色，登录后=蓝白默认**（world-engine 预览页除外）。正式评测与截图对比时，登录态须作为记录变量（同一页面登录/未登录分别留档）。

> **分支级监测（2026-09-20 起）**：upstream fetch 配置已改为拉全分支（`+refs/heads/*:refs/remotes/upstream/*`，基线 84 个）。盯梢不只看 master——UI 重构代码可能先落在 feature/refactor 分支上再合并，分支级动态（新分支出现、活跃分支新提交）同样按主判据判读：仅当分支动态体现 Workbench 主页面落地特征才报触发，否则记"预评"级动态。

## 附录：上游 HEAD 盯梢记录（任务008 触发监测）

- 2026-09-20 09:00 | 4590627 | 首次基线
- 2026-09-20 分支级基线 | master 4590627 | 分支 84 个 | fetch 配置改为拉全分支（refs/heads/*），盯梢口径升级为分支级（见第 6 节注记）
